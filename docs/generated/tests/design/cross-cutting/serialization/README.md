---
generated: true
model: claude-fable-5
generated_at: 2026-07-08T14:59:37Z
source_commit: 8ac9e49a5b7020af6a8040544b9f76f0fbf40898
watched_paths_digest: 60f1a8a05667738d86c14e10379e954c06900dc12cede14092b14ad907cc71b2
source_doc: docs/generated/design/cross-cutting/serialization.md
source_doc_digest: 1bd6466a65577a768ef03f48f4d41c5efe70ac2cecff8f88540217809c9b628f
warning: "Auto-generated. May drift from source. Do not edit by hand."
---

# Tests for cross-cutting/serialization

## Intent

Tests verify the serialization-machinery claims in
[`docs/generated/design/cross-cutting/serialization.md`](../../../design/cross-cutting/serialization.md)
that survive into the `slangc` CLI: `.slang-module` round-tripping
(write with `-o`, read with `-dump-module` / `-get-module-info`), the
`-get-supported-module-versions` probe that exposes the
backwards-compat version range, the `EmbeddedDownstreamIR` marker that
appears when `-embed-downstream-ir` is used, the IR flat-module read
path whose parent/child rebuild and string-table resolution are
observable through a write/`-dump-module` round-trip, and the
source-location serialization that surfaces as `#line` directives in
C-family text and `OpSource` / `OpString` markers in SPIR-V assembly.

Most of the source doc is internal — the `serialize(serializer, value)`
template pattern, the Fossil memory-mappable property, the raw RIFF
chunk layout, the flat-module table encoding and its
`SLANG_RELEASE_ASSERT` validation, the deprecated repro round-trip,
and the "adding a new serialized field" walkthrough are all out of
scope because they have no slangc-CLI observable. The bundle is
therefore small by design (11 tests).

## Functional coverage

| Claim                                                                                                                                                                                  | Intent     | Anchor                                                                                                                           | Tests                                                                                              |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| deserializeFromFlatModule rebuilds parent/child links from the flat childCounts table; nested declaration structure (a struct with fields and a method, plus a generic function) survives a module write/read round-trip intact.                                              | functional | [#ir-flat-module-read-path](../../../design/cross-cutting/serialization.md#ir-flat-module-read-path)                             | [`module-roundtrip-rebuilds-nested-children.slang`](module-roundtrip-rebuilds-nested-children.slang) |
| IR flat-module serialization stores string payloads in separate stringLengths/stringChars tables; a string literal inside a module function survives the write/read round-trip and reappears verbatim under -dump-module.                                                    | functional | [#ir-flat-module-read-path](../../../design/cross-cutting/serialization.md#ir-flat-module-read-path)                             | [`module-roundtrip-preserves-string-literal.slang`](module-roundtrip-preserves-string-literal.slang) |
| Container packaging composes AST + IR + auxiliary data; with -embed-downstream-ir a SPIR-V blob joins the container and surfaces as an EmbeddedDownstreamIR marker under -dump-module. | functional | [#riff-container-format](../../../design/cross-cutting/serialization.md#riff-container-format)                                   | [`embed-downstream-ir-spirv.slang`](embed-downstream-ir-spirv.slang)                               |
| Negative control to the -embed-downstream-ir claim: without the flag, the container does not carry a downstream-IR chunk and -dump-module shows no EmbeddedDownstreamIR marker.        | regression | [#riff-container-format](../../../design/cross-cutting/serialization.md#riff-container-format)                                   | [`no-embed-downstream-ir-absent.slang`](no-embed-downstream-ir-absent.slang)                       |
| Source locations survive the compile and reappear in emitted text as #line directives on C-family targets and as OpSource/OpLine markers on SPIR-V.                                    | functional | [#source-location-serialization](../../../design/cross-cutting/serialization.md#source-location-serialization)                   | [`source-loc-line-directives-multi-target.slang`](source-loc-line-directives-multi-target.slang)   |
| When debug info is requested, source-location serialization expands to a debug-info stream that names the source file via an OpString in SPIR-V.                                       | functional | [#source-location-serialization](../../../design/cross-cutting/serialization.md#source-location-serialization)                   | [`source-loc-spirv-debug.slang`](source-loc-spirv-debug.slang)                                     |
| A serialized module carries its name and version; -get-module-info reads them back, proving the version gate the doc names is recorded at write time.                                  | functional | [#versioning-and-backwards-compatibility](../../../design/cross-cutting/serialization.md#versioning-and-backwards-compatibility) | [`module-info-name-version.slang`](module-info-name-version.slang)                                 |
| The compiler exposes the inclusive [min, max] range of module versions it accepts; -get-supported-module-versions prints the range that backwards-compat policy relies on.             | functional | [#versioning-and-backwards-compatibility](../../../design/cross-cutting/serialization.md#versioning-and-backwards-compatibility) | [`supported-module-versions.slang`](supported-module-versions.slang)                               |
| The version baked into a freshly-written module is a positive integer; the version-gate machinery the doc names always stamps a value.                                                 | functional | [#versioning-and-backwards-compatibility](../../../design/cross-cutting/serialization.md#versioning-and-backwards-compatibility) | [`module-version-in-range.slang`](module-version-in-range.slang)                                   |
| A .slang-module written by slangc can be re-read by the compiler and its IR dumped, demonstrating the AST/IR/container round-trip the doc describes.                                   | functional | [#what-is-serialized](../../../design/cross-cutting/serialization.md#what-is-serialized)                                         | [`module-round-trip-dump.slang`](module-round-trip-dump.slang)                                     |
| AST-serialization preserves the checked AST that backs an import-able module; the public symbol's export name and type signature survive the write/read cycle.                         | functional | [#what-is-serialized](../../../design/cross-cutting/serialization.md#what-is-serialized)                                         | [`module-roundtrip-preserves-public-symbol.slang`](module-roundtrip-preserves-public-symbol.slang) |

## Untested claims

| Claim                                                                                                                                                                                                                                             | Reason                 | Anchor                                                                                                                           | Why untested                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Container packaging on -target dxil — with -embed-downstream-ir a DXIL blob joins the container and surfaces as `EmbeddedDownstreamIR(10 : Int, ...)` under -dump-module. The SPIR-V variant in Functional coverage exercises the same code path. | gpu-dxc-dxil           | [#riff-container-format](../../../design/cross-cutting/serialization.md#riff-container-format)                                   | Producing a DXIL blob requires the DXC binary, which the agent runtime does not include. CI nightly has DXC; this would be a single-CHECK `EmbeddedDownstreamIR(10 : Int,` smoke test once gated on DXC availability. |
| The Fossil backend's memory-mappable property (`#fossil-backend`).                                                                                                                                                                                | (unclassified)         | [#fossil-backend](../../../design/cross-cutting/serialization.md#fossil-backend)                                                 | an internal performance claim with no observable.                                                                                                                                                                     |
| `Unrecognized`-opcode deserializer behaviour (`#versioning-and-backwards-compatibility`).                                                                                                                                                         | (unclassified)         | [#versioning-and-backwards-compatibility](../../../design/cross-cutting/serialization.md#versioning-and-backwards-compatibility) | would require constructing a module whose opcode set exceeds the current compiler's, which the CLI cannot do.                                                                                                         |
| The `serialize(serializer, value)` template pattern (`#the-serialize-pattern`).                                                                                                                                                                   | needs-unit-test        | [#the-serialize-pattern](../../../design/cross-cutting/serialization.md#the-serialize-pattern)                                   | a C++ idiom, no CLI surface.                                                                                                                                                                                          |
| The `SLANG_SERIALIZE_FOSSIL_ENABLE_VALIDATION_CHECKS` toggle (`#fossil-backend`).                                                                                                                                                                 | compile-time-toggle    | [#fossil-backend](../../../design/cross-cutting/serialization.md#fossil-backend)                                                 | baked into the binary at compile time.                                                                                                                                                                                |
| `-dump-repro` / `-load-repro` round-trip (`#round-trip-and-repro-files`).                                                                                                                                                                         | deprecated             | [#round-trip-and-repro-files](../../../design/cross-cutting/serialization.md#round-trip-and-repro-files)                         | deprecated per CLAUDE.md.                                                                                                                                                                                             |
| The "Adding a new serialized field" developer walkthrough (`#adding-a-new-serialized-field`).                                                                                                                                                     | process-doc            | [#adding-a-new-serialized-field](../../../design/cross-cutting/serialization.md#adding-a-new-serialized-field)                   | process documentation.                                                                                                                                                                                                |
| Raw RIFF chunk-code inspection (`#riff-container-format`).                                                                                                                                                                                        | requires-external-tool | [#riff-container-format](../../../design/cross-cutting/serialization.md#riff-container-format)                                   | would require parsing bytes; the doc defers chunk codes to source files.                                                                                                                                              |
| `-line-directive-mode source-map` zip emission (`#source-location-serialization`) — the FileCheck runner cannot see inside a zip archive without an external `unzip` step. The default `#line` mode covers the same user-facing round-trip claim. | requires-external-tool | [#source-location-serialization](../../../design/cross-cutting/serialization.md#source-location-serialization)                   | Verification would need a non-Slang tool the runner does not include (e.g., `unzip`, hex dumper, decompiler).                                                                                                         |
| The flat-module table encoding: parallel `instAllocInfo` / `childCounts` / `sourceLocs` arrays walked in preorder by `traverseInstsInSerializationOrder`, with a single `operandIndices` list holding a type-use slot then each operand and `nullptr` encoded as `-1`. | internal-source-fact | [#ir-flat-module-read-path](../../../design/cross-cutting/serialization.md#ir-flat-module-read-path) | The array layout and the `-1` null convention have no CLI observable beyond the round-trip already covered; pinning them would need raw-byte inspection or a C++ unit test against the serializer. |
| `deserializeFromFlatModule` validates flat-table invariants with `SLANG_RELEASE_ASSERT` before dereferencing (per-inst table sizes, `-1 <= index < numInsts` operand range, string-length bounds) and asserts full table consumption plus an `IRModuleInst` root after the walk. | needs-unit-test | [#ir-flat-module-read-path](../../../design/cross-cutting/serialization.md#ir-flat-module-read-path) | Triggering the asserts requires a malformed `.slang-module` the CLI cannot produce; a C++ unit test feeding corrupted flat tables to the reader would verify each check fires. |
| The literal/string-consumption check is skipped when the reader mapped unknown opcodes to `Unrecognized`, deferring to the recoverable read failure in the container layer. | needs-unit-test | [#ir-flat-module-read-path](../../../design/cross-cutting/serialization.md#ir-flat-module-read-path) | Requires a module carrying opcodes newer than the reading compiler, which this build's CLI cannot author; a C++ unit test with a hand-built payload could. |
| Write and read sides share the `kMaxIRSerializationDepth` (512) recursion budget so deeply-nested modules fail fast instead of overflowing the stack. | needs-unit-test | [#ir-flat-module-read-path](../../../design/cross-cutting/serialization.md#ir-flat-module-read-path) | Surface Slang code cannot produce IR nested anywhere near 512 levels, and exceeding the budget manifests as an assert rather than a diagnostic a `DIAGNOSTIC_TEST` could pin. |
| Most enum-valued AST fields get their `serialize(...)` overload from a FIDDLE template driven by a single `enumTypeNames` list, delegating to `serializeEnum` which encodes the value as a `FossilUInt`. | internal-source-fact | [#what-is-serialized](../../../design/cross-cutting/serialization.md#what-is-serialized) | Which mechanism generated the overload is invisible in CLI output — an enum field round-trips identically whether serialized by the template or a bespoke function. |
| Source-location reconstruction uses `createSourceFileWithSize` to build a placeholder-sized file plus a single view; the file's content is not serialized. | internal-source-fact | [#source-location-serialization](../../../design/cross-cutting/serialization.md#source-location-serialization) | The reconstruction mechanism is only visible in C++; the emitted-text tests in Functional coverage already cover the user-facing consequence (path and line numbers survive). |

## Doc gaps observed

| Anchor                                                                                                                           | Kind                  | Gap                                                                                                                                                                                                                                                                                                                                                                                | Suggested addition |
| -------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| [#what-is-serialized](../../../design/cross-cutting/serialization.md#what-is-serialized)                                         | undocumented-behavior | `## What is serialized` lists AST + IR + container as the three serialized flavors but does not name `-dump-module` or `-get-module-info` as the user-facing CLI commands that read each back. A "User-facing CLI" subsection naming `-dump-module` / `-get-module-info` / `-get-supported-module-versions` would let tests cite a CLI anchor instead of inferring from the prose. |                    |
| [#riff-container-format](../../../design/cross-cutting/serialization.md#riff-container-format)                                   | undocumented-behavior | `## RIFF container format` describes container-level orchestration but never spells out the `EmbeddedDownstreamIR` chunk marker that `-dump-module` surfaces. The marker is observable in CLI output; the doc should note it as the user-visible name of the downstream-IR chunk.                                                                                                  |                    |
| [#versioning-and-backwards-compatibility](../../../design/cross-cutting/serialization.md#versioning-and-backwards-compatibility) | undocumented-behavior | `## Versioning and backwards compatibility` says "the C++ side carries a versioning gate" but does not mention that the version integer is exposed via `slangc -get-module-info` and the supported range via `slangc -get-supported-module-versions`.                                                                                                                              |                    |
| [#source-location-serialization](../../../design/cross-cutting/serialization.md#source-location-serialization)                   | undocumented-behavior | `## Source-location serialization` describes round-tripping internal location records but does not enumerate the target-side shape of those locations (`#line` for C-family, `OpSource` / `OpString` / `OpLine` for SPIR-V, none for WGSL). Tests had to observe the per-target shape directly.                                                                                    |                    |
| [#source-location-serialization](../../../design/cross-cutting/serialization.md#source-location-serialization)                   | ambiguous-claim       | The doc says both the unadjusted and the `#line`-adjusted line-start tables are captured per source file, but does not say which view the consumers use — e.g. whether emitted `#line` directives or post-reload diagnostics report the physical line or the `#line`-remapped one when the source itself contains a `#line` directive. A test cannot pin an expectation without guessing between the two tables.                                                                    | State which of the two line-start tables the emitted-text and diagnostic paths consult when a source file contains `#line` directives, ideally with a short before/after example. |
| [#what-is-not-in-this-document](../../../design/cross-cutting/serialization.md#what-is-not-in-this-document)                     | undocumented-behavior | The `## What is not in this document` section names `slang-fossil.h` for the fossil-chunk layout but the doc itself does not appear in the watched paths for this bundle, so even the format-level layout cannot be cross-referenced from inside a citation.                                                                                                                       |                    |
