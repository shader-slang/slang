---
generated: true
model: claude-opus-5[1m]
generated_at: 2026-08-13T00:00:00+00:00
source_commit: c0e5ca5c55ff5ea6b210ac9418bac04728cc45e0
watched_paths_digest: 3b23aa8745718c3c9b2740ec584b30f97dee58483ad3f8efe781def69aa1b5d4
source_doc: docs/generated/design/syntax-reference/tokens.md
source_doc_digest: 9d5ada50ee544f1140e8ec2680b782e744150aa0799083a207772d40391d0b73
warning: "Auto-generated. May drift from source. Do not edit by hand."
---

# Tests for design/syntax-reference/tokens

## Intent

This bundle exercises the lexer-surface claims in
[`docs/generated/design/syntax-reference/tokens.md`](../../../../design/syntax-reference/tokens.md):
how a source character stream becomes a token stream. Coverage is
organised by the doc's own sections — the content-token catalog
(literals and their suffixes), the trivia rules for comments, the
punctuation / operator spelling tables, the preprocessor markers, the
token-flag consequences, the special-case lexing rules, and the
source-location claim. Every claim is target-independent (it is
resolved before any back-end runs), so tests use `INTERPRET`,
`COMPARE_COMPUTE -cpu`, `DIAGNOSTIC_TEST`, or a `-target cpp` text
check when the observation is a string that only survives into emitted
source; no test needs a GPU or a downstream toolchain.

Because a token kind is not directly observable through `slangc`, each
test picks the smallest program whose acceptance (or diagnostic) is
only possible under the documented tokenization — e.g. a compound
assignment that could not parse if `<<=` lexed as `<<` then `=`, or a
qualified call that could not resolve if `::` lexed as two `Colon`
tokens.

## Claims

Enumerated per [`_claims.md` §1](../../../_meta/prompts/_claims.md), grouped by
the document's own headings.

**`#token-kind-taxonomy`**

1. Tokens come in five groups: end markers, content tokens, trivia, preprocessor markers, and punctuation / operators.
2. Every punctuation kind is both a `TokenType` enumerator and a diagnostic spelling, because `PUNCTUATION(id, text)` expands to `TOKEN(id, "'<text>'")`.

**`#end-markers-and-special`**

3. `Unknown` should not appear in valid input.
4. `EndOfFile` is returned when the lexer reaches end of input.
5. A character matching no dispatch arm and not a non-ASCII code point yields `Invalid`; the diagnostic chosen depends on the character's value, and is suppressed entirely under `kLexerFlag_SuppressDiagnostics` while the `Invalid` token is produced either way.

**`#content-tokens`**

6. `Identifier` covers every keyword; classification is deferred to the parser via syntax-decl lookup.
7. `IntegerLiteral` accepts decimal, `0x`/`0X` hex, `0b`/`0B` binary, and a leading-zero octal run.
8. `FloatingPointLiteral` accepts `1.5`, `1.`, `.5`, `1e10` / `0e-3`, `0x1.8p3`, and the legacy `1#INF` / `0#INF` form.
9. A dot followed by `x` or `r` does not start a fraction: `1.xxx` lexes as `IntegerLiteral`, `Dot`, `Identifier`, because that spelling is reserved for swizzling a scalar literal.
10. `StringLiteral` raw text includes the opening and closing quotes; escapes are decoded and validated later.
11. `CharLiteral`'s one-character rule is enforced at decode time, not by the lexer, which only finds the closing quote.
12. Neither numeric rule validates its suffix: any run of ASCII letters, digits and underscores after the numeric body is folded into the token's raw text, so there is no fixed suffix set at lex time.
13. Integer digits that do not fit in 64 bits are `E10012`, `integerLiteralTooLargeForAnyType`.
14. The accepted integer suffixes are `u`/`U`, `l`/`L`, `ll`/`LL` and `z`/`Z` in any order, with the two letters of `ll` matching in case; anything else is *invalid suffix '...' on integer literal*.
15. The accepted float suffixes are empty or `f`/`F` for `float`, `h`/`H`/`hf`/`HF`/`fh`/`FH` for `half`, and `l`/`L`/`lf`/`LF`/`fl`/`FL` for `double`.
16. A one-letter float suffix is case-insensitive; a two-letter one must have both letters in the same case, so `hf` and `HF` are accepted while `Hf` is not.

**`#trivia-whitespace-and-comments`**

17. The lexer emits whitespace, newlines, line comments and block comments as their own tokens so consumers can choose whether to skip them.
18. Nested block comments are not supported.

**`#preprocessor-markers`**

19. `Pound`, `PoundPound` and `CompletionRequest` (`#?`, emitted at the cursor position) are distinct kinds.

**`#punctuation-and-structural-symbols`**

20. `DotDot` and `At` are lexed as distinct kinds but have no parser consumer at this commit.
21. `DoubleRightArrow` (`=>`) has lambda syntax as its only parser consumer.
22. `Dollar` prefixes a Slang value operand inside a `spirv_asm` block and introduces the compile-time `$for`; `DollarDollar` prefixes a *type* operand.
23. `Scope` (`::`) is the namespace / qualified-name separator.

**`#operators`**

24. The lexer takes the longest spelling that matches, implemented as nested lookahead rather than a table, with the shorter kind returned only from a `default:` arm.
25. `>>=` is therefore a single token, `>=` comes from an `=` directly after the first `>`, and a bare `>` gives `OpGreater`.
26. The choice is made with no parse context; `OpLess` is the one case the parser has to revisit, to disambiguate it from generic application.

**`#token-data-layout` / `#token-flags`**

27. `charsNameUnion` is a tagged union: with the `Name` flag set the text is interned as a `Name*`, otherwise the token holds a raw pointer plus length into the source buffer.
28. `AtStartOfLine` marks the first token on a *logical* line and is preserved across intervening whitespace and comments, so an escaped newline — which emits no `NewLine` token — does not start a new one.
29. A preprocessor directive body may therefore be continued onto the next physical line: `#define BUMP(x) \` followed by `((x) + 1)` defines a macro whose body is `((x) + 1)`.
30. A `Pound` token starts a directive only when it carries `AtStartOfLine`.
31. `AfterWhitespace` records that a token was preceded by whitespace, and the preprocessor reads it both to preserve spacing when stringizing and to tell a function-like macro definition (`NAME(` with no gap) from an object-like one.
32. `ScrubbingNeeded` records that a line continuation was folded, so `Lexer::lexToken` can strip it from the stored content.

**`#special-case-lexing-rules`**

33. A `\` immediately before a newline is consumed and folded out, but the resulting token's source location still refers to the original physical line.
34. Continuation folding sits below comment recognition, so a `//` comment whose line ends in `\` swallows the next physical line as well.
35. The lexer has no include-header mode: `<...>` after `#include` is emitted as ordinary `OpLess`, path, and `OpGreater` tokens, and the path is reassembled by the preprocessor.

## Functional coverage

| Claim                                                                                                                                                                                       | Intent     | Anchor                                                                                                                  | Tests                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| A double-quoted `StringLiteral` is recognized by the lexer and its text content survives end-to-end through to emitted code.                                                                | functional | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`string-literal-basic.slang`](string-literal-basic.slang)                                     |
| A single-quoted character literal (`CharLiteral`) is recognized by the lexer and carries the character's integer code-point value.                                                          | functional | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`char-literal-value.slang`](char-literal-value.slang)                                         |
| Boundary - `2147483647` (int32 MAX) is a valid IntegerLiteral that fits in `int` without overflow.                                                                                          | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`int-literal-int32-max.slang`](int-literal-int32-max.slang)                                   |
| Boundary - a CharLiteral whose content is an escape sequence (`'\\'`, `'\n'`) lexes as one token and decodes to the escaped code point.                                                     | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`char-literal-escape-backslash.slang`](char-literal-escape-backslash.slang)                   |
| Boundary - a binary integer literal (`0b1111`) lexes as IntegerLiteral and decodes to its base-2 value.                                                                                     | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`int-literal-binary-base.slang`](int-literal-binary-base.slang)                               |
| Boundary - a hexadecimal integer literal (`0xFF`) is a valid IntegerLiteral and decodes to its base-16 value.                                                                               | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`int-literal-hex-base.slang`](int-literal-hex-base.slang)                                     |
| Boundary - an octal integer literal (`0777`) lexes as IntegerLiteral and decodes to its base-8 value.                                                                                       | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`int-literal-octal-base.slang`](int-literal-octal-base.slang)                                 |
| Boundary - escape sequences (`\t`, `\n`, `\\`, `\"`) inside a regular StringLiteral are accepted by the lexer and round-trip through to emitted C++.                                        | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`string-literal-escape-sequences.slang`](string-literal-escape-sequences.slang)               |
| Boundary - the `h` suffix on a FloatingPointLiteral types it as `half` (16-bit float).                                                                                                      | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`numeric-suffix-h-is-half.slang`](numeric-suffix-h-is-half.slang)                             |
| Boundary - the `lf` suffix on a FloatingPointLiteral types it as `double` (64-bit float).                                                                                                   | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`numeric-suffix-lf-is-double.slang`](numeric-suffix-lf-is-double.slang)                       |
| Boundary - the `ll` suffix on an IntegerLiteral types it as `int64_t` (signed 64-bit).                                                                                                      | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`numeric-suffix-ll-is-int64.slang`](numeric-suffix-ll-is-int64.slang)                         |
| Boundary - the `ull` suffix on an IntegerLiteral types it as `uint64_t` (unsigned 64-bit).                                                                                                  | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`numeric-suffix-ull-is-uint64.slang`](numeric-suffix-ull-is-uint64.slang)                     |
| Boundary - the empty StringLiteral (`""`) is a valid token whose content is zero characters.                                                                                                | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`string-literal-empty.slang`](string-literal-empty.slang)                                     |
| Boundary - the integer literal `0` lexes as IntegerLiteral with int type at the lower edge of the int range.                                                                                | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`int-literal-zero.slang`](int-literal-zero.slang)                                             |
| Boundary - the null escape `'\0'` lexes as a CharLiteral and decodes to integer 0.                                                                                                          | boundary   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`char-literal-null.slang`](char-literal-null.slang)                                           |
| Negative - a CharLiteral with more than one character (`'AB'`) is rejected with the "illegal character literal" diagnostic.                                                                 | negative   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`negative-multi-char-literal.slang`](negative-multi-char-literal.slang)                       |
| Negative - an integer literal followed by a non-documented suffix (`xyz`) triggers the "invalid suffix on integer literal" diagnostic.                                                      | negative   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`negative-invalid-int-suffix.slang`](negative-invalid-int-suffix.slang)                       |
| Negative - an integer literal too wide for any built-in integer type triggers the documented "integer literal is too large" diagnostic.                                                     | negative   | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`negative-integer-literal-too-large.slang`](negative-integer-literal-too-large.slang)         |
| The `f` suffix on a floating-point literal types the token as `float` (single precision), distinguishing it from the unsuffixed form.                                                       | functional | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`numeric-suffix-f-is-float.slang`](numeric-suffix-f-is-float.slang)                           |
| The `u` suffix on an integer literal makes the token's type unsigned; the suffix is part of the literal token's raw text and is decoded by the parser/checker.                              | functional | [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | [`numeric-suffix-u-is-uint.slang`](numeric-suffix-u-is-uint.slang)                             |
| Negative - a character matching no lexer dispatch arm yields an `Invalid` token and the printable-character form of the illegal-character diagnostic (E10000).                              | negative   | [#end-markers-and-special](../../../../design/syntax-reference/tokens.md#end-markers-and-special)                       | [`negative-illegal-character.slang`](negative-illegal-character.slang)                         |
| Each compound-assignment spelling (`<<=`, `>>=`, `&=`, `\|=`, `^=`, `%=`) lexes as one operator token rather than as an operator followed by a separate `=`.                                | functional | [#operators](../../../../design/syntax-reference/tokens.md#operators)                                                   | [`operator-compound-assign-tokens.slang`](operator-compound-assign-tokens.slang)               |
| Each multi-character comparison / logical / increment spelling (`<<`, `>>`, `==`, `!=`, `>=`, `<=`, `&&`, `\|\|`, `++`, `--`) lexes as one operator token.                                  | functional | [#operators](../../../../design/syntax-reference/tokens.md#operators)                                                   | [`operator-comparison-and-logical-tokens.slang`](operator-comparison-and-logical-tokens.slang) |
| The two-character spelling `##` lexes as a single `PoundPound` token that the preprocessor uses to paste adjacent macro arguments into one identifier.                                      | functional | [#preprocessor-markers](../../../../design/syntax-reference/tokens.md#preprocessor-markers)                             | [`preprocessor-pound-pound-token-paste.slang`](preprocessor-pound-pound-token-paste.slang)     |
| The three-character spelling `...` lexes as a single `Ellipsis` token, which the preprocessor consumes as a variadic macro parameter list.                                                  | functional | [#punctuation-and-structural-symbols](../../../../design/syntax-reference/tokens.md#punctuation-and-structural-symbols) | [`punctuation-ellipsis-variadic-macro.slang`](punctuation-ellipsis-variadic-macro.slang)       |
| The two-character spelling `->` lexes as a single `RightArrow` token, which introduces a function's return type.                                                                            | functional | [#punctuation-and-structural-symbols](../../../../design/syntax-reference/tokens.md#punctuation-and-structural-symbols) | [`punctuation-right-arrow-return-type.slang`](punctuation-right-arrow-return-type.slang)       |
| The two-character spelling `::` lexes as a single `Scope` token that separates a namespace from the name it qualifies.                                                                      | functional | [#punctuation-and-structural-symbols](../../../../design/syntax-reference/tokens.md#punctuation-and-structural-symbols) | [`punctuation-scope-qualified-name.slang`](punctuation-scope-qualified-name.slang)             |
| The two-character spelling `=>` lexes as a single `DoubleRightArrow` token, whose only consumer is lambda syntax.                                                                           | functional | [#punctuation-and-structural-symbols](../../../../design/syntax-reference/tokens.md#punctuation-and-structural-symbols) | [`punctuation-double-right-arrow-lambda.slang`](punctuation-double-right-arrow-lambda.slang)   |
| A token's nominal source location honours `#line`, so `__LINE__` after `#line 100` reports 100 rather than the physical line.                                                               | functional | [#source-location](../../../../design/syntax-reference/tokens.md#source-location)                                       | [`source-location-line-directive.slang`](source-location-line-directive.slang)                 |
| A `\'` inside a character literal is stepped over while lexing, so `'\''` is one CharLiteral that decodes to the apostrophe code point 39.                                                  | boundary   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`char-literal-escaped-quote.slang`](char-literal-escaped-quote.slang)                         |
| A backslash immediately before a newline is consumed and folded out; the two halves of an identifier split across the continuation become a single token.                                   | functional | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`backslash-line-continuation.slang`](backslash-line-continuation.slang)                       |
| A bare `0` followed by a decimal exponent (`0e10`) lexes as a single FloatingPointLiteral, not as an integer literal with a bogus suffix.                                                   | functional | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`float-literal-zero-exponent.slang`](float-literal-zero-exponent.slang)                       |
| Block comments do not nest; an inner `/*` inside `/* ... */` is content, and the outer comment ends at the first `*/`.                                                                      | functional | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`block-comment-no-nesting.slang`](block-comment-no-nesting.slang)                             |
| Boundary - a leading-zero exponent with a `-` sign (`0e-3`) lexes as a single FloatingPointLiteral token.                                                                                   | boundary   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`float-literal-zero-exponent-minus-sign.slang`](float-literal-zero-exponent-minus-sign.slang) |
| Boundary - a leading-zero exponent with an explicit `+` sign (`0e+1`) lexes as a single FloatingPointLiteral token.                                                                         | boundary   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`float-literal-zero-exponent-plus-sign.slang`](float-literal-zero-exponent-plus-sign.slang)   |
| Boundary - a multi-character delimiter (`long`) on a raw string opens with `R"long(` and closes only at `)long"`.                                                                           | boundary   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`raw-string-multi-char-delimiter.slang`](raw-string-multi-char-delimiter.slang)               |
| Boundary - a raw string with an empty delimiter (`R"(...)"`) lexes correctly, terminating only at `)"`.                                                                                     | boundary   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`raw-string-empty-delimiter.slang`](raw-string-empty-delimiter.slang)                         |
| Boundary - an uppercase exponent marker after a bare zero (`0E5`) still lexes as a single FloatingPointLiteral.                                                                             | boundary   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`float-literal-zero-exponent-uppercase.slang`](float-literal-zero-exponent-uppercase.slang)   |
| Boundary - backslash-line-continuation runs before lexing, so a `\<newline>` at the end of a `//` line splices the following physical line into the comment and the line is consumed by it. | boundary   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`line-comment-backslash-extends.slang`](line-comment-backslash-extends.slang)                 |
| Inside a raw string literal `R"d(...)d"`, backslashes are taken literally — no escape processing is performed by the lexer.                                                                 | functional | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`raw-string-no-escape.slang`](raw-string-no-escape.slang)                                     |
| Negative - a leading-digit run after `0` is lexed base-8 only after the lexer reports the `'0' prefix indicates octal literal` warning (W10002).                                            | negative   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`negative-octal-literal-warning.slang`](negative-octal-literal-warning.slang)                 |
| Negative - a raw string whose delimiter starts with a bare `"` (as in `R""(...)"`) is rejected with the quote-cannot-be-delimiter diagnostic (E10010).                                      | negative   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`negative-raw-string-quote-delimiter.slang`](negative-raw-string-quote-delimiter.slang)       |
| Negative - a `\u` escape with fewer than four hex digits is rejected at decode time with the invalid-unicode-escape diagnostic (E10008).                                                    | negative   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`negative-unicode-escape-digit-count.slang`](negative-unicode-escape-digit-count.slang)       |
| Negative - an empty character literal (`''`) is rejected eagerly with the "illegal character literal" diagnostic (E10001).                                                                  | negative   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`negative-empty-char-literal.slang`](negative-empty-char-literal.slang)                       |
| Negative - an unescaped newline inside a double-quoted literal body is one of the two failures the string-literal helper reports itself, as `newline in literal` (E10005).                  | negative   | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`negative-newline-in-string-literal.slang`](negative-newline-in-string-literal.slang)         |
| The legacy MSVC infinity form with a leading zero (`0#INF`) lexes as a single FloatingPointLiteral rather than an integer followed by `#INF`.                                               | functional | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | [`float-literal-zero-msvc-infinity.slang`](float-literal-zero-msvc-infinity.slang)             |
| A folded `\`-newline emits no `NewLine` token, so the token after it is not marked as starting a logical line and a macro body can span two physical lines.                                 | functional | [#token-flags](../../../../design/syntax-reference/tokens.md#token-flags)                                               | [`line-continuation-in-macro-definition.slang`](line-continuation-in-macro-definition.slang)   |
| A `BlockComment` (`/* ... */`) is treated as trivia; code on either side of the comment is parsed normally and the comment text has no semantic effect.                                     | functional | [#trivia-whitespace-and-comments](../../../../design/syntax-reference/tokens.md#trivia-whitespace-and-comments)         | [`block-comment-removed.slang`](block-comment-removed.slang)                                   |
| A `LineComment` (`// ...`) ends at the end of its physical line; code on the following line is not part of the comment.                                                                     | functional | [#trivia-whitespace-and-comments](../../../../design/syntax-reference/tokens.md#trivia-whitespace-and-comments)         | [`line-comment-ends-at-newline.slang`](line-comment-ends-at-newline.slang)                     |
| Boundary - an empty block comment (`/**/`) is a valid BlockComment token and is removed from the token stream.                                                                              | boundary   | [#trivia-whitespace-and-comments](../../../../design/syntax-reference/tokens.md#trivia-whitespace-and-comments)         | [`block-comment-empty.slang`](block-comment-empty.slang)                                       |

## Untested claims

| Claim                                                                                                                                                                                                                                    | Reason                 | Anchor                                                                                                                  | Why untested                                                                                                                                                                                                                                                      |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The `Unknown` kind belongs to a default-constructed `Token` and should not appear in valid input.                                                                                                                                        | internal-source-fact   | [#end-markers-and-special](../../../../design/syntax-reference/tokens.md#end-markers-and-special)                       | The claim is about the value of a default-constructed C++ object, not about any input a compiler invocation can supply; nothing in a `.slang` file can produce it.                                                                                                |
| `EndOfFile` is returned when the lexer reaches end of input.                                                                                                                                                                             | internal-source-fact   | [#end-markers-and-special](../../../../design/syntax-reference/tokens.md#end-markers-and-special)                       | Every well-formed compile ends the token stream this way, so the claim has no discriminating observation through `slangc`; a lexer-level C++ unit test reading the final token would be the way to see it.                                                        |
| The illegal-character fall-through picks between `illegalCharacterPrint`, `unexpectedEndOfInput`, and `illegalCharacterHex` by the character's value, and skips the diagnostic entirely under `kLexerFlag_SuppressDiagnostics`.          | needs-unit-test        | [#end-markers-and-special](../../../../design/syntax-reference/tokens.md#end-markers-and-special)                       | The printable arm is covered by [`negative-illegal-character.slang`](negative-illegal-character.slang); the hex arm needs a non-UTF-8 byte the harness cannot round-trip, and the suppression flag is a `Lexer` construction argument with no CLI surface.        |
| `DotDot` (`..`) is lexed as a distinct kind but has no parser consumer at this commit.                                                                                                                                                   | implementation-detail  | [#punctuation-and-structural-symbols](../../../../design/syntax-reference/tokens.md#punctuation-and-structural-symbols) | With no consumer, every use of `..` is a parse error, and that error is identical to the one two adjacent `Dot` tokens would produce — the token kind has no observable consequence through `slangc`.                                                             |
| `Ellipsis`, `Scope`, `RightArrow`, and `DoubleRightArrow` are only some of the punctuation kinds; the single-character kinds (`;`, `,`, `.`, braces, brackets, parens, `:`, `?`, `@`, `$`, `$$`) each have their own row.                | out-of-bundle          | [#punctuation-and-structural-symbols](../../../../design/syntax-reference/tokens.md#punctuation-and-structural-symbols) | Single-character punctuation has no maximal-munch ambiguity to verify, and every well-formed test in this bundle and in `design/syntax-reference/grammar` already lexes them incidentally.                                                                        |
| `CompletionRequest` (`#?`) is lexed by the `#?` arm of the `#` branch and emitted at the cursor position to request completion.                                                                                                          | needs-unit-test        | [#preprocessor-markers](../../../../design/syntax-reference/tokens.md#preprocessor-markers)                             | Only reachable through the language-server completion pipeline; no `slangc` CLI invocation produces or consumes a `#?` token, so a C++ unit test against the lexer / LSP layer would be needed.                                                                   |
| The lexer has no include-header mode: `HandleIncludeDirective` reassembles a `<...>` path by concatenating the tokens between `<` and `>`, while the quoted form arrives as a single `StringLiteral`.                                    | needs-multi-file-test  | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | Observing the reassembly requires a second file on an include search path (a path spelled with `-`, `/`, and `.` so it must be rebuilt from several tokens); a single-file `//TEST` cannot supply it.                                                             |
| After diagnosing an empty, multi-character, bad-escape, or invalid-UTF-8 body, the lexer still produces a `CharLiteral` token and the failure surfaces only when a consumer asks for the value.                                          | implementation-detail  | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | The recovered token's kind has no observable consequence through `slangc` — compilation already fails with E10001 / E10008 (covered by the negative tests); only a lexer-level unit test could inspect the surviving token.                                       |
| A malformed UTF-8 literal body reports `invalidUtf8ByteSequence`.                                                                                                                                                                        | requires-external-tool | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | The input must contain a deliberately invalid byte sequence, which cannot be authored as valid UTF-8 test source; a binary-safe fixture writer outside the `.slang` file would be needed.                                                                         |
| `endOfFileInLiteral` fires when a literal is left open at end of input.                                                                                                                                                                  | needs-cli-test         | [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | The trigger is an unterminated literal as the last thing in the file, which leaves no room for the trailing `//CHECK` annotations `DIAGNOSTIC_TEST` needs; a wrapper script invoking `slangc` on a fixture and grepping stderr is the shape that would verify it. |
| The `Name` flag discriminates the `chars` / `name` union, and interned identifiers store a `Name*` while other tokens store a raw pointer plus length into the source buffer.                                                            | internal-source-fact   | [#token-data-layout](../../../../design/syntax-reference/tokens.md#token-data-layout)                                   | A storage-representation choice inside `Token`; identical user-visible behaviour either way, so no `slangc` invocation can distinguish the two branches of the union.                                                                                             |
| `AfterWhitespace` lets the preprocessor tell a function-like macro definition (`NAME(` with no gap) from an object-like one, and preserve spacing when stringizing.                                                                      | out-of-bundle          | [#token-flags](../../../../design/syntax-reference/tokens.md#token-flags)                                               | This is preprocessor-directive semantics rather than tokenization; `design/pipeline/01-lex-preprocess` owns macro-definition-form and stringize claims.                                                                                                           |
| `ScrubbingNeeded` is set on a token whose content had a line continuation folded, and `Lexer::lexToken` uses it to strip the continuation from the stored text.                                                                          | internal-source-fact   | [#token-flags](../../../../design/syntax-reference/tokens.md#token-flags)                                               | The user-visible consequence (the spliced identifier / comment) is already covered by [`backslash-line-continuation.slang`](backslash-line-continuation.slang); the flag itself is bookkeeping with no separate observation.                                      |
| A `SourceLoc` is a 32-bit key into one `SourceView`, and the `Actual` / `Emit` interpretations differ from `Nominal` in whether they honour `#line` and source maps; a derived view's origin is retrieved with `getInitiatingSourceLoc`. | needs-unit-test        | [#source-location](../../../../design/syntax-reference/tokens.md#source-location)                                       | Only the `Nominal` reading is reachable from source (via `__LINE__`, covered by [`source-location-line-directive.slang`](source-location-line-directive.slang)); selecting `Actual` / `Emit` or calling `getInitiatingSourceLoc` is a C++ API choice.             |
| Every keyword arrives at the parser as `TokenType::Identifier`, with keyword status decided by syntax-decl lookup.                                                                                                                       | out-of-bundle          | [#what-this-catalog-does-not-cover](../../../../design/syntax-reference/tokens.md#what-this-catalog-does-not-cover)     | The doc explicitly hands this claim off to `keywords-and-builtins.md`, so the test belongs in the `design/syntax-reference/keywords-and-builtins` bundle.                                                                                                         |

## Doc gaps observed

| Anchor                                                                                                                  | Kind                  | Gap                                                                                                                                                                                                                                                                                                            | Suggested addition                                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | undocumented-behavior | The `FloatingPointLiteral` row does not say which literal shapes the lexer accepts. A leading dot with no integer part (`.5`) and a hexadecimal float (`0x1.0p4`) both lex successfully, but a doc-only reader has no way to know they are legal — the only float forms named anywhere are `1e10` and `1#INF`. | Add a short "accepted shapes" list to the `FloatingPointLiteral` row: integer-with-fraction, trailing dot, leading dot, decimal exponent, hexadecimal `0x…p…`, and the `#INF` form.        |
| [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | missing-surface       | The `IntegerLiteral` row says "Suffixes (`u`, `l`, `ul`, ...) are part of the token's raw text" and the float row says the same for "(`f`, `lf`, ...)", but neither closes the ellipsis. Tests exist for `u`, `f`, `h`, `lf`, `ll`, and `ull`; a reader cannot tell whether that is the full set.              | Replace both ellipses with the complete accepted suffix list for each literal kind, and state whether the match is case-insensitive.                                                       |
| [#content-tokens](../../../../design/syntax-reference/tokens.md#content-tokens)                                         | undocumented-behavior | Nothing in the doc names the diagnostics a consumer raises when it decodes a literal token: an unrecognised integer suffix and an integer that fits no built-in type are both rejected, but the section only says decoding happens "later".                                                                    | Add the decode-time diagnostics (invalid suffix, integer too large for any type) to the content-token section alongside the existing lexer-time ones, with their error codes.              |
| [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | drift-from-source     | The octal bullet says a leading digit run "is lexed base-8 after a `LexerDiagnostics::octalLiteral` diagnostic" without stating its severity. It is a **warning** (10002), so octal literals compile and run; the wording reads as if lexing is gated on an error.                                             | State the severity explicitly: "a warning (`'0' prefix indicates octal literal`); the literal is still lexed and decoded base-8."                                                          |
| [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | ambiguous-claim       | The leading-zero bullet calls `0#INF` "the legacy MSVC infinity form" and says it lexes as a `FloatingPointLiteral`, but never states what value the token decodes to (positive infinity, or zero because the mantissa is `0`). A value-level assertion cannot be anchored without guessing.                   | State the decoded value of the `0#INF` / `1#INF` forms in the leading-zero bullet (e.g. "decodes to positive infinity regardless of the leading digit", or the actual rule).               |
| [#special-case-lexing-rules](../../../../design/syntax-reference/tokens.md#special-case-lexing-rules)                   | missing-example       | The line-continuation bullet says a `\` before a newline is "consumed and folded out" but does not show the consequence for a `//` comment: because folding happens before comment recognition, a `//` line ending in `\` swallows the whole next physical line.                                               | Add a two-line worked example of a `//` comment ending in a backslash, showing that the following physical line is part of the comment.                                                    |
| [#token-flags](../../../../design/syntax-reference/tokens.md#token-flags)                                               | missing-example       | The `AtStartOfLine` row explains that escaped newlines "do not start a new one (used by the preprocessor for directive recognition)" purely in terms of lexer bookkeeping. The reader-facing consequence — that a `#define` body may be continued onto the next physical line — is never shown.                | Add a worked `#define` spanning two physical lines to the `AtStartOfLine` row, noting that this is exactly the case the flag's continuation carve-out enables.                             |
| [#punctuation-and-structural-symbols](../../../../design/syntax-reference/tokens.md#punctuation-and-structural-symbols) | missing-surface       | The `At`, `Dollar`, and `DollarDollar` rows have empty Notes cells, so a reader cannot tell what user-level syntax (if any) produces `@`, `$`, or `$$`, or whether they are reserved for a future feature.                                                                                                     | Fill in the Notes cells for `At`, `Dollar`, and `DollarDollar` with the construct that consumes each (or "reserved; no parser consumer at this commit", as the `DotDot` row already does). |
| [#operators](../../../../design/syntax-reference/tokens.md#operators)                                                   | undocumented-behavior | The operator table lists every spelling but never states the maximal-munch rule that makes it work — that `>>=` is one token rather than `>>` then `=`, and how the lexer resolves the overlap between `>`, `>=`, `>>`, and `>>=`.                                                                             | Add a one-paragraph preamble to the operator table stating that the lexer takes the longest matching spelling, and call out the `>`-prefixed family as the worked example.                 |
| [#source-location](../../../../design/syntax-reference/tokens.md#source-location)                                       | missing-surface       | The section explains that `Nominal` "honours `#line` directives and source maps" but names no user-level surface that reads the decoded location, so a reader cannot tell where the difference is observable.                                                                                                  | Note that `__LINE__` (and diagnostic line numbers) read the `Nominal` interpretation, so `#line` is the user-visible way to change what a token reports.                                   |

## Sibling-bundle overlap

Two claim families in this doc are deliberately left to their siblings
rather than duplicated here:

- keyword classification (`Identifier` for every keyword) →
  `design/syntax-reference/keywords-and-builtins`, which the doc's
  "What this catalog does not cover" section names explicitly;
- macro-definition form, stringize spacing, and directive recognition
  (the consumers of `AfterWhitespace` and `AtStartOfLine`) →
  `design/pipeline/01-lex-preprocess`. The one exception is
  [`line-continuation-in-macro-definition.slang`](line-continuation-in-macro-definition.slang),
  which is kept here because the claim under test is the _lexer's_
  carve-out (a folded newline emits no `NewLine` token), not the
  preprocessor's use of it.
