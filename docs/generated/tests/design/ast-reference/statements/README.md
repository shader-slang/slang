---
generated: true
model: claude-opus-5[1m]
generated_at: 2026-08-13T00:00:00+00:00
source_commit: c0e5ca5c55ff5ea6b210ac9418bac04728cc45e0
watched_paths_digest: 779d1fe5a276f7b28bdb31b97503a410e81a3d052f4efac724ff5174adcc3df2
source_doc: docs/generated/design/ast-reference/statements.md
source_doc_digest: c541a2a8e41d3a177a92e5cff0ff2301d09419489f9b9d114f60e9a65142d9e0
warning: "Auto-generated. May drift from source. Do not edit by hand."
---

# Tests for design/ast-reference/statements

## Intent

This bundle exercises
[`docs/generated/design/ast-reference/statements.md`](../../../../design/ast-reference/statements.md),
the reference for the concrete `Stmt` subclasses in the Slang AST.

Slang does not expose its AST, so every claim on that page is tested
through the behaviour it produces: which branch runs, how many
iterations happen, which name a lookup resolves to, in what order side
effects land, and which diagnostic fires. Claims that are only about
the C++ shape of a node — the parent class, a field's type, the
identity node behind `break` matching — are not testable this way and
are listed under `## Untested claims` instead.

The coverage strategy is one functional test per statement family
(block scoping, `if`/`else`, the loop family, `switch` dispatch,
jumps and labels, `return`, `defer`, `throw`/`catch`, declaration /
expression / empty statements, `__target_switch` and `__stage_switch`,
`$for`, `__requireCapability`), plus boundary probes on the axes the
page implies — zero-, one-, and many-iteration loops, an empty block,
a five-deep nest, a range of length zero, a switch with only a
`default:` and one with no `default:` — and one negative test per
family that has a natural rejection form.

Statement behaviour is target-independent almost everywhere, so
`INTERPRET` carries most of the bundle. Three situations pull a test
onto a target: `switch` (the interpreter does not execute it, so the
C++ emit is the observation point), `throw`/`catch` (not modelled
faithfully by the interpreter, so CPU compute runs it), and
`__target_switch` / `__stage_switch` (the whole point is that the
selected body differs per target, so those fan out across targets).

## Claims

Enumerated per [`_claims.md` §1](../../../_meta/prompts/_claims.md), grouped by
the document's own headings.

**`#nodes` (the class table)**

1. `SeqStmt` is a flat sequence used where a single statement is required.
2. `BlockStmt` introduces a `ScopeDecl` for block-scoped declarations.
3. `EmptyStmt` is a bare `;`.
4. `DeclStmt` is a declaration used where a statement is expected.
5. `IfStmt` holds predicate and both branches, and is also what `if (let x = ...)` desugars into.
6. `TargetCaseStmt::capability` holds a `CapabilityName` code.
7. `ContinueStmt` takes no label, unlike `break`.
8. `ReturnStmt::expression` is null for a bare `return` in a `void` function.
9. `CatchStmt` with `errorVar == null` is a catch-all.
10. `UniqueStmtIDNode` is synthesized rather than parsed as a statement.

**`#blockstmt-and-seqstmt`**

11. Only `BlockStmt`, the scoped `for`, `__GPU_FOREACH` and the compile-time `for` are given a `ScopeDecl`; `WhileStmt` / `DoWhileStmt` introduce no scope of their own.
12. `SwitchStmt` leaves `scopeDecl` null and takes its lexical scope from the `BlockStmt` the parser builds for its body, so the whole switch body is a single scope: a variable declared under `case 1:` is still in scope under `case 2:` and `default:`.
13. That same variable shadows a same-named variable declared outside the `switch` for the rest of the body.
14. A block's first statement becomes `body` directly; a second one causes a `SeqStmt` to be created and both moved into it.
15. An empty block gets an `EmptyStmt` body rather than a null one.

**`#unparsedstmt`**

16. A function body is first captured as raw tokens in an `UnparsedStmt`, together with the two scopes in effect, and re-parsed on demand with exactly the lookup environment it had at its original position.
17. An `UnparsedStmt` is not expected to survive into later phases; the re-parse yields a normal `BlockStmt`.

**`#ifstmt`**

18. `negativeStatement` is null for an `if` without an `else`.
19. There is no dedicated node for `if let`: it desugars at parse time into a `SeqStmt` holding a `DeclStmt` for a synthesized binding followed by an ordinary `IfStmt`.

**`#switchstmt-casestmt-defaultstmt`**

20. `SwitchStmt::body` is always a `BlockStmt`.
21. Case labels are not the parents of the statements under them — each `CaseStmt` / `DefaultStmt` is a marker in the sequence and the following statements are siblings.

**`#loop-family`**

22. Every loop is `break`-able, but only `for` is given a `ScopeDecl` of its own.
23. `ForStmt::initialStatement` is parsed as a statement so a `DeclStmt` can introduce loop variables.
24. Anything other than a `DeclStmt` or `ExpressionStmt` in the init position is `E20001`, "unexpected statement, expected expression", and the loop node is kept so parsing can recover.
25. `UnscopedForStmt` is produced instead of `ForStmt` when the source language is HLSL, and it fills in `scopeDecl` but never pushes the scope, so the loop variable leaks into the surrounding scope.
26. `do` parses its body first and then decides: a following `while` produces a `DoWhileStmt`, a following `catch` produces a `CatchStmt`, and anything else is an error.

**`#compiletimeforstmt`**

27. The bounds must be compile-time constants, and the loop is unrolled at compile time, emitting no runtime loop.
28. `Range` is a required literal keyword; any other identifier there is `E20004`, "unexpected identifier, expected 'Range'".
29. `$for (i in Range(4))` iterates 0 through 3 and `$for (i in Range(2, 5))` iterates 2 through 4 — the range is half-open in both spellings.
30. The loop variable is added to the statement's own `ScopeDecl`, so the body can refer to it.

**`#targetswitchstmt-stageswitchstmt-targetcasestmt`**

31. A `TargetSwitchStmt` dispatches statically by capability and a `StageSwitchStmt` by pipeline stage; the two share a parser and differ only in the node allocated.

## Functional coverage

| Claim                                                                                                                                                                                                                                  | Intent                 | Anchor                                                                                                                                               | Tests                                                                                                                                                                      |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C25: `UnscopedForStmt` is produced for HLSL input and leaks the loop variable into the surrounding scope. | needs-cli-test         | [#loop-family](../../../../design/ast-reference/statements.md#loop-family) | The unscoped form is selected by `getSourceLanguage()`, which follows the **input file extension**: the same source compiles and reads the loop variable after the loop when named `.hlsl`, and reports `E30015 undefined identifier 'i'` when named `.slang`. `-lang hlsl` on a `.slang` file does *not* select it. Every file in this suite is `.slang`, so the claim has no reachable form here. |
| A BlockStmt containing a single statement executes that statement and introduces a scope; a local declared inside is not visible after the block closes.                                                                               | boundary               | [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                       | [`blockstmt-single-statement.slang`](blockstmt-single-statement.slang)                                                                                                     |
| A BlockStmt is a ScopeStmt; a local declared inside `{ ... }` is not visible outside the block, so a same-named outer variable retains its outer value after the block closes.                                                         | functional             | [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                       | [`blockstmt-introduces-scope.slang`](blockstmt-introduces-scope.slang)                                                                                                     |
| A SeqStmt bundles several statements where a single Stmt slot is required; the canonical surface form is multiple declarators in one declaration (`int a, b;`) used in statement position.                                             | functional             | [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                       | [`seqstmt-multi-declarator.slang`](seqstmt-multi-declarator.slang)                                                                                                         |
| A `switch` introduces no scope of its own and takes its lexical scope from the block the parser builds for its body, so a variable declared under one case label is in scope under the following case labels.                          | functional, regression | [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                       | [`switchstmt-body-block-scope.slang`](switchstmt-body-block-scope.slang), [`switchstmt-case-decl-used-in-later-case.slang`](switchstmt-case-decl-used-in-later-case.slang) |
| A deeply nested chain of BlockStmts (>=5 levels) each introducing its own scope still resolves shadowed names to the innermost binding; the outermost variable is preserved.                                                           | stress                 | [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                       | [`blockstmt-deeply-nested-stress.slang`](blockstmt-deeply-nested-stress.slang)                                                                                             |
| An empty BlockStmt `{}` is accepted in statement position and has no observable effect; the surrounding state is preserved across the block.                                                                                           | boundary               | [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                       | [`blockstmt-empty.slang`](blockstmt-empty.slang)                                                                                                                           |
| A CompileTimeForStmt iterates a compile-time-constant range and is unrolled at compile time; the body executes once per iteration with the loop variable bound to a compile-time constant.                                             | functional             | [#compiletimeforstmt](../../../../design/ast-reference/statements.md#compiletimeforstmt)                                                             | [`compiletimefor-unrolls.slang`](compiletimefor-unrolls.slang)                                                                                                             |
| A CompileTimeForStmt over a 32-element range unrolls to 32 copies of the body; the summed loop-variable values match the closed-form 0+1+...+31 = 496.                                                                                 | stress                 | [#compiletimeforstmt](../../../../design/ast-reference/statements.md#compiletimeforstmt)                                                             | [`compiletimefor-large-range-stress.slang`](compiletimefor-large-range-stress.slang)                                                                                       |
| A CompileTimeForStmt with a zero-length range (`$for(i in Range(0))`) unrolls to zero copies of the body; the surrounding accumulator is unchanged by the unrolled-no-times loop.                                                      | boundary               | [#compiletimeforstmt](../../../../design/ast-reference/statements.md#compiletimeforstmt)                                                             | [`compiletimefor-zero-range.slang`](compiletimefor-zero-range.slang)                                                                                                       |
| A compile-time `for` accepts a two-argument `Range(begin, end)` whose begin bound is used, whereas the one-argument `Range(end)` form leaves the begin bound implicit.                                                                 | boundary               | [#compiletimeforstmt](../../../../design/ast-reference/statements.md#compiletimeforstmt)                                                             | [`compiletimefor-two-argument-range.slang`](compiletimefor-two-argument-range.slang)                                                                                       |
| `Range` is a required literal keyword in the compile-time `for` syntax, not an ordinary callable, so any other name in that position is rejected while the statement is parsed.                                                        | negative               | [#compiletimeforstmt](../../../../design/ast-reference/statements.md#compiletimeforstmt)                                                             | [`compiletimefor-range-keyword-required.slang`](compiletimefor-range-keyword-required.slang)                                                                               |
| A DeclStmt is a `DeclBase` used in statement position; the canonical use is a local variable declaration whose name is visible to subsequent statements in the same block.                                                             | functional             | [#declstmt-expressionstmt-and-emptystmt](../../../../design/ast-reference/statements.md#declstmt-expressionstmt-and-emptystmt)                       | [`declstmt-local-visible-later.slang`](declstmt-local-visible-later.slang)                                                                                                 |
| A `;` immediately after an `if` is suspicious rather than illegal: it still yields an EmptyStmt, but the compiler reports the potentially-unintended-empty-statement warning.                                                          | negative               | [#declstmt-expressionstmt-and-emptystmt](../../../../design/ast-reference/statements.md#declstmt-expressionstmt-and-emptystmt)                       | [`emptystmt-after-if-diagnosed.slang`](emptystmt-after-if-diagnosed.slang)                                                                                                 |
| A statement that starts with an identifier is re-parsed as a declaration when an identifier follows the type and as an expression statement otherwise, so `S s;` and `s.v = 3;` in the same block are classified differently.          | functional             | [#declstmt-expressionstmt-and-emptystmt](../../../../design/ast-reference/statements.md#declstmt-expressionstmt-and-emptystmt)                       | [`declstmt-vs-expressionstmt-backtracking.slang`](declstmt-vs-expressionstmt-backtracking.slang)                                                                           |
| An EmptyStmt gives a concrete statement for an empty slot; a loop whose body is a bare `;` is accepted and iterates via its side-effect expression alone.                                                                              | functional             | [#declstmt-expressionstmt-and-emptystmt](../../../../design/ast-reference/statements.md#declstmt-expressionstmt-and-emptystmt)                       | [`emptystmt-loop-body-semicolon.slang`](emptystmt-loop-body-semicolon.slang)                                                                                               |
| An EmptyStmt is a bare `;` accepted in statement position; it carries no state and produces no effect.                                                                                                                                 | functional             | [#declstmt-expressionstmt-and-emptystmt](../../../../design/ast-reference/statements.md#declstmt-expressionstmt-and-emptystmt)                       | [`emptystmt-bare-semicolon.slang`](emptystmt-bare-semicolon.slang)                                                                                                         |
| An ExpressionStmt holds an arbitrary `Expr` used for its side effects; the value of the expression is computed and discarded, and any side effects (function call, assignment) are observed.                                           | functional             | [#declstmt-expressionstmt-and-emptystmt](../../../../design/ast-reference/statements.md#declstmt-expressionstmt-and-emptystmt)                       | [`expressionstmt-side-effect.slang`](expressionstmt-side-effect.slang)                                                                                                     |
| A DeferStmt placed at function entry has the entire function body as its surrounding scope; the deferred inner statement runs after every other statement in the function but before the caller resumes.                               | boundary               | [#deferstmt](../../../../design/ast-reference/statements.md#deferstmt)                                                                               | [`deferstmt-at-function-entry.slang`](deferstmt-at-function-entry.slang)                                                                                                   |
| A DeferStmt's inner statement runs when the enclosing scope exits rather than at the position the `defer` is written.                                                                                                                  | functional             | [#deferstmt](../../../../design/ast-reference/statements.md#deferstmt)                                                                               | [`deferstmt-scope-exit-order.slang`](deferstmt-scope-exit-order.slang)                                                                                                     |
| A DeferStmt's inner statement still runs when the enclosing scope exits via an early `return`, not only when the scope falls through its end.                                                                                          | functional             | [#deferstmt](../../../../design/ast-reference/statements.md#deferstmt)                                                                               | [`deferstmt-runs-on-early-return.slang`](deferstmt-runs-on-early-return.slang)                                                                                             |
| Multiple DeferStmt inner statements written in a single scope all run when that scope exits, after the surrounding body's own statements.                                                                                              | boundary               | [#deferstmt](../../../../design/ast-reference/statements.md#deferstmt)                                                                               | [`deferstmt-multiple-lifo-order.slang`](deferstmt-multiple-lifo-order.slang)                                                                                               |
| `defer` takes a single statement, so a deferred block is written without a trailing semicolon and the whole block is what gets deferred.                                                                                               | boundary               | [#deferstmt](../../../../design/ast-reference/statements.md#deferstmt)                                                                               | [`deferstmt-block-needs-no-semicolon.slang`](deferstmt-block-needs-no-semicolon.slang)                                                                                     |
| A 5-arm if/else-if/else-if/else-if/else chain (nested IfStmts in each negativeStatement slot) dispatches to exactly the matching arm; later arms do not fire and the final else handles the unmatched fallback.                        | stress                 | [#ifstmt](../../../../design/ast-reference/statements.md#ifstmt)                                                                                     | [`ifstmt-elseif-chain-stress.slang`](ifstmt-elseif-chain-stress.slang)                                                                                                     |
| An IfStmt evaluates its predicate Expr exactly once per execution of the statement; a side-effecting predicate's effect is observed exactly once, regardless of which branch is selected.                                              | boundary               | [#ifstmt](../../../../design/ast-reference/statements.md#ifstmt)                                                                                     | [`ifstmt-predicate-evaluated-once.slang`](ifstmt-predicate-evaluated-once.slang)                                                                                           |
| An IfStmt holds predicate, positive and negative statements; only the branch matching the predicate is executed, and the unchosen branch's side effects are not observed.                                                              | functional             | [#ifstmt](../../../../design/ast-reference/statements.md#ifstmt)                                                                                     | [`ifstmt-else-branch.slang`](ifstmt-else-branch.slang)                                                                                                                     |
| An IfStmt's negativeStatement slot is null for an `if` without an `else`; when the predicate is false the program continues with no side effects from the missing else branch.                                                         | functional             | [#ifstmt](../../../../design/ast-reference/statements.md#ifstmt)                                                                                     | [`ifstmt-no-else-noop.slang`](ifstmt-no-else-noop.slang)                                                                                                                   |
| There is no dedicated statement form for `if let`; it desugars at parse time into an ordinary IfStmt whose positive branch is prefixed with a binding of the unwrapped value, so the bound name is readable only on the positive path. | functional             | [#ifstmt](../../../../design/ast-reference/statements.md#ifstmt)                                                                                     | [`ifstmt-if-let-binding.slang`](ifstmt-if-let-binding.slang)                                                                                                               |
| A BreakStmt is a ChildStmt whose target must be an enclosing BreakableStmt (loop or switch); used outside any such enclosing statement it is rejected.                                                                                 | negative               | [#labelstmt-breakstmt-continuestmt-and-discardstmt](../../../../design/ast-reference/statements.md#labelstmt-breakstmt-continuestmt-and-discardstmt) | [`breakstmt-outside-loop-rejected.slang`](breakstmt-outside-loop-rejected.slang)                                                                                           |
| A ContinueStmt is a ChildStmt whose target must be an enclosing LoopStmt; used outside any loop the checker rejects it because no matching enclosing loop exists.                                                                      | negative               | [#labelstmt-breakstmt-continuestmt-and-discardstmt](../../../../design/ast-reference/statements.md#labelstmt-breakstmt-continuestmt-and-discardstmt) | [`continuestmt-outside-loop-rejected.slang`](continuestmt-outside-loop-rejected.slang)                                                                                     |
| A ContinueStmt restarts the nearest enclosing loop; a `continue` in an inner loop skips only the inner iteration's remainder while the outer loop still completes all of its iterations.                                               | functional             | [#labelstmt-breakstmt-continuestmt-and-discardstmt](../../../../design/ast-reference/statements.md#labelstmt-breakstmt-continuestmt-and-discardstmt) | [`continuestmt-nearest-enclosing-loop.slang`](continuestmt-nearest-enclosing-loop.slang)                                                                                   |
| A ContinueStmt skips the remainder of the current iteration and advances to the next; statements after the `continue` are not executed for that iteration.                                                                             | functional             | [#labelstmt-breakstmt-continuestmt-and-discardstmt](../../../../design/ast-reference/statements.md#labelstmt-breakstmt-continuestmt-and-discardstmt) | [`continuestmt-skips-rest-of-iteration.slang`](continuestmt-skips-rest-of-iteration.slang)                                                                                 |
| A DiscardStmt is a fragment-shader pixel-kill construct; using `discard` from a non-fragment entry point is rejected as an unavailable feature for that stage.                                                                         | negative               | [#labelstmt-breakstmt-continuestmt-and-discardstmt](../../../../design/ast-reference/statements.md#labelstmt-breakstmt-continuestmt-and-discardstmt) | [`discardstmt-non-fragment-rejected.slang`](discardstmt-non-fragment-rejected.slang)                                                                                       |
| A LabelStmt attaches a label token to an inner statement; a `BreakStmt` with a matching `targetLabel` breaks out of the labeled (non-innermost) loop rather than the innermost one.                                                    | functional             | [#labelstmt-breakstmt-continuestmt-and-discardstmt](../../../../design/ast-reference/statements.md#labelstmt-breakstmt-continuestmt-and-discardstmt) | [`labelstmt-labeled-break.slang`](labelstmt-labeled-break.slang)                                                                                                           |
| An unlabeled BreakStmt exits the innermost enclosing BreakableStmt; the outer loop continues its remaining iterations.                                                                                                                 | functional             | [#labelstmt-breakstmt-continuestmt-and-discardstmt](../../../../design/ast-reference/statements.md#labelstmt-breakstmt-continuestmt-and-discardstmt) | [`breakstmt-exits-innermost-loop.slang`](breakstmt-exits-innermost-loop.slang)                                                                                             |
| A DoWhileStmt tests its predicate after each iteration; the body executes at least once even when the predicate is false at entry.                                                                                                     | functional             | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`dowhilestmt-runs-once.slang`](dowhilestmt-runs-once.slang)                                                                                                               |
| A ForStmt is a ScopeStmt; the loop variable introduced by `initialStatement` is scoped to the body and does not leak into the surrounding scope.                                                                                       | functional             | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`forstmt-variable-scoped.slang`](forstmt-variable-scoped.slang)                                                                                                           |
| A ForStmt iterated 100 times accumulates the documented count; the loop variable advances monotonically and the side-effect expression runs once per iteration.                                                                        | stress                 | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`forstmt-many-iterations-stress.slang`](forstmt-many-iterations-stress.slang)                                                                                             |
| A ForStmt whose predicate is true exactly once executes the body exactly once and then exits; both initialStatement and sideEffectExpression run before the predicate fails on the second test.                                        | boundary               | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`forstmt-one-iteration.slang`](forstmt-one-iteration.slang)                                                                                                               |
| A ForStmt whose predicateExpression is false at entry executes zero iterations; the loop body does not run at all.                                                                                                                     | boundary               | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`forstmt-zero-iterations.slang`](forstmt-zero-iterations.slang)                                                                                                           |
| A ForStmt with empty predicateExpression and empty sideEffectExpression (`for (init; ; )`) loops indefinitely; a body-internal break terminates it after the documented number of iterations.                                          | boundary               | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`forstmt-init-only-with-break.slang`](forstmt-init-only-with-break.slang)                                                                                                 |
| A WhileStmt tests its predicate before each iteration; when the predicate is false at entry the body does not execute at all.                                                                                                          | functional             | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`whilestmt-skips-zero-iterations.slang`](whilestmt-skips-zero-iterations.slang)                                                                                           |
| A WhileStmt with a constant-true predicate iterates indefinitely; a body-internal BreakStmt terminates the LoopStmt at the documented iteration.                                                                                       | boundary               | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`whilestmt-true-with-break.slang`](whilestmt-true-with-break.slang)                                                                                                       |
| A `do` statement's body is parsed before the compiler decides what was built: a following `while` makes a do-while loop and a following `catch` makes a protected body, and anything else is an error.                                 | negative               | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`dostmt-without-while-or-catch-rejected.slang`](dostmt-without-while-or-catch-rejected.slang)                                                                             |
| A `for` header's initial position is parsed as a statement so that a declaration can introduce loop variables there, but only a declaration or an expression statement is accepted; anything else is diagnosed.                        | negative               | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                           | [`forstmt-init-non-expression-rejected.slang`](forstmt-init-non-expression-rejected.slang)                                                                                 |
| A RequireCapabilityStmt is a statement-level capability assertion (`__requireCapability(...)`) scoped to the enclosing function; the function compiles when the target capability set satisfies the listed atoms.                      | functional             | [#requirecapabilitystmt](../../../../design/ast-reference/statements.md#requirecapabilitystmt)                                                       | [`requirecapability-in-function.slang`](requirecapability-in-function.slang)                                                                                               |
| Each name listed by a `__requireCapability` statement is validated as it is read, so a name that does not resolve to a capability is dropped and diagnosed as an unknown capability.                                                   | negative               | [#requirecapabilitystmt](../../../../design/ast-reference/statements.md#requirecapabilitystmt)                                                       | [`requirecapability-unknown-name-rejected.slang`](requirecapability-unknown-name-rejected.slang)                                                                           |
| A ReturnStmt in a non-void function is required to carry an expression; `return;` is rejected with `'return' should have an expression`.                                                                                               | negative               | [#returnstmt](../../../../design/ast-reference/statements.md#returnstmt)                                                                             | [`returnstmt-missing-expression-rejected.slang`](returnstmt-missing-expression-rejected.slang)                                                                             |
| A ReturnStmt in a void function is allowed to omit its expression (`return;`); the function halts and statements after the bare return do not execute on the early-return path.                                                        | boundary               | [#returnstmt](../../../../design/ast-reference/statements.md#returnstmt)                                                                             | [`returnstmt-void-bare.slang`](returnstmt-void-bare.slang)                                                                                                                 |
| A ReturnStmt taken inside a loop body halts the entire function; the remaining iterations of the loop and all statements after the loop do not execute.                                                                                | boundary               | [#returnstmt](../../../../design/ast-reference/statements.md#returnstmt)                                                                             | [`returnstmt-inside-loop.slang`](returnstmt-inside-loop.slang)                                                                                                             |
| A ReturnStmt yields a value and halts the function; statements after a taken return are not executed.                                                                                                                                  | functional             | [#returnstmt](../../../../design/ast-reference/statements.md#returnstmt)                                                                             | [`returnstmt-halts-function.slang`](returnstmt-halts-function.slang)                                                                                                       |
| The checker matches a ReturnStmt's expression against the enclosing function's declared return type; `return 1;` in a `void` function is rejected because `int` does not implicitly convert to `void`.                                 | negative               | [#returnstmt](../../../../design/ast-reference/statements.md#returnstmt)                                                                             | [`returnstmt-value-in-void-rejected.slang`](returnstmt-value-in-void-rejected.slang)                                                                                       |
| The checker matches a ReturnStmt's expression against the enclosing function's declared return type; returning a struct value from an `int` function is rejected with a type mismatch.                                                 | negative               | [#returnstmt](../../../../design/ast-reference/statements.md#returnstmt)                                                                             | [`returnstmt-type-mismatch-rejected.slang`](returnstmt-type-mismatch-rejected.slang)                                                                                       |
| A BreakStmt nested inside a SwitchStmt targets the enclosing SwitchStmt (a BreakableStmt) and not any surrounding loop; control returns to the statement after the switch and the surrounding loop continues.                          | boundary               | [#switchstmt-casestmt-defaultstmt](../../../../design/ast-reference/statements.md#switchstmt-casestmt-defaultstmt)                                   | [`breakstmt-from-switch.slang`](breakstmt-from-switch.slang)                                                                                                               |
| A SwitchStmt dispatches on its condition value to the matching case section, and a `BreakStmt` inside it returns control to the statement after the switch.                                                                            | functional             | [#switchstmt-casestmt-defaultstmt](../../../../design/ast-reference/statements.md#switchstmt-casestmt-defaultstmt)                                   | [`switchstmt-case-dispatch.slang`](switchstmt-case-dispatch.slang)                                                                                                         |
| A SwitchStmt whose body holds only a `default:` marker dispatches every condition value to that default section.                                                                                                                       | boundary               | [#switchstmt-casestmt-defaultstmt](../../../../design/ast-reference/statements.md#switchstmt-casestmt-defaultstmt)                                   | [`switchstmt-default-only.slang`](switchstmt-default-only.slang)                                                                                                           |
| A SwitchStmt without a DefaultStmt marker lets an unmatched condition value pass through the switch without entering any case section.                                                                                                 | boundary               | [#switchstmt-casestmt-defaultstmt](../../../../design/ast-reference/statements.md#switchstmt-casestmt-defaultstmt)                                   | [`switchstmt-no-default.slang`](switchstmt-no-default.slang)                                                                                                               |
| Case labels are markers in the statement sequence rather than parents of the statements under them, so two adjacent case labels with nothing between them share the statements that follow.                                            | functional             | [#switchstmt-casestmt-defaultstmt](../../../../design/ast-reference/statements.md#switchstmt-casestmt-defaultstmt)                                   | [`switchstmt-fall-through.slang`](switchstmt-fall-through.slang)                                                                                                           |
| A StageSwitchStmt dispatches statically by pipeline stage; only the case body whose label matches the entry point's stage reaches the emitted code.                                                                                    | functional             | [#targetswitchstmt-stageswitchstmt-targetcasestmt](../../../../design/ast-reference/statements.md#targetswitchstmt-stageswitchstmt-targetcasestmt)   | [`stageswitch-static-dispatch.slang`](stageswitch-static-dispatch.slang)                                                                                                   |
| A TargetSwitchStmt is a static dispatch chosen at compile time by matching capabilities; a target that matches no TargetCaseStmt selects the `default` body and the unmatched case bodies do not reach the emitted code.               | functional             | [#targetswitchstmt-stageswitchstmt-targetcasestmt](../../../../design/ast-reference/statements.md#targetswitchstmt-stageswitchstmt-targetcasestmt)   | [`targetswitch-static-dispatch.slang`](targetswitch-static-dispatch.slang)                                                                                                 |
| Capability labels stacked in front of one body inside a `__target_switch` each select that same body, so every stacked capability compiles to the shared case body while other targets fall to `default`.                              | boundary               | [#targetswitchstmt-stageswitchstmt-targetcasestmt](../../../../design/ast-reference/statements.md#targetswitchstmt-stageswitchstmt-targetcasestmt)   | [`targetcase-stacked-labels-share-body.slang`](targetcase-stacked-labels-share-body.slang)                                                                                 |
| The capability named by a `__target_switch` case label is resolved while the statement is parsed, so a name that is not a known capability is diagnosed as an unknown target name.                                                     | negative               | [#targetswitchstmt-stageswitchstmt-targetcasestmt](../../../../design/ast-reference/statements.md#targetswitchstmt-stageswitchstmt-targetcasestmt)   | [`targetcase-unknown-name-rejected.slang`](targetcase-unknown-name-rejected.slang)                                                                                         |
| A CatchStmt with a null `errorVar` is a catch-all that does not bind the error value; it catches errors of any type from the protected body.                                                                                           | functional             | [#throwstmt-and-catchstmt](../../../../design/ast-reference/statements.md#throwstmt-and-catchstmt)                                                   | [`catchstmt-catch-all.slang`](catchstmt-catch-all.slang)                                                                                                                   |
| A ThrowStmt in an errorable function transfers control to the caller's matching CatchStmt; the catch handler runs and the protected body's remaining statements are skipped.                                                           | functional             | [#throwstmt-and-catchstmt](../../../../design/ast-reference/statements.md#throwstmt-and-catchstmt)                                                   | [`throw-catch-handler-runs.slang`](throw-catch-handler-runs.slang)                                                                                                         |
| A ThrowStmt is only valid inside a function declared to throw the matching error type; an uncaught `throw` in a non-throwing function is rejected.                                                                                     | negative               | [#throwstmt-and-catchstmt](../../../../design/ast-reference/statements.md#throwstmt-and-catchstmt)                                                   | [`throwstmt-without-throws-rejected.slang`](throwstmt-without-throws-rejected.slang)                                                                                       |
| A ThrowStmt's `expression` Expr is transferred to the matching CatchStmt's `errorVar` ParamDecl; the handler observes the originally thrown enum value distinctly across distinct enum members.                                        | boundary               | [#throwstmt-and-catchstmt](../../../../design/ast-reference/statements.md#throwstmt-and-catchstmt)                                                   | [`throwstmt-enum-value-preserved.slang`](throwstmt-enum-value-preserved.slang)                                                                                             |
| A `do` body may be followed by several `catch` clauses; the clauses chain so that each later handler protects the one before it, and an error raised in the body is taken by the first matching handler.                               | boundary               | [#throwstmt-and-catchstmt](../../../../design/ast-reference/statements.md#throwstmt-and-catchstmt)                                                   | [`catchstmt-multiple-clauses-chained.slang`](catchstmt-multiple-clauses-chained.slang)                                                                                     |
| Nested CatchStmt blocks resolve to the innermost matching handler; a throw inside an inner try is caught by the inner handler and the outer handler does not fire.                                                                     | boundary               | [#throwstmt-and-catchstmt](../../../../design/ast-reference/statements.md#throwstmt-and-catchstmt)                                                   | [`catchstmt-nested-try.slang`](catchstmt-nested-try.slang)                                                                                                                 |
| There is no `try` statement: a `try` written at statement position is parsed as an expression statement, so `try f();` is a call whose result is discarded while the error still propagates to the caller's handler.                   | functional             | [#throwstmt-and-catchstmt](../../../../design/ast-reference/statements.md#throwstmt-and-catchstmt)                                                   | [`try-in-statement-position.slang`](try-in-statement-position.slang)                                                                                                       |
| A function body is captured as raw tokens while declarations are parsed and only turned into a real block later, so a body may name a function declared further down the same file.                                                    | functional             | [#unparsedstmt](../../../../design/ast-reference/statements.md#unparsedstmt)                                                                         | [`unparsedstmt-body-sees-later-decl.slang`](unparsedstmt-body-sees-later-decl.slang)                                                                                       |

## Untested claims

| Claim                                                                                                                                                                                                                             | Reason                | Anchor                                                                                                                                             | Why untested                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `UnscopedForStmt` is the HLSL-compatibility form of `for`: the same parser routine builds it instead of a `ForStmt` when the source language is HLSL, and in that case the loop variable leaks into the surrounding scope.        | needs-cli-test        | [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                         | The form is selected by the input's source language, which `slangc` derives from the file extension; a bundle made of `.slang` files can never request it (passing `-lang hlsl` on a `.slang` file still parses with Slang scoping). A wrapper script compiling an `.hlsl` input and asserting the loop variable is still readable after the loop would verify it. |
| `GpuForeachStmt` is the host-side compute-foreach over a grid, written as `__GPU_FOREACH(device, gridDims, LAMBDA(...) { ... })`.                                                                                                 | gpu-other             | [#nodes](../../../../design/ast-reference/statements.md#nodes)                                                                                     | The statement dispatches work through a host-side device object, so it needs a runtime that can supply that device; none of the directives this bundle can run provides one.                                                                                                                                                                                       |
| `IntrinsicAsmStmt` is the inline intrinsic-assembly statement used by core-module intrinsics.                                                                                                                                     | internal-source-fact  | [#nodes](../../../../design/ast-reference/statements.md#nodes)                                                                                     | `__intrinsic_asm` is reserved for core-module sources; user code compiled through `slang-test` cannot spell it, so there is no observable consequence to assert.                                                                                                                                                                                                   |
| `BreakableStmt`s carry a `UniqueStmtIDNode* uniqueID` that `ChildStmt`s reference via `targetOuterStmtID`, and `UniqueStmtIDNode` is declared as a `Decl` subclass for serialization convenience.                                 | internal-source-fact  | [#family-hierarchy](../../../../design/ast-reference/statements.md#family-hierarchy)                                                               | Only the user-visible result of that linkage — which enclosing statement a `break` or labeled `break` leaves — is observable, and that is already covered; the identity node itself has no surface.                                                                                                                                                                |
| `BlockStmt` carries a `closingSourceLoc`, and `IfStmt::afterLoc` records the first token past the whole `if` (a language-server signal).                                                                                          | internal-source-fact  | [#ifstmt](../../../../design/ast-reference/statements.md#ifstmt)                                                                                   | Both are source-location bookkeeping consumed by the language server; no `slang-test` directive renders them.                                                                                                                                                                                                                                                      |
| The call site that re-parses an `UnparsedStmt` lives in the checker, outside this page's watched paths.                                                                                                                           | internal-source-fact  | [#unparsedstmt](../../../../design/ast-reference/statements.md#unparsedstmt)                                                                       | This is a statement about where the code lives, not about compiler output; the observable half (a body may name a declaration that appears later) is covered by a test.                                                                                                                                                                                            |
| `SeqStmt` is also built by `Parser::parseIfLetStatement` and by the per-case body loop in `parseTargetSwitchStmtImpl`, and a `default:` label is recorded as a `TargetCaseStmt` whose `capabilityToken` content has been emptied. | internal-source-fact  | [#targetswitchstmt-stageswitchstmt-targetcasestmt](../../../../design/ast-reference/statements.md#targetswitchstmt-stageswitchstmt-targetcasestmt) | These name which node the parser allocates and how a token is blanked; the user-visible behaviour they produce (`if let` binding, stacked target labels, `default` selection) is covered by separate tests.                                                                                                                                                        |
| `WhileStmt` and `DoWhileStmt` introduce no scope of their own, unlike `for`.                                                                                                                                                      | implementation-detail | [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                     | A `while`/`do` body is written as a block, which supplies a scope of its own, so no program distinguishes "the loop has no scope" from "the loop has a scope that exactly matches its body".                                                                                                                                                                       |
| When the deferred statement actually runs is decided by IR lowering rather than by the AST; see the AST-to-IR page.                                                                                                               | out-of-bundle         | [#deferstmt](../../../../design/ast-reference/statements.md#deferstmt)                                                                             | The lowering itself belongs to `design/pipeline/04-ast-to-ir`; this bundle only asserts the source-level ordering a user observes.                                                                                                                                                                                                                                 |

## Doc gaps observed

| Anchor                                                                                                                                             | Kind            | Gap                                                                                                                                                                                                                                                                                                                                            | Suggested addition                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#loop-family](../../../../design/ast-reference/statements.md#loop-family) | missing-surface | The `UnscopedForStmt` row says the form is chosen "when `getSourceLanguage()` is `SourceLanguage::HLSL`" but never says what sets that. It follows the input file's extension, not the `-lang` option: the same source leaks the loop variable when named `.hlsl` and reports `E30015` when named `.slang`, and adding `-lang hlsl` to the `.slang` compile does not change that. | State that the unscoped form follows the input file extension and note explicitly that `-lang hlsl` does not select it, so a reader knows the only way to reach the behaviour. |
| [#deferstmt](../../../../design/ast-reference/statements.md#deferstmt)                                                                             | ambiguous-claim | The section now says only that "when the deferred statement actually runs is decided by IR lowering, not by the AST" and points elsewhere. A reader of this page cannot tell whether `defer` fires at the end of the enclosing block, at function exit, or also on an early `return` — yet those are the properties a user writes `defer` for. | Restate the observable rule in one sentence before deferring to the AST-to-IR page, e.g. "the deferred statement runs when the enclosing scope exits, including when it exits via `return`, `break` or `throw`". |
| [#targetswitchstmt-stageswitchstmt-targetcasestmt](../../../../design/ast-reference/statements.md#targetswitchstmt-stageswitchstmt-targetcasestmt) | missing-surface | The section says an unrecognized capability name "is diagnosed immediately as `Diagnostics::UnknownTargetName`", which is an internal identifier; the page never gives the user-visible code or message, and does not mention that a second diagnostic about an invalid `target_switch` case follows it.                                       | Name the user-visible diagnostic (`E29110`, "unknown target name '<name>'") next to the internal identifier, and note that an invalid case label also produces a follow-on `invalid target_switch case` error.   |
| [#requirecapabilitystmt](../../../../design/ast-reference/statements.md#requirecapabilitystmt)                                                     | missing-example | The section describes validation and the `Diagnostics::UnknownCapability` path but shows no example of the statement itself, so a reader does not learn the accepted spelling or whether more than one atom may be listed.                                                                                                                     | Add a two-line example showing the accepted form (e.g. `__requireCapability(hlsl);` inside a function body) and give the user-visible diagnostic code `E36105` alongside the internal identifier.                |
| [#loop-family](../../../../design/ast-reference/statements.md#loop-family)                                                                         | missing-surface | The section says `Parser::ParseForStatement` "diagnoses anything that is not a `DeclStmt` or `ExpressionStmt`" in the initial position, but never shows what such a statement looks like in source or what the user sees.                                                                                                                      | Add a one-line example of the rejected form (e.g. `for ({ int i = 0; } n < 3; ++n)`) and name the diagnostic the user gets (`E20001`, "unexpected statement, expected expression").                              |
| [#nodes](../../../../design/ast-reference/statements.md#nodes)                                                                                     | missing-surface | The `UnscopedForStmt` row says it is "produced only for HLSL input" without saying how a user selects HLSL input, so the leaking-loop-variable behaviour cannot be reproduced from the page alone.                                                                                                                                             | State that the source language is chosen by the input file's extension (`.hlsl` versus `.slang`), and show the loop variable still being readable after the loop in an HLSL input.                               |
| [#blockstmt-and-seqstmt](../../../../design/ast-reference/statements.md#blockstmt-and-seqstmt)                                                     | missing-example | The section explains that `SwitchStmt` leaves `scopeDecl` null and "takes its lexical scope from the `BlockStmt` the parser builds for its body", but gives no example of the consequence a user can see.                                                                                                                                      | Add a short example showing that a variable declared under one `case` label is in scope under the following labels, since that is the user-visible meaning of the switch having no scope of its own.             |
| [#declstmt-expressionstmt-and-emptystmt](../../../../design/ast-reference/statements.md#declstmt-expressionstmt-and-emptystmt)                     | missing-surface | The section says a `;` right after an `if` "still yields an `EmptyStmt` but also reports `Diagnostics::UnintendedEmptyStatement`" — an internal identifier with no user-visible code, severity, or message text.                                                                                                                               | Give the user-visible warning (`E20101`, "potentially unintended empty statement at this location; use {} instead.") and say explicitly that it is a warning, not an error.                                      |
| [#compiletimeforstmt](../../../../design/ast-reference/statements.md#compiletimeforstmt)                                                           | missing-example | The section states that `Range` is a required literal keyword and that "the one-argument form `Range(end)` leaves `rangeBeginExpr` null", but shows neither spelling in source, so the two-argument form's syntax has to be guessed.                                                                                                           | Add both spellings as a two-line example (`$for(i in Range(4))` and `$for(i in Range(2, 5))`) and note that any other name in that position is a parse error.                                                    |

## Known failures

Two tests in this bundle are listed in
[`_meta/expected-failures.txt`](../../../_meta/expected-failures.txt):

- [`ifstmt-predicate-evaluated-once.slang`](ifstmt-predicate-evaluated-once.slang)
  — pre-existing slangi VM bug (issue 11375): the second `printf`
  after a call that took an `inout` parameter reads out of bounds.
- [`switchstmt-case-decl-used-in-later-case.slang`](switchstmt-case-decl-used-in-later-case.slang)
  — a variable declared under one `case` label and used under another
  is accepted silently by the frontend, but the C++ emitted with debug
  info references an undeclared identifier, so the CPU run cannot
  build. Recorded as the pending finding
  `switch-case-decl-used-in-later-case-invalid-cpp-emit`. Its companion
  [`switchstmt-body-block-scope.slang`](switchstmt-body-block-scope.slang)
  observes the same claim at the frontend and passes.
