---
generated: true
model: claude-opus-5[1m]
generated_at: 2026-08-04T00:00:00+00:00
source_commit: 7e725f15572c6589ee6d738a8856fb3348f11617
watched_paths_digest: dbfbda1756b7d272688648c15ee13dc082b3c386232d82ea279b83af5aa49b07
source_doc: docs/generated/design/ir-reference/values.md
source_doc_digest: f73efc050f44ce978c7d1abafaf470026b7b40e2c0cf8ce4aa89b29d86baffba
warning: "Auto-generated. May drift from source. Do not edit by hand."
---

# Tests for design/ir-reference/values

## Intent

This bundle is the per-opcode regression suite for the IR value-producing
family catalogued in
[`docs/generated/design/ir-reference/values.md`](../../../../design/ir-reference/values.md):
literals and their inline payload encoding, arithmetic and bitwise
operators, the non-short-circuiting logical opcodes and the branch-free
`select`, comparisons, the `emitCast` conversion table, memory and field
access, aggregate constructors and projections, the swizzle trio, and the
`Optional` / `Result` helpers. The strategy is one claim per catalog row
plus the boundary probes each row's operand domain implies: for a numeric
opcode that means the documented `MIN` / `MAX` / zero / overflow edges of
every type the row applies to; for a conversion opcode it means every
(from, to) pair the `emitCast` table admits at the type ranks Slang
exposes; for an indexed opcode it means the lower edge, the upper edge,
and the rejected out-of-range case.

The primary observation point is `-target <text-target> -dump-ir
-o /dev/null`, FileChecked against the IR dump, because nearly every claim
in this doc is about which opcode lowering emits rather than about emitted
target text. Patterns are anchored at `func %main` or a user-named callee
so the large IR preamble cannot satisfy them, operands come from `uniform`
globals or `SV_DispatchThreadID` so the constant folder has nothing to
collapse, and every observed value is stored into an `RWStructuredBuffer`
so dead-code elimination cannot remove it. Where a value opcode genuinely
has target-divergent user-visible text — vector swizzle, literal
emission, the signedness fan-out of `div` / `shr` / `irem` / `cmpLT` /
`cmpGE` / `intCast` / `castFloatToInt` in SPIR-V — the claim also carries
per-target `SIMPLE` emission directives with a distinct CHECK prefix each,
since the doc makes no cross-target uniformity claim for those.

Catalog rows already sampled by
[`cross-cutting/ir-instructions`](../../cross-cutting/ir-instructions/README.md)
(`add` / `sub` / `mul` / `div` on `int`, `cmpGT`, `intCast` from `uint`,
the `var` + `load` + `store` + `get_field_addr` quartet on a local struct,
single-component `swizzle`, `rwstructuredBufferGetElementPtr`) are not
re-smoke-tested here; this bundle drills into the rest of the catalog and
into the boundaries of the shared rows.

## Functional coverage

| Claim                                                                                                                                                                                                                                                                                                           | Intent                | Anchor                                                                                                                    | Tests                                                                                                                                |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| A `floatN(...)` vector-constructor call with non-constant component values lowers to `makeVector(%a, %b, ...)` of type `Vec(Float, N : Int)`.                                                                                                                                                                   | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-makevector.slang`](aggregate-makevector.slang)                                                                           |
| A `floatRxC(...)` matrix-constructor call with non-constant component values lowers to `makeMatrix(...)` producing a `Mat(Float, R : Int, C : Int, L : Int)` value.                                                                                                                                             | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-makematrix.slang`](aggregate-makematrix.slang)                                                                           |
| A `makeTuple(...)` call lowers to the `makeTuple` IR opcode producing a `tuple_type(...)` value.                                                                                                                                                                                                                | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-maketuple.slang`](aggregate-maketuple.slang)                                                                             |
| A specialized `CoopVec<T, N>(args...)` constructor call collapses, after value-pack expansion, to a `makeCoopVector(...)` IR opcode with one operand per element.                                                                                                                                               | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-makecoopvector.slang`](aggregate-makecoopvector.slang)                                                                   |
| An `InitializerListExpr` assigned to a fixed-size array lvalue lowers to a single `makeArray(...)` opcode carrying one operand per element, which is then stored through the array's address.                                                                                                                   | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-makearray-initializer.slang`](aggregate-makearray-initializer.slang)                                                     |
| Boundary: `float2(fa, fb)` (smallest documented vector dimension) lowers to `makeVector(%fa, %fb)` with result type `Vec(Float, 2 : Int)`.                                                                                                                                                                      | boundary              | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`makeVector-float2-smallest-vector.slang`](makeVector-float2-smallest-vector.slang)                                                 |
| Stress: a `float4x4(...)` constructor with sixteen `uniform` float operands lowers to a single `makeMatrix(...)` opcode with sixteen variadic operands — pattern-bloat probe for the largest documented matrix dimension.                                                                                       | stress                | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`makeMatrix-float4x4-largest-documented.slang`](makeMatrix-float4x4-largest-documented.slang)                                       |
| Stress: two distinct `float4(...)` constructions feeding a vector `add` lower to two `makeVector(...)` opcodes plus an `add(%a, %b)` on the vector type — pattern-bloat probe for componentwise vector arithmetic.                                                                                              | stress                | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`makevector-pair-feeding-add-stress.slang`](makevector-pair-feeding-add-stress.slang)                                               |
| The single-argument matrix constructor `float2x2(x)` lowers to `makeMatrixFromScalar` with one operand, splatting the scalar across the matrix.                                                                                                                                                                 | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-makematrixfromscalar.slang`](aggregate-makematrixfromscalar.slang)                                                       |
| The single-argument vector constructor `float3(x)` lowers to `MakeVectorFromScalar` with exactly one operand, not to a `makeVector` with the scalar repeated.                                                                                                                                                   | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-makevectorfromscalar.slang`](aggregate-makevectorfromscalar.slang)                                                       |
| The synthesized `__makeCoopVec<T, N>(expand each U)` variadic-pack core-module builtin (invoked by `CoopVec<T, N>(args...)` constructors) lowers to a `makeCoopVectorFromValuePack(...)` IR opcode in the generic-unspecialized form of `CoopVec.$init`.                                                        | functional            | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | [`aggregate-makecoopvectorfromvaluepack.slang`](aggregate-makecoopvectorfromvaluepack.slang)                                         |
| Bitwise `&` / `\|` / `^` lower to `and` / `or` / `xor`, and prefix `~` lowers to `bitnot`.                                                                                                                                                                                                                      | functional            | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`arith-bitwise.slang`](arith-bitwise.slang)                                                                                         |
| Boundary: `a * 4294967295u` (multiplication by `uint` MAX) lowers to `mul(%a, 4294967295 : UInt)` with the MAX literal preserved as the inline payload.                                                                                                                                                         | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`mul-uint-by-max-literal.slang`](mul-uint-by-max-literal.slang)                                                                     |
| Boundary: `bitfieldExtract(a, 0, 0)` lowers to `bitfieldExtract(%a, 0 : UInt, 0 : UInt)` — the count=0 edge of the bitfield-width range.                                                                                                                                                                        | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`bitfieldExtract-count-zero.slang`](bitfieldExtract-count-zero.slang)                                                               |
| Boundary: `bitfieldExtract(a, 0, 32)` (full-width extract on a 32-bit `uint`) lowers to `bitfieldExtract(%a, 0 : UInt, 32 : UInt)` — the upper-edge of the bitfield-width range.                                                                                                                                | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`bitfieldExtract-count-full-width.slang`](bitfieldExtract-count-full-width.slang)                                                   |
| Boundary: `bitfieldExtract(a, 31, 1)` (top-bit extract on a 32-bit `uint`) lowers to `bitfieldExtract(%a, 31 : UInt, 1 : UInt)` — the high-edge of the offset axis.                                                                                                                                             | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`bitfieldExtract-offset-high-edge.slang`](bitfieldExtract-offset-high-edge.slang)                                                   |
| Boundary: `bitfieldInsert(a, b, 0, 32)` (full-width replace on a 32-bit `uint`) lowers to `bitfieldInsert(%a, %b, 0 : UInt, 32 : UInt)` — the upper edge of the bitfield-width range.                                                                                                                           | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`bitfieldInsert-count-full-width.slang`](bitfieldInsert-count-full-width.slang)                                                     |
| Boundary: arithmetic right-shift by 31 (`a >> 31`) on a 32-bit signed int lowers to `shr(%a, 31 : Int)` — the upper-edge valid amount; canonical "extract sign bit" idiom.                                                                                                                                      | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`shr-int-by-width-minus-one.slang`](shr-int-by-width-minus-one.slang)                                                               |
| Boundary: float divide by positive-zero literal (`fa / 0.0`) lowers to `div(%a, 0 : Float)` — the `div` opcode survives even when the divisor is the IEEE-754 +zero, where integer divide-by-zero is rejected as a diagnostic.                                                                                  | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`div-float-by-positive-zero-uniform.slang`](div-float-by-positive-zero-uniform.slang)                                               |
| Boundary: integer remainder by 1 (`a % 1`) lowers to `irem(%a, 1 : Int)` — the smallest non-zero divisor edge for the documented `irem` opcode.                                                                                                                                                                 | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`irem-int-by-one.slang`](irem-int-by-one.slang)                                                                                     |
| Boundary: prefix `-` on a `uniform float` lowers to `neg(%fa)` — the float-typed sibling of the integer `neg` exercised in the smoke test.                                                                                                                                                                      | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`neg-uniform-float.slang`](neg-uniform-float.slang)                                                                                 |
| Boundary: prefix `-` on a `uniform int` (whose value spans the full signed-32 range, including the documented MIN where unary negation overflows) lowers to `neg(%a)`.                                                                                                                                          | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`neg-uniform-int.slang`](neg-uniform-int.slang)                                                                                     |
| Boundary: right-shift by 0 (`a >> 0`) lowers to `shr(%a, 0 : Int)` — the lower edge of the shift-amount axis on the `shr` opcode.                                                                                                                                                                               | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`shr-int-by-zero.slang`](shr-int-by-zero.slang)                                                                                     |
| Boundary: shift by 0 (`a << 0`) lowers to `shl(%a, 0 : Int)` — the lower edge of the shift-amount axis.                                                                                                                                                                                                         | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`shl-uint-by-zero.slang`](shl-uint-by-zero.slang)                                                                                   |
| Boundary: shift by 31 (`a << 31`) on a 32-bit `uint` lowers to `shl(%a, 31 : Int)` — the documented "width minus one" valid edge.                                                                                                                                                                               | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`shl-uint-by-width-minus-one.slang`](shl-uint-by-width-minus-one.slang)                                                             |
| Boundary: shift by 32 (`a << 32`) on a 32-bit `uint` still lowers to `shl(%a, 32 : Int)` (one past the documented valid range) — the opcode appears in IR even though semantic-check emits a left-shift-overflow warning. Verifies that lowering proceeds past the warning, leaving the literal payload intact. | boundary              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`shl-uint-by-width-overflow.slang`](shl-uint-by-width-overflow.slang)                                                               |
| Integer `%` lowers to `irem`, floating-point `%` lowers to `frem`, and prefix `-` lowers to `neg`.                                                                                                                                                                                                              | functional            | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`arith-irem-frem-neg.slang`](arith-irem-frem-neg.slang)                                                                             |
| Negative boundary: integer `a / 0` is rejected by semantic check with "divide by zero" — no `div` IR opcode is produced. (Float divide-by-zero is permitted; see `div-float-by-positive-zero-uniform.slang`.)                                                                                                   | negative              | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`int-divide-by-zero-literal-rejected.slang`](int-divide-by-zero-literal-rejected.slang)                                             |
| Prefix `!` on a boolean lowers to the `not` opcode.                                                                                                                                                                                                                                                             | functional            | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`logical-not.slang`](logical-not.slang)                                                                                             |
| The `<<` operator lowers to `shl` and `>>` lowers to `shr`; the doc notes the arithmetic-vs-logical choice is keyed by operand signedness.                                                                                                                                                                      | functional            | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`arith-shifts.slang`](arith-shifts.slang)                                                                                           |
| The `bitfieldExtract(v, off, count)` and `bitfieldInsert(b, ins, off, count)` builtins lower to the IR opcodes of the same names.                                                                                                                                                                               | functional            | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`bitfield-extract-insert.slang`](bitfield-extract-insert.slang)                                                                     |
| The `irem` opcode lowers to `OpSRem` for signed and `OpUMod` for unsigned remainder, and `frem` lowers to `OpFRem` — three distinct SPIR-V ops keyed by operand type.                                                                                                                                           | expansion             | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`irem-frem-emit-spirv-signedness.slang`](irem-frem-emit-spirv-signedness.slang)                                                     |
| The `shr` opcode lowers to `OpShiftRightArithmetic` for a signed operand and `OpShiftRightLogical` for an unsigned operand, realizing the doc's "arithmetic / logical chosen by operand signedness".                                                                                                            | expansion             | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`shr-emit-spirv-signedness.slang`](shr-emit-spirv-signedness.slang)                                                                 |
| The unified `div` IR opcode lowers to three distinct SPIR-V ops keyed by operand type: `OpSDiv` for signed, `OpUDiv` for unsigned, `OpFDiv` for floating-point.                                                                                                                                                 | expansion             | [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                               | [`div-emit-spirv-signedness.slang`](div-emit-spirv-signedness.slang)                                                                 |
| Boundary: `<=` / `>=` on `uniform float` operands lower to `cmpLE` / `cmpGE` — verifying the ordered-comparison opcodes survive on the float type (existing smoke test covers integer ordering).                                                                                                                | boundary              | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpLE-cmpGE-float-uniform.slang`](cmpLE-cmpGE-float-uniform.slang)                                                                 |
| Boundary: `==` against the float literal `0.0` lowers to `cmpEQ(%fa, 0 : Float)`, preserving the literal payload on the comparison operand.                                                                                                                                                                     | boundary              | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpEQ-float-pos-zero-literal.slang`](cmpEQ-float-pos-zero-literal.slang)                                                           |
| Boundary: `==` against the negative-zero literal `-0.0` lowers to `cmpEQ(%fa, -0 : Float)`, with the sign-distinct payload preserved on the operand.                                                                                                                                                            | boundary              | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpEQ-float-neg-zero-literal.slang`](cmpEQ-float-neg-zero-literal.slang)                                                           |
| Boundary: `fa != fa` on a `uniform float` surfaces `cmpNE(%fa, %fa)` — the NaN-detection idiom is preserved through IR lowering.                                                                                                                                                                                | boundary              | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpNE-self-uniform-float.slang`](cmpNE-self-uniform-float.slang)                                                                   |
| Boundary: `fa == fa` on a `uniform float` is not constant-folded away — both operands are the same SSA value, so the comparison surfaces `cmpEQ(%fa, %fa)`. This is the canonical NaN-detection idiom (NaN != NaN per IEEE-754).                                                                                | boundary              | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpEQ-self-uniform-float.slang`](cmpEQ-self-uniform-float.slang)                                                                   |
| Expansion: `double <= double` lowers to `cmpLE(%a, %b)` — the doc places the float ranks under the same `cmpLE` opcode as int.                                                                                                                                                                                  | expansion             | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmple-double-uniform.slang`](cmple-double-uniform.slang)                                                                           |
| Expansion: comparing two `int64_t` values lowers to `cmpLT(%a, %b)` — the doc places all integer ranks under one opcode.                                                                                                                                                                                        | expansion             | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmplt-int64-uniform.slang`](cmplt-int64-uniform.slang)                                                                             |
| Expansion: comparing two `int8_t` values lowers to `cmpEQ(%a, %b)` — verifies the opcode is the same shape at the small-int rank.                                                                                                                                                                               | expansion             | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpeq-int8-uniform.slang`](cmpeq-int8-uniform.slang)                                                                               |
| Expansion: comparing two `uint` (unsigned) values via `!=` lowers to `cmpNE(%a, %b)` — same opcode as signed-int and float operands.                                                                                                                                                                            | expansion             | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpne-uint-uniform.slang`](cmpne-uint-uniform.slang)                                                                               |
| The `<` / `<=` / `>=` operators lower to `cmpLT` / `cmpLE` / `cmpGE`.                                                                                                                                                                                                                                           | functional            | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`comparison-lt-le-ge.slang`](comparison-lt-le-ge.slang)                                                                             |
| The `==` operator lowers to `cmpEQ` and `!=` lowers to `cmpNE`.                                                                                                                                                                                                                                                 | functional            | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`comparison-eq-ne.slang`](comparison-eq-ne.slang)                                                                                   |
| The `cmpEQ` opcode lowers to `OpIEqual` for integer operands (signed and unsigned collapse to the same op) and `OpFOrdEqual` for floating-point operands.                                                                                                                                                       | expansion             | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpeq-emit-spirv-int-vs-float.slang`](cmpeq-emit-spirv-int-vs-float.slang)                                                         |
| The `cmpGE` opcode lowers to three distinct SPIR-V ops keyed by operand type: `OpSGreaterThanEqual` for signed, `OpUGreaterThanEqual` for unsigned, `OpFOrdGreaterThanEqual` for floating-point.                                                                                                                | expansion             | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmpge-emit-spirv-signedness.slang`](cmpge-emit-spirv-signedness.slang)                                                             |
| The `cmpLT` opcode lowers to three distinct SPIR-V ordering ops keyed by operand type: `OpSLessThan` for signed, `OpULessThan` for unsigned, `OpFOrdLessThan` for floating-point.                                                                                                                               | expansion             | [#comparison](../../../../design/ir-reference/values.md#comparison)                                                       | [`cmplt-emit-spirv-signedness.slang`](cmplt-emit-spirv-signedness.slang)                                                             |
| A function with an `out T` parameter has IR type `OutParam(T)`; the callee writes through the parameter with `store`, and the caller passes a `Ptr(T)` from a local `var`.                                                                                                                                      | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`out-param-type.slang`](out-param-type.slang)                                                                                       |
| A precision-changing float-to-float cast lowers to `floatCast`.                                                                                                                                                                                                                                                 | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`conversion-floatcast.slang`](conversion-floatcast.slang)                                                                           |
| Boundary: `bit_cast<float>(uintVal)` lowers to `bitCast(%a)` with `Float` result type — reinterpret across the signed/unsigned + int/float boundary.                                                                                                                                                            | boundary              | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitCast-uint-to-float-reinterpret.slang`](bitCast-uint-to-float-reinterpret.slang)                                                 |
| Boundary: precision-narrowing `float -> half` lowers to a single `floatCast(%fa)` opcode.                                                                                                                                                                                                                       | boundary              | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`floatCast-float-to-half-narrowing.slang`](floatCast-float-to-half-narrowing.slang)                                                 |
| Boundary: same-width signed-to-unsigned `int -> uint` lowers to `intCast(%a)`, even though no bit-width change is involved — the `intCast` opcode covers the signedness-flip step.                                                                                                                              | boundary              | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intCast-int-to-uint-same-width.slang`](intCast-int-to-uint-same-width.slang)                                                       |
| Boundary: widest float-widening `half -> double` lowers to a `floatCast(%ha)` opcode.                                                                                                                                                                                                                           | boundary              | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`floatCast-half-to-double-widening.slang`](floatCast-half-to-double-widening.slang)                                                 |
| Constructor-style `float(intVal)` lowers to `castIntToFloat`; `int(floatVal)` lowers to `castFloatToInt`.                                                                                                                                                                                                       | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`conversion-int-float.slang`](conversion-int-float.slang)                                                                           |
| Expansion: 16-bit same-width reinterpret `bit_cast<half>(uint16_t)` lowers to `bitCast(%u16a)` with `Half` result.                                                                                                                                                                                              | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitcast-uint16-to-half-reinterpret.slang`](bitcast-uint16-to-half-reinterpret.slang)                                               |
| Expansion: `bit_cast<double>(uint64_t)` 64-bit same-width reinterpret lowers to `bitCast(%u64a)` with `Double` result.                                                                                                                                                                                          | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitcast-uint64-to-double-reinterpret.slang`](bitcast-uint64-to-double-reinterpret.slang)                                           |
| Expansion: `bit_cast<float>(int)` signed-int-to-float reinterpret lowers to `bitCast(%a)` with `Float` result.                                                                                                                                                                                                  | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitcast-int32-to-float-reinterpret.slang`](bitcast-int32-to-float-reinterpret.slang)                                               |
| Expansion: `bit_cast<uint>(float)` reverse-direction reinterpret lowers to `bitCast(%fa)` with `UInt` result.                                                                                                                                                                                                   | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitcast-float-to-uint32-reinterpret.slang`](bitcast-float-to-uint32-reinterpret.slang)                                             |
| Expansion: `double -> float` precision-narrowing lowers to `floatCast(%da)` with `Float` result.                                                                                                                                                                                                                | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`floatcast-double-to-float-narrow.slang`](floatcast-double-to-float-narrow.slang)                                                   |
| Expansion: `double -> int64_t` truncation lowers to a single `castFloatToInt(%da)` with `Int64` result.                                                                                                                                                                                                         | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloattoint-double-to-int64.slang`](castfloattoint-double-to-int64.slang)                                                       |
| Expansion: `double -> int8_t` truncation lowers to a single `castFloatToInt(%da)` with `Int8` result — combines precision loss and rank narrowing in one opcode.                                                                                                                                                | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloattoint-double-to-int8-narrow.slang`](castfloattoint-double-to-int8-narrow.slang)                                           |
| Expansion: `double -> int` truncation lowers to a single `castFloatToInt(%da)` with `Int` result — no intermediate `float` step.                                                                                                                                                                                | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloattoint-double-to-int32.slang`](castfloattoint-double-to-int32.slang)                                                       |
| Expansion: `float -> int` truncation lowers to a single `castFloatToInt(%fa)` with `Int` result.                                                                                                                                                                                                                | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloattoint-float-to-int32.slang`](castfloattoint-float-to-int32.slang)                                                         |
| Expansion: `float -> uint` truncation (signed-to-unsigned float-to-int) lowers to a single `castFloatToInt(%fa)` with `UInt` result.                                                                                                                                                                            | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloattoint-float-to-uint32.slang`](castfloattoint-float-to-uint32.slang)                                                       |
| Expansion: `half -> uint` truncation lowers to a single `castFloatToInt(%ha)` with `UInt` result.                                                                                                                                                                                                               | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloattoint-half-to-uint32.slang`](castfloattoint-half-to-uint32.slang)                                                         |
| Expansion: `int -> half` lowers directly to `castIntToFloat(%a)` with `Half` result — no intermediate `float` step at IR level.                                                                                                                                                                                 | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castinttofloat-int32-to-half.slang`](castinttofloat-int32-to-half.slang)                                                           |
| Expansion: `int == half` triggers `castIntToFloat` on the int operand (to `Half`), then `cmpEQ` — confirms the conversion opcode targets the common float type by result rank.                                                                                                                                  | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`cmpeq-int-vs-half-implicit-conversion.slang`](cmpeq-int-vs-half-implicit-conversion.slang)                                         |
| Expansion: `int(32-bit) -> int8_t` narrowing across the full int ladder lowers to `intCast(%a)` with `Int8` result.                                                                                                                                                                                             | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-int32-to-int8-narrow.slang`](intcast-int32-to-int8-narrow.slang)                                                           |
| Expansion: `int16_t -> uint8_t` narrowing across signedness lowers to a single `intCast(%i16a)` with `UInt8` result.                                                                                                                                                                                            | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-int16-to-uint8-narrow-across-signedness.slang`](intcast-int16-to-uint8-narrow-across-signedness.slang)                     |
| Expansion: `int64_t -> uint16_t` narrowing across signedness lowers to a single `intCast(%i64a)` with `UInt16` result.                                                                                                                                                                                          | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-int64-to-uint16-narrow-across-signedness.slang`](intcast-int64-to-uint16-narrow-across-signedness.slang)                   |
| Expansion: `int8_t + int16_t` promotes the int8 operand via `intCast` to int16 before `add` — surfaces the same `intCast` opcode used for explicit constructor casts.                                                                                                                                           | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`add-int8-plus-int16-promotes-via-intcast.slang`](add-int8-plus-int16-promotes-via-intcast.slang)                                   |
| Expansion: `int8_t -> int16_t` one-step signed widening lowers to a single `intCast(%i8a)` with `Int16` result.                                                                                                                                                                                                 | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-int8-to-int16-widen.slang`](intcast-int8-to-int16-widen.slang)                                                             |
| Expansion: `select(c, intVal, floatVal)` implicitly converts the int arm via `castIntToFloat` so both arms meet a common float type; the `select` opcode then takes two `Float` operands.                                                                                                                       | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`select-mixed-int-float-arms-implicit-conversion.slang`](select-mixed-int-float-arms-implicit-conversion.slang)                     |
| Expansion: `uint32 -> int8_t` narrowing across signedness lowers to a single `intCast(%a)` with `Int8` result.                                                                                                                                                                                                  | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-uint32-to-int8-narrow-across-signedness.slang`](intcast-uint32-to-int8-narrow-across-signedness.slang)                     |
| Expansion: `uint8_t -> int32` widening (zero-extend then re-interpret) lowers to a single `intCast(%u8a)` with `Int` result.                                                                                                                                                                                    | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-uint8-to-int32-zero-extend.slang`](intcast-uint8-to-int32-zero-extend.slang)                                               |
| Expansion: comparing `int < float` triggers an implicit `castIntToFloat` on the int operand before `cmpLT`; verifies the documented conversion opcode bridges the type mismatch.                                                                                                                                | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`cmplt-int-vs-float-implicit-conversion.slang`](cmplt-int-vs-float-implicit-conversion.slang)                                       |
| Expansion: middle-of-ladder float widening `float -> double` lowers to `floatCast(%fa)` with `Double` result.                                                                                                                                                                                                   | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`floatcast-float-to-double-widen.slang`](floatcast-float-to-double-widen.slang)                                                     |
| Expansion: one-rung float widening `half -> float` lowers to a single `floatCast(%ha)` with `Float` result.                                                                                                                                                                                                     | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`floatcast-half-to-float-widen.slang`](floatcast-half-to-float-widen.slang)                                                         |
| Expansion: reverse 16-bit reinterpret `bit_cast<uint16_t>(half)` lowers to `bitCast(%ha)` with `UInt16` result.                                                                                                                                                                                                 | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitcast-half-to-uint16-reinterpret.slang`](bitcast-half-to-uint16-reinterpret.slang)                                               |
| Expansion: reverse 64-bit reinterpret `bit_cast<uint64_t>(double)` lowers to `bitCast(%da)` with `UInt64` result.                                                                                                                                                                                               | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitcast-double-to-uint64-reinterpret.slang`](bitcast-double-to-uint64-reinterpret.slang)                                           |
| Expansion: signed 64-to-32 truncation lowers to a single `intCast(%i64a)` with `Int` result — the reverse direction of the sign-extend widening case.                                                                                                                                                           | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-int64-to-int32-truncate.slang`](intcast-int64-to-int32-truncate.slang)                                                     |
| Expansion: signed `int -> float` lowers to a single `castIntToFloat(%a)` with `Float` result.                                                                                                                                                                                                                   | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castinttofloat-int32-to-float.slang`](castinttofloat-int32-to-float.slang)                                                         |
| Expansion: signed widening `int32 -> int64_t` selects the sign-extend variant of `intCast`; lowers to `intCast(%a)` with `Int64` result.                                                                                                                                                                        | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-int32-to-int64-sign-extend.slang`](intcast-int32-to-int64-sign-extend.slang)                                               |
| Expansion: small-int `uint8_t -> float` lowers to a single `castIntToFloat(%u8a)` with `Float` result.                                                                                                                                                                                                          | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castinttofloat-uint8-to-float.slang`](castinttofloat-uint8-to-float.slang)                                                         |
| Expansion: small-signed-int to double `int16_t -> double` lowers to `castIntToFloat(%i16a)` with `Double` result.                                                                                                                                                                                               | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castinttofloat-int16-to-double.slang`](castinttofloat-int16-to-double.slang)                                                       |
| Expansion: smallest-rank float to small signed int `half -> int16_t` lowers to `castFloatToInt(%ha)` with `Int16` result.                                                                                                                                                                                       | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloattoint-half-to-int16.slang`](castfloattoint-half-to-int16.slang)                                                           |
| Expansion: unsigned `uint -> float` lowers to `castIntToFloat(%a)` with `Float` result — same opcode as the signed case, signedness encoded by operand type.                                                                                                                                                    | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castinttofloat-uint32-to-float.slang`](castinttofloat-uint32-to-float.slang)                                                       |
| Expansion: widest float-narrowing `double -> half` lowers to a single `floatCast(%da)` with `Half` result.                                                                                                                                                                                                      | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`floatcast-double-to-half-precision-loss.slang`](floatcast-double-to-half-precision-loss.slang)                                     |
| Expansion: widest int-to-float `int64_t -> double` lowers to a single `castIntToFloat(%i64a)` with `Double` result.                                                                                                                                                                                             | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castinttofloat-int64-to-double.slang`](castinttofloat-int64-to-double.slang)                                                       |
| Expansion: widest signed widening `int8_t -> int64_t` (7 bits of extension) lowers to a single `intCast(%i8a)` with `Int64` result.                                                                                                                                                                             | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-int8-to-int64-widen-across-extend.slang`](intcast-int8-to-int64-widen-across-extend.slang)                                 |
| Expansion: zero-width-change `uint64_t -> int64_t` signedness flip lowers to a single `intCast(%u64a)` with `Int64` result.                                                                                                                                                                                     | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-uint64-to-int64-same-width-flip.slang`](intcast-uint64-to-int64-same-width-flip.slang)                                     |
| Negative: `bit_cast` between differently-sized types is rejected with `E41202: bit_cast size mismatch` — no `bitCast` IR opcode is produced.                                                                                                                                                                    | negative              | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`bitcast-different-width-rejected.slang`](bitcast-different-width-rejected.slang)                                                   |
| The `bit_cast<T>(...)` builtin lowers to the `bitCast` opcode (reinterpret bits without changing them).                                                                                                                                                                                                         | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`conversion-bitcast.slang`](conversion-bitcast.slang)                                                                               |
| The `emitCast` Int-to-Enum entry lowers `MyEnum(intValue)` to `CastIntToEnum`, and the Enum-to-Int entry lowers `int(enumValue)` to `CastEnumToInt`.                                                                                                                                                            | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`conversion-enum-int-roundtrip.slang`](conversion-enum-int-roundtrip.slang)                                                         |
| The `intCast` opcode lowers to `OpSConvert` when widening a signed integer and `OpUConvert` when widening an unsigned integer, realizing the doc's "sign/zero extension chosen by types".                                                                                                                       | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`intcast-emit-spirv-extend-signedness.slang`](intcast-emit-spirv-extend-signedness.slang)                                           |
| The core-module `reinterpret<T, U>(value)` function lowers to the one-operand `reinterpret` opcode, which keeps the bit pattern and changes only the type tag.                                                                                                                                                  | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`conversion-reinterpret.slang`](conversion-reinterpret.slang)                                                                       |
| The core-module `unmodified(x)` marker lowers to the no-op `unmodified` cast opcode applied to the `out` parameter it annotates.                                                                                                                                                                                | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`conversion-unmodified-out-param.slang`](conversion-unmodified-out-param.slang)                                                     |
| `castFloatToInt` lowers to `OpConvertFToS`/`OpConvertFToU` keyed by the target integer's signedness, and `castIntToFloat` lowers to `OpConvertSToF`/`OpConvertUToF` keyed by the source integer's signedness.                                                                                                   | expansion             | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`castfloatint-emit-spirv-signedness.slang`](castfloatint-emit-spirv-signedness.slang)                                               |
| `intCast` is the integer-to-integer conversion opcode; both narrowing (int -> int16_t) and widening (int -> uint64_t) lower through it.                                                                                                                                                                         | functional            | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | [`conversion-intcast-narrow.slang`](conversion-intcast-narrow.slang)                                                                 |
| Default-initialising a vector or matrix does not emit `defaultConstruct`; lowering decomposes the result type first and builds the concrete aggregate (`MakeVectorFromScalar` / `makeMatrixFromScalar`) from a zero literal.                                                                                    | functional            | [#defaultconstruct](../../../../design/ir-reference/values.md#defaultconstruct)                                           | [`defaultconstruct-vector-splat.slang`](defaultconstruct-vector-splat.slang)                                                         |
| A `MemberExpr` on a struct _value_ (rvalue position) lowers to `get_field`, while the lvalue path uses `get_field_addr`.                                                                                                                                                                                        | functional            | [#fieldaddress-vs-fieldextract](../../../../design/ir-reference/values.md#fieldaddress-vs-fieldextract)                   | [`memory-get-field-rvalue.slang`](memory-get-field-rvalue.slang)                                                                     |
| Array indexing in lvalue position lowers to `getElementPtr`; indexing into a struct-field array value lowers to `getElement`.                                                                                                                                                                                   | functional            | [#getelementptr-vs-getelement](../../../../design/ir-reference/values.md#getelementptr-vs-getelement)                     | [`memory-getelement-and-getelementptr.slang`](memory-getelement-and-getelementptr.slang)                                             |
| An integer literal `42` and a float literal `3.5` emit as a recognizable numeric token on every text-emit target.                                                                                                                                                                                               | expansion, functional | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`literal-emit-multi-target.slang`](literal-emit-multi-target.slang), [`literal-emit-spirv-asm.slang`](literal-emit-spirv-asm.slang) |
| Boundary: `uint8_t(256)` (one past unsigned-8 MAX) constant-folds to a `0 : UInt8` payload, demonstrating unsigned modular wrap on the narrowing path.                                                                                                                                                          | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`cast-uint8-max-plus-one-wraps.slang`](cast-uint8-max-plus-one-wraps.slang)                                                         |
| Boundary: a `double` literal at the edge of the documented finite range survives lowering and the IR formatter renders the upper-finite value as `inf : Double` when the literal would overflow double's normal range.                                                                                          | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`add-double-overflow-folds-to-inf.slang`](add-double-overflow-folds-to-inf.slang)                                                   |
| Boundary: documented `int16_t` MAX (32767) and MIN (-32768) survive lowering as `32767 : Int16` and `-32768 : Int16` on the `store` operands.                                                                                                                                                                   | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`store-int16-max-literal.slang`](store-int16-max-literal.slang)                                                                     |
| Boundary: documented `int64_t` MAX (9223372036854775807) survives lowering as `9223372036854775807 : Int64` on the `add` operand.                                                                                                                                                                               | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`add-int64-max-literal.slang`](add-int64-max-literal.slang)                                                                         |
| Boundary: documented `int8_t` MIN (-128) survives lowering as `-128 : Int8` on the `store` operand.                                                                                                                                                                                                             | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`store-int8-min-literal.slang`](store-int8-min-literal.slang)                                                                       |
| Boundary: documented `int` MAX (2147483647) survives lowering as a literal payload on the `add` operand of `add(%a, 2147483647 : Int)`.                                                                                                                                                                         | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`add-int32-max-literal.slang`](add-int32-max-literal.slang)                                                                         |
| Boundary: documented `int` MIN (-2147483648, expressed as `-2147483647 - 1` to avoid literal-overflow warning) survives lowering as `-2147483648 : Int` on the `add` operand.                                                                                                                                   | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`add-int32-min-literal.slang`](add-int32-min-literal.slang)                                                                         |
| Boundary: documented `uint8_t` MAX (255) and lower-edge (0) survive lowering as `255 : UInt8` and `0 : UInt8` on the `store` operands.                                                                                                                                                                          | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`store-uint8-max-literal.slang`](store-uint8-max-literal.slang)                                                                     |
| Boundary: documented `uint` MAX (4294967295) survives lowering as `4294967295 : UInt` on the `add` operand.                                                                                                                                                                                                     | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`add-uint32-max-literal.slang`](add-uint32-max-literal.slang)                                                                       |
| Boundary: largest-finite `float` literal (~3.40282e+38) survives lowering as `3.40282e+38 : Float` on an `add` operand.                                                                                                                                                                                         | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`add-float-max-literal.slang`](add-float-max-literal.slang)                                                                         |
| Boundary: negative-zero `-0.0` literal survives as `-0 : Float` on a `mul` operand, distinct from positive `0` in the IR payload.                                                                                                                                                                               | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`mul-float-neg-zero-literal.slang`](mul-float-neg-zero-literal.slang)                                                               |
| Boundary: positive `0.0` literal survives as `0 : Float` on a `mul` operand (the `add` identity collapses `+0`, so `mul` is used to keep the literal observable).                                                                                                                                               | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`mul-float-pos-zero-literal.slang`](mul-float-pos-zero-literal.slang)                                                               |
| Boundary: smallest positive normal `float` (~1.17549e-38) survives lowering as `1.17549e-38 : Float` on an `add` operand.                                                                                                                                                                                       | boundary              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`add-float-smallest-normal-literal.slang`](add-float-smallest-normal-literal.slang)                                                 |
| Constant-family opcodes (IntLit / FloatLit / BoolLit / VoidLit) carry their value inline on the IRInst and appear as `<value> : <Type>` operands rather than as separately-named lines.                                                                                                                         | functional            | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`literals-inline-payload.slang`](literals-inline-payload.slang)                                                                     |
| Negative boundary: an integer literal larger than the documented `int64` MAX (99999999999999999999) is rejected with "integer literal is too large to be represented in any integer type" — the literal payload encoding has finite range.                                                                      | negative              | [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)                           | [`int-literal-overflow-any-type-rejected.slang`](int-literal-overflow-any-type-rejected.slang)                                       |
| A `StringLiteralExpr` becomes a `string_constant` whose bytes are stored inline on the instruction, so the literal prints as its quoted text at the use site rather than as a separately-named operand line.                                                                                                    | functional            | [#literals-constant-group](../../../../design/ir-reference/values.md#literals-constant-group)                             | [`literals-string-constant-inline.slang`](literals-string-constant-inline.slang)                                                     |
| A `void`-returning function's terminator is `return_val(void_constant)`, surfacing the unique `void` value the doc lists as `VoidLit`.                                                                                                                                                                          | functional            | [#literals-constant-group](../../../../design/ir-reference/values.md#literals-constant-group)                             | [`return-val-void-constant.slang`](return-val-void-constant.slang)                                                                   |
| `castToVoid` is declared but never constructed: a `(void)expr` cast is intercepted and yields the unique `void_constant` value instead, and the operand's side effects survive as their own instructions.                                                                                                       | functional            | [#live-but-unproduced-opcodes](../../../../design/ir-reference/values.md#live-but-unproduced-opcodes)                     | [`cast-to-void-yields-void-constant.slang`](cast-to-void-yields-void-constant.slang)                                                 |
| The free `and(x, y)` / `or(x, y)` core-module functions lower to the `logicalAnd` / `logicalOr` opcodes, which print in the IR dump as `and` / `or` on a `Bool` result.                                                                                                                                         | functional            | [#logical](../../../../design/ir-reference/values.md#logical)                                                             | [`logical-and-or-no-short-circuit.slang`](logical-and-or-no-short-circuit.slang)                                                     |
| The short-circuiting `&&` operator is NOT the lowering of `logicalAnd`; it lowers to an `ifElse` plus a join-block `param` so the right-hand side is evaluated only on the taken edge.                                                                                                                          | functional            | [#logical](../../../../design/ir-reference/values.md#logical)                                                             | [`logical-short-circuit-lowers-to-ifelse.slang`](logical-short-circuit-lowers-to-ifelse.slang)                                       |
| A module-scope `static const T K = value;` declaration lowers to `globalConstant(<value>)` in the IR.                                                                                                                                                                                                           | functional            | [#memory](../../../../design/ir-reference/values.md#memory)                                                               | [`memory-global-constant.slang`](memory-global-constant.slang)                                                                       |
| Pointer `operator+` lowers to `getOffsetPtr(base, offset)`, which advances a pointer by a count of elements rather than by bytes and is a different opcode from `getElementPtr`.                                                                                                                                | functional            | [#memory](../../../../design/ir-reference/values.md#memory)                                                               | [`memory-getoffsetptr-pointer-arithmetic.slang`](memory-getoffsetptr-pointer-arithmetic.slang)                                       |
| Boundary: `Optional<int> o = none;` (bare `none` literal at the construction site) lowers to `makeOptionalNone` — the empty-side edge of the Optional value space.                                                                                                                                              | boundary              | [#result--optional--conditional-helpers](../../../../design/ir-reference/values.md#result--optional--conditional-helpers) | [`optional-always-none.slang`](optional-always-none.slang)                                                                           |
| Boundary: a function that unconditionally returns `Optional<int>` from a value (no `none` branch) surfaces `makeOptionalValue(%x)` exactly once — the value-side edge of the Optional value space.                                                                                                              | boundary              | [#result--optional--conditional-helpers](../../../../design/ir-reference/values.md#result--optional--conditional-helpers) | [`optional-always-value.slang`](optional-always-value.slang)                                                                         |
| `Optional<T>` constructors (`return x;` and `return none;`) lower to `makeOptionalValue` / `makeOptionalNone`, and accessors `o.hasValue` / `o.value` lower to `optionalHasValue` / `getOptionalValue`.                                                                                                         | functional            | [#result--optional--conditional-helpers](../../../../design/ir-reference/values.md#result--optional--conditional-helpers) | [`optional-make-has-value.slang`](optional-make-has-value.slang)                                                                     |
| `Result<T, E>` helpers are observable through Slang's throwing-function surface: a `throws` function lowers `return x;` to `makeResultValue(x)` and `throw e;` to `makeResultError(e)`, while a `try` / `catch` site in the caller lowers to `isResultError`, `getResultValue`, and `getResultError` accessors. | functional            | [#result--optional--conditional-helpers](../../../../design/ir-reference/values.md#result--optional--conditional-helpers) | [`result-make-and-accessors.slang`](result-make-and-accessors.slang)                                                                 |
| A scalar-conditioned ternary `?:` inside a function does NOT lower to `select`; it lowers to an `ifElse` with then/else blocks and a join-block `param`, because Slang's scalar `?:` short-circuits.                                                                                                            | functional            | [#select](../../../../design/ir-reference/values.md#select)                                                               | [`select-scalar-ternary-lowers-to-ifelse.slang`](select-scalar-ternary-lowers-to-ifelse.slang)                                       |
| A ternary `?:` whose condition is a `vector<bool,N>` (not a `BasicExpressionType`) lowers to the branch-free `select` opcode rather than to `ifElse` plus a join-block param.                                                                                                                                   | functional            | [#select](../../../../design/ir-reference/values.md#select)                                                               | [`select-vector-bool-condition-ternary.slang`](select-vector-bool-condition-ternary.slang)                                           |
| Boundary: `select(c, 1, 0)` with literal-integer true/false arms lowers to `select(%c, 1 : Int, 0 : Int)` with the BoolLit-equivalent literal payloads on the result operands.                                                                                                                                  | boundary              | [#select](../../../../design/ir-reference/values.md#select)                                                               | [`select-true-literal-bool.slang`](select-true-literal-bool.slang)                                                                   |
| Expansion: `select(c, vec3, vec3)` with vector operands lowers to a single `select(%c, %a, %b)` with `Vec(Float, 3)` result — verifies the opcode handles aggregate operand pairs.                                                                                                                              | expansion             | [#select](../../../../design/ir-reference/values.md#select)                                                               | [`select-vector-float3-arms.slang`](select-vector-float3-arms.slang)                                                                 |
| Expansion: `select` with widest-rank float arms lowers to `select(%c, %a, %b)` with `Double` result.                                                                                                                                                                                                            | expansion             | [#select](../../../../design/ir-reference/values.md#select)                                                               | [`select-double-arms.slang`](select-double-arms.slang)                                                                               |
| Expansion: `select` with widest-rank integer arms lowers to `select(%c, %a, %b)` with `Int64` result.                                                                                                                                                                                                           | expansion             | [#select](../../../../design/ir-reference/values.md#select)                                                               | [`select-int64-arms.slang`](select-int64-arms.slang)                                                                                 |
| The `select(condition, trueValue, falseValue)` builtin lowers to the branch-free `select` IR opcode.                                                                                                                                                                                                            | functional            | [#select](../../../../design/ir-reference/values.md#select)                                                               | [`select-builtin.slang`](select-builtin.slang)                                                                                       |
| A multi-component swizzle (`v.xyz`) lowers to a `swizzle` opcode taking the source vector and one literal index per output lane.                                                                                                                                                                                | functional            | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-multi-component.slang`](swizzle-multi-component.slang)                                                                     |
| A multi-component vector swizzle (`v.xyz`) lowers to a recognizable `.x` / `.y` / `.z` member-access form on every text-emit target.                                                                                                                                                                            | expansion, functional | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-emit-multi-target.slang`](swizzle-emit-multi-target.slang), [`swizzle-emit-spirv-asm.slang`](swizzle-emit-spirv-asm.slang) |
| A swizzle in lvalue position (`dst.xy = ...`) lowers to `swizzledStore` mutating memory in place.                                                                                                                                                                                                               | functional            | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-store-lvalue.slang`](swizzle-store-lvalue.slang)                                                                           |
| Assignment to a matrix-swizzle lvalue lowers to `matrixSwizzleStore`, whose trailing operands come in (row, col) pairs — two operands per selected element, not one.                                                                                                                                            | functional            | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-matrix-swizzle-store.slang`](swizzle-matrix-swizzle-store.slang)                                                           |
| Boundary: M=4 swizzle from an N=2 source (`vec2.xxxy`) lowers to `swizzle(%v, 0, 0, 0, 1)` — M > N, mixed-repeat indices.                                                                                                                                                                                       | boundary              | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-mixed-xxxy-from-vec2.slang`](swizzle-mixed-xxxy-from-vec2.slang)                                                           |
| Boundary: full-length swizzle `.xyzw` on a `float4` lowers to `swizzle(%v, 0, 1, 2, 3)` — the M=N edge of the swizzle width.                                                                                                                                                                                    | boundary              | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-full-length-xyzw.slang`](swizzle-full-length-xyzw.slang)                                                                   |
| Boundary: repeat-component swizzle (`v.xxxx`) lowers to `swizzle(%v, 0, 0, 0, 0)` — the same literal index repeated four times, exercising the duplicate-lane case of the doc's "M elements" claim.                                                                                                             | boundary              | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-repeat-component-xxxx.slang`](swizzle-repeat-component-xxxx.slang)                                                         |
| Boundary: single-component swizzle from a `float4` (`v.x`) lowers to `swizzle(%v, 0 : Int)` with exactly one literal index — the M=1 edge of the doc's "M elements from an N-vector" claim.                                                                                                                     | boundary              | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-single-component-from-vec4.slang`](swizzle-single-component-from-vec4.slang)                                               |
| Negative boundary: accessing the `.w` component of a `float3` (one past the N=3 edge) is rejected with "'w' is not a member of 'vector<float,3>'." — confirms the index-range check at every documented vector width.                                                                                           | negative              | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-w-on-vec3-rejected.slang`](swizzle-w-on-vec3-rejected.slang)                                                               |
| Negative boundary: accessing the `.z` component of a `float2` (out of the documented N-vector range) is rejected by semantic check with "'z' is not a member of 'vector<float,2>'." — no swizzle opcode is produced.                                                                                            | negative              | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | [`swizzle-z-on-vec2-rejected.slang`](swizzle-z-on-vec2-rejected.slang)                                                               |
| A local variable becomes a nullary `var` whose result type is `Ptr<T>`, so member and element access chain off it through address opcodes rather than through the value.                                                                                                                                        | functional            | [#var-vs-globalvar-vs-alloca](../../../../design/ir-reference/values.md#var-vs-globalvar-vs-alloca)                       | [`memory-var-result-type-is-ptr.slang`](memory-var-result-type-is-ptr.slang)                                                         |

## Untested claims

| Claim                                                                                                                                                                                                                                                                                                   | Reason                | Anchor                                                                                                                    | Why untested                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Poison` is an infectious undefined value, hoistable so every poison value of a given type dedupes to one inst, and `LoadFromUninitializedMemory` is the `freeze(undef)`-like load that frontend diagnostics surface.                                                                                   | link-stage-only       | [#poison](../../../../design/ir-reference/values.md#poison)                                                               | The IR dump carries `Poison` only as a preamble sigil declaration; no surface named by the doc makes it an active operand of a user instruction at this bundle's `lower` stage, and reading an uninitialized local is a frontend diagnostic rather than a positive opcode observation.  |
| `alloca` allocates stack storage sized from an RTTI object at run time, `getAddr` would mark a pointer as explicitly obtained, `PtrCast` converts between pointer types of different element types, and `ReinterpretOptional` performs a covariant `Optional<T>` to `Optional<U>` conversion.           | implementation-detail | [#live-but-unproduced-opcodes](../../../../design/ir-reference/values.md#live-but-unproduced-opcodes)                     | The doc states outright that nothing constructs these four at `source_commit`, so there is no input that makes one appear. Their absence is only observable per-program, and the one case with a core-module declaration that could mislead a reader (`castToVoid`) already has a test. |
| `assumeAddress` marks an address obtained via `__getAddress` so IR validation can reject targets that forbid it, and is lowered away to its operand after validation.                                                                                                                                   | link-stage-only       | [#memory](../../../../design/ir-reference/values.md#memory)                                                               | The `validateAndRemoveAssumeAddress` pass runs before the first snapshot a `-dump-ir` test can observe, so the opcode is already gone by the earliest observation point available to this bundle.                                                                                       |
| `copyLogical` copies a whole value between two pointers member by member without reinterpreting bytes, and its inherited `getVal()` returns the source pointer rather than a value.                                                                                                                     | link-stage-only       | [#memory](../../../../design/ir-reference/values.md#memory)                                                               | Produced by buffer-element-type legalization, an IR pass well past this bundle's `lower` stage; the doc names no AST surface for it.                                                                                                                                                    |
| `updateElement` is a functional update that returns a copy of an aggregate with one nested element replaced, carrying an arbitrarily long access chain of struct keys and element indices after its first two operands.                                                                                 | link-stage-only       | [#updateelement](../../../../design/ir-reference/values.md#updateelement)                                                 | The doc marks the `AST origin` as `(synthesized)`; the opcode is introduced by later IR passes to keep code in SSA form, not by any expression visitor this bundle can reach.                                                                                                           |
| `swizzleSet` returns a copy of a vector with the selected lanes replaced by a source value, with its indices starting at operand 2.                                                                                                                                                                     | link-stage-only       | [#swizzle-swizzleset-swizzledstore](../../../../design/ir-reference/values.md#swizzle-swizzleset-swizzledstore)           | The doc distinguishes it from `swizzledStore` by whether the assignment destination is addressable; every portable swizzle-assignment surface probed here is addressable and therefore takes the `swizzledStore` path, so `swizzleSet` never appears at `lower`.                        |
| `makeValuePack` constructs a hoistable value-pack aggregate from a `PackExpr`, `makeTargetTuple` / `getTargetTupleElement` build and read a target-keyed tuple for `targetSwitch`, and `makeCombinedTextureSampler` bundles a texture with a sampler.                                                   | out-of-bundle         | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | Value packs belong to the variadic-generics surface covered by `generics-and-existentials`, `targetSwitch` is owned by `ir-reference/control-flow`, and combined texture-samplers are a resource-type claim owned by `ir-reference/types`.                                              |
| `SumVectorElements` and `SumMatrixElements` sum all elements of a vector or matrix, and `vectorReshape` reshapes a vector.                                                                                                                                                                              | link-stage-only       | [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                               | All three are marked `(synthesized)`; the first two are introduced by autodiff transposition and the third only by `IRBuilder::emitVectorReshape`, none of which runs at this bundle's `lower` stage.                                                                                   |
| `makeUInt64(low, high)` constructs a 64-bit value from two 32-bit halves because several targets have no direct `uint64` literal form.                                                                                                                                                                  | link-stage-only       | [#makeuint64](../../../../design/ir-reference/values.md#makeuint64)                                                       | The doc gives the `AST origin` as `(synthesized)` and notes there is no `MakeUInt64Expr`; a natural `uint64_t` literal keeps its inline payload at `lower` and the split into halves, if it happens, is a backend concern.                                                              |
| `makeString` constructs a `String` from a `NativeString`, `getNativeStr` returns an unowned view of one, and `getNativePtr` / `getManagedPtrWriteRef` / `ManagedPtrAttach` / `ManagedPtrDetach` manipulate `ComPtr<T>`-style managed pointers.                                                          | needs-unit-test       | [#strings-and-native-pointers](../../../../design/ir-reference/values.md#strings-and-native-pointers)                     | These are host-side COM / managed-pointer surfaces with no shader-language spelling; a C++ unit test in `tools/slang-unit-test/` driving `IRBuilder` and the host targets is the shape of test that would verify them.                                                                  |
| `allocObj` allocates an object value for host-side and managed-pointer lowering, and `CUDA_LDG` is the read-only cached load introduced by the CUDA immutable-load pass.                                                                                                                                | link-stage-only       | [#object-and-cuda-helpers](../../../../design/ir-reference/values.md#object-and-cuda-helpers)                             | Both are marked `(synthesized)`; `CUDA_LDG` in particular is introduced by a CUDA-specific pass that runs long after `lower`, so it cannot appear in the observation window this bundle uses.                                                                                           |
| `makeConditionalValue` and `getConditionalValue` construct and read a `Conditional<T>`, and `makeResultValue` / `makeResultError` / `isResultError` / `getResultValue` / `getResultError` are described as `Result<T, E>` member-call lowerings.                                                        | out-of-bundle         | [#result--optional--conditional-helpers](../../../../design/ir-reference/values.md#result--optional--conditional-helpers) | The `Result` half is covered here through the error-handling surface (`result-make-and-accessors.slang`); `Conditional<T>` has no core-module constructor call reachable from shader code, and the doc gives no other surface.                                                          |
| `extractTaggedUnionTag` and `extractTaggedUnionPayload` read the tag and payload of an existential's tagged-union representation.                                                                                                                                                                       | out-of-bundle         | [#result--optional--conditional-helpers](../../../../design/ir-reference/values.md#result--optional--conditional-helpers) | The doc itself hands these off to `generics-and-existentials.md`, and records that their `IRBuilder` emitters have no callers at `source_commit`.                                                                                                                                       |
| The whole `constexpr*` family (`constexprAdd` through `constexprEnumCast`) lowers folded `IntVal` subclasses so identical compile-time values dedupe, with `BuiltinOperationKind::Mod` becoming `constexprIRem` and `&&` / `\|\|` becoming `constexprAnd` / `constexprOr` inside a constant expression. | link-stage-only       | [#constexpr-family](../../../../design/ir-reference/values.md#constexpr-family)                                           | These come from `lowerVal` walking the checked, folded `IntVal` form rather than from any expression visitor, and appear interspersed with ordinary arithmetic in generic array-extent lowering; the doc names no surface that isolates one at this bundle's stage.                     |
| `CastUInt2ToDescriptorHandle` / `CastDescriptorHandleToUInt2`, `CastUInt64ToDescriptorHandle` / `CastDescriptorHandleToUInt64`, and `CastDescriptorHandleToResource` / `CastResourceToDescriptorHandle` convert to and from the opaque bindless `DescriptorHandle<T>`.                                  | gpu-bindless          | [#descriptor-handle-conversions](../../../../design/ir-reference/values.md#descriptor-handle-conversions)                 | The 64-bit pair is gated on the `spvBindlessTextureNV` and `cuda` capabilities, and the resource pair is produced by Metal parameter-block lowering; a bindless-capable target configuration is what would unblock a test.                                                              |
| `BuiltinCast` is the fallback `emitCast` emits when either side's `TypeCastStyle` is `Unknown`, leaving the conversion for a later pass.                                                                                                                                                                | implementation-detail | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | The doc characterises the opcode by an internal classification (`TypeCastStyle::Unknown`) rather than by a user-level type pair, so there is no way to pick a source construct that is guaranteed to land in that cell rather than in a concrete one.                                   |
| `CastPtrToBool` tests a pointer for non-null, `CastPtrToInt` reinterprets a pointer as an integer, and `CastIntToPtr` does the reverse.                                                                                                                                                                 | gpu-vulkan-extension  | [#conversions](../../../../design/ir-reference/values.md#conversions)                                                     | Raw pointer values with integer conversions need a target with physical storage-buffer addresses enabled; no portable text-emit configuration in this bundle exposes them.                                                                                                              |
| Every opcode in this family has an `IR<struct_name>` wrapper struct, hand-written wrappers ignore the Lua operand names, and generated wrappers derive their accessors from the Lua `operands` list.                                                                                                    | internal-source-fact  | [#c-wrappers-hand-written-vs-generated](../../../../design/ir-reference/values.md#c-wrappers-hand-written-vs-generated)   | The C++ class hierarchy and accessor names are invisible to every `slang-test` directive; the IR dump prints the lowercase Lua key, never the wrapper name.                                                                                                                             |
| Two `IntLit 42` produce the same IR value because the `Constant` opcodes are deduplicated through the constant map, and the `constexpr*` and `Poison` opcodes dedupe through the hoistable flag.                                                                                                        | implementation-detail | [#literals-constant-group](../../../../design/ir-reference/values.md#literals-constant-group)                             | Deduplication is `IRInst*` identity, which the textual dump renders identically whether or not two insts were merged; no test directive distinguishes the two states.                                                                                                                   |

## Doc gaps observed

| Anchor                                                                                                        | Kind                  | Gap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Suggested addition                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                   | drift-from-source     | The table gives `makeStruct` the `AST origin` "`InitializerListExpr` (line 6970) and `getDefaultVal` (line 6731)", but every natural struct-initializer surface probed here — `Foo f = { a, b };`, `Foo f = Foo(a, b);`, and assigning an initializer list to a struct-typed lvalue — lowers to a call to a synthesized `$init` function instead, with no `makeStruct` in the dump. The sibling `makeArray` row _is_ reachable, but only when the initializer list is assigned to an existing array lvalue rather than used in a declaration. | Add a sentence to the Aggregate constructors intro saying that struct initializer lists are routed through the synthesized `$init` constructor and that `makeStruct` survives only where no `$init` exists, and note on the `makeArray` row that a declaration-with-initializer is streamed into element stores while an assignment builds the aggregate. |
| [#aggregate-constructors](../../../../design/ir-reference/values.md#aggregate-constructors)                   | missing-surface       | `makeCoopMatrixFromScalar` is listed as `(synthesized)` with `emitDefaultConstruct` for cooperative-matrix types as its only producer, but `CoopMat` carries user-defined `__init()` / `__init(T)` constructors in `hlsl.meta.slang` that intercept the default-construct path, so every probe (`CoopMat<T,...>()`, an array of them, a struct member) lowers to a `$init` call instead.                                                                                                                                                      | State on the `makeCoopMatrixFromScalar` row that the core-module `CoopMat` constructors shadow the default-construct path, so the opcode is only reachable from internal `IRBuilder` use and not from Slang source.                                                                                                                                       |
| [#reshape-and-pack-helpers](../../../../design/ir-reference/values.md#reshape-and-pack-helpers)               | drift-from-source     | The table gives `matrixReshape` the `AST origin` "core-module `matrix.__init(matrix<T,R,C,L>)` reshaping overloads", but `float2x2(m3)` where `m3` is a `float3x3` lowers to two `getElement` reads, two `swizzle`s, and a `makeMatrix` — no `matrixReshape` opcode appears.                                                                                                                                                                                                                                                                  | Either mark the `matrixReshape` row `(synthesized)` like `vectorReshape`, or name the specific reshaping overload that survives as a single opcode instead of being expanded into element reads.                                                                                                                                                          |
| [#reshape-and-pack-helpers](../../../../design/ir-reference/values.md#reshape-and-pack-helpers)               | drift-from-source     | The table gives `getTupleElement` the `AST origin` "`MemberExpr` on a tuple; `EachExpr`", but reading `t._0` from a `Tuple<int, float>` lowers to `swizzle(%t, 0 : Int)` — the same opcode that reads vector lanes — not to `getTupleElement`.                                                                                                                                                                                                                                                                                                | Say on the `getTupleElement` row that positional tuple member access reuses the `swizzle` opcode at lowering, and name the surface (if any) that produces `getTupleElement` directly.                                                                                                                                                                     |
| [#conversions](../../../../design/ir-reference/values.md#conversions)                                         | drift-from-source     | The table gives `EnumCast` the `AST origin` "`emitCast` table (Enum→Enum)", but converting between two `enum` types with the same underlying type — `Other(c)` where `c` is a `Color` — lowers to a `CastEnumToInt` followed by a `CastIntToEnum`, because the checker routes the conversion through the underlying integer type before `emitCast` sees an Enum→Enum pair.                                                                                                                                                                    | Note on the `EnumCast` row that a source-level enum-to-enum conversion is decomposed by the checker into an enum-to-int and an int-to-enum step, so the Enum→Enum cell is reached only when a single `emitCast` call already has both enum types in hand.                                                                                                 |
| [#conversions](../../../../design/ir-reference/values.md#conversions)                                         | missing-surface       | The table lists `outImplicitCast` and `inOutImplicitCast` with the `AST origin` "`out`-argument lowering in `tryGetAddress`", but a same-type `out` / `inout` argument passes the caller's `var` pointer straight through with no cast instruction, and an argument whose type differs is rejected at semantic check before lowering ("argument must be an l-value"). No portable surface in the doc produces either opcode.                                                                                                                  | Add the condition under which `tryGetAddress` inserts the cast — presumably a legalization-introduced type difference rather than a source-level one — or mark both rows `(synthesized)` alongside the other pass-introduced conversions.                                                                                                                 |
| [#conversions](../../../../design/ir-reference/values.md#conversions)                                         | missing-surface       | The Conversions table does not mention the HLSL-compatible reinterpret builtins `asuint` / `asint` / `asfloat` / `asdouble`, which a reader looking for the bit-reinterpret opcode will reach for first. Probing shows they stay call-shaped at `lower` (a `call %asuint(...)` carrying a `targetIntrinsic` decoration) rather than becoming `bitCast`, so the reader is left without a mapping.                                                                                                                                              | Add a note under the `bitCast` row naming `asuint` / `asint` / `asfloat` / `asdouble` and saying they remain calls through the value pipeline and are resolved by the target intrinsic mechanism, unlike `bit_cast<T>` which becomes the opcode directly.                                                                                                 |
| [#memory](../../../../design/ir-reference/values.md#memory)                                                   | missing-surface       | The `swizzleSet` row is distinguished from `swizzledStore` only by the phrase "when the destination is addressable" on the latter, which does not tell a reader what makes a swizzle destination _un_-addressable in Slang source. Every swizzle-assignment surface tried here took the `swizzledStore` path.                                                                                                                                                                                                                                 | Give one concrete example of a non-addressable swizzle destination (for example a swizzle of a value returned by a function, or of a `let`-bound temporary) so the `swizzleSet` row has a reachable surface.                                                                                                                                              |
| [#defaultconstruct](../../../../design/ir-reference/values.md#defaultconstruct)                               | missing-example       | The section explains that `emitDefaultConstruct` decomposes the result type first and "only falls back to `kIROp_DefaultConstruct` ... when its `fallback` argument is set", so "a `defaultConstruct` in the IR usually means the type was one it could not decompose" — but it never names a type that cannot be decomposed. A reader cannot construct an example that actually yields the opcode.                                                                                                                                           | Name one or two concrete result types for which decomposition fails (an opaque resource type, an interface-typed value, a generic type parameter) so the fallback case has a worked example.                                                                                                                                                              |
| [#undefined-and-default-construct](../../../../design/ir-reference/values.md#undefined-and-default-construct) | missing-surface       | Both `LoadFromUninitializedMemory` and `Poison` are given the `AST origin` "(synthesized)" with no indication of which pass synthesizes them, unlike other synthesized rows on the page which name their producing pass and file.                                                                                                                                                                                                                                                                                                             | Add the producing pass and file for each row, in the same style as the `copyLogical` and `CUDA_LDG` rows, so a reader knows where to look for an example.                                                                                                                                                                                                 |
| [#literal-payload-encoding](../../../../design/ir-reference/values.md#literal-payload-encoding)               | undocumented-behavior | The section says every `Constant` opcode stores its value inline on the `IRInst`, but does not say how the dump _renders_ that payload, which is what a reader of an IR dump actually needs. In practice a literal appears as `<value> : <Type>` inline on each use (`42 : Int`, `-0 : Float`, `void_constant`) and never as a separate `let %N = integer_constant(...)` definition line, and an out-of-range `double` renders as `inf : Double`.                                                                                             | Add a two-line example of dump output showing a literal appearing inline as an operand, and state explicitly that literals never get their own definition line.                                                                                                                                                                                           |
| [#arithmetic-and-bitwise](../../../../design/ir-reference/values.md#arithmetic-and-bitwise)                   | undocumented-behavior | The table treats `div` and `irem` as total operations keyed only by operand type, but a literal zero divisor on the integer forms is rejected at semantic check and never reaches lowering, whereas the floating-point forms accept a zero divisor and keep the literal on the `div` operand. The asymmetry is invisible from the table.                                                                                                                                                                                                      | Add a sentence after the table noting that integer division or remainder by a constant zero is diagnosed in the frontend, so those cases produce no opcode at all, while the floating-point forms lower normally.                                                                                                                                         |
