---
generated: true
model: claude-opus-5[1m]
generated_at: 2026-08-04T00:00:00+00:00
source_commit: 7e725f15572c6589ee6d738a8856fb3348f11617
watched_paths_digest: f5a71578a7adbed43419a108d4f552132ead5e2a086e82d51a087bd2803b6554
source_doc: docs/generated/design/name-resolution/scopes.md
source_doc_digest: 5dd289eab5487116539277ab860308013ccf93c7f342f7901a9be1b9b39834cf
warning: "Auto-generated. May drift from source. Do not edit by hand."
---

# Tests for name-resolution/scopes

## Intent

This bundle exercises the lexical-scoping rules described in
[`docs/generated/design/name-resolution/scopes.md`](../../../../design/name-resolution/scopes.md):
which AST nodes introduce a fresh `Scope`, what is reachable from which
point in the source, how lookup walks the parent chain plus the sibling
chain, and the documented edge cases (per-decl hiding inside a block,
extension-member exclusion, generic-parameter isolation, namespace
reopening, `using`-decl sibling wiring, the synthesized `This` member of
an interface, interface constraint placement, deferred body parsing, and
the `if (let ...)` synthetic scope).

Scoping is not directly observable, so every claim is translated into
name resolution: either **a name resolves to the declaration the doc
says it does** — a positive `//TEST:INTERPRET` whose `printf` echoes a
value that only a correct binding can produce — or **a name is not in
scope at a given point** — a `//DIAGNOSTIC_TEST:SIMPLE(diag=CHECK)`
pinning `undefined identifier` (E30015) at the use site with
caret-anchored annotations. Each scope kind that the doc's
"Scope-bearing AST nodes" table names and that has a surface syntax gets
both halves: a positive that resolves a name inside the scope, and a
negative that proves the same name is not visible just outside it, since
the boundary is what the doc is mostly about.

Multi-backend rule: every claim on this page is fixed at semantic check,
before any target-specific lowering, so each test carries a single
directive and there is no per-target emission fan-out. No test needs a
GPU.

## Functional coverage

| Claim                                                                                                                                                                                                                                   | Intent     | Anchor                                                                                                               | Tests                                                                                                                      |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| A `__constraint` whose subject is an associated type reached through `This` (e.g. `This.A : IBar`) is permitted, unlike a bare-This subject.                                                                                            | functional | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`constraint-subject-assoctype-allowed.slang`](constraint-subject-assoctype-allowed.slang)                                 |
| A `__constraint` whose subject resolves to the bare `This` type is rejected with constraint-subject-cannot-be-this-type (E30427); giving an interface a base is the inheritance clause's role.                                          | negative   | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`constraint-subject-this-rejected.slang`](constraint-subject-this-rejected.slang)                                         |
| A `using` declaration only extends the sibling chain of the scope captured where it was written, so a `using` inside a namespace does not make the names unqualified outside that namespace.                                            | negative   | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`using-inside-namespace-not-visible-outside.slang`](using-inside-namespace-not-visible-outside.slang)                     |
| A block-local declaration is still hidden from lookup while its own initializer is checked, so a same-named outer local is what the initializer resolves to.                                                                            | boundary   | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`block-decl-init-resolves-outer-same-name.slang`](block-decl-init-resolves-outer-same-name.slang)                         |
| A function body parsed after the declaration pass resolves names against the scope chain that was live at its declaration site, not at file scope.                                                                                      | functional | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`deferred-body-resolves-at-declaration-site-scope.slang`](deferred-body-resolves-at-declaration-site-scope.slang)         |
| A sibling of the outer decl that mentions a generic type parameter cannot reach it; its scope chain does not pass through the GenericDecl.                                                                                              | negative   | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`generic-param-not-visible-in-sibling-decl.slang`](generic-param-not-visible-in-sibling-decl.slang)                       |
| ExtensionDecl members are reachable only via member lookup on the extended type, never as unqualified file-scope names.                                                                                                                 | negative   | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`extension-method-not-in-enclosing-scope.slang`](extension-method-not-in-enclosing-scope.slang)                           |
| Inside a block the `Decl::hiddenFromLookup` flag hides a local until its DeclStmt; a forward reference inside the same block is rejected as undefined.                                                                                  | negative   | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`block-scope-forward-ref-rejected.slang`](block-scope-forward-ref-rejected.slang)                                         |
| The block-local `hiddenFromLookup` rule does not apply at file scope; a function may refer to another function declared later in the same file.                                                                                         | functional | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`file-scope-forward-reference-allowed.slang`](file-scope-forward-reference-allowed.slang)                                 |
| When a `using` argument does not resolve to a namespace-like entity, no sibling scope is added and the checker diagnoses expected-a-namespace (E30061).                                                                                 | negative   | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes)           | [`using-non-namespace-rejected.slang`](using-non-namespace-rejected.slang)                                                 |
| A default-implementation body inside an interface parses against a scope whose chain reaches the interface's requirement list, so a sibling requirement binds unqualified.                                                              | functional | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`interface-default-impl-body-sees-requirement.slang`](interface-default-impl-body-sees-requirement.slang)                 |
| A generic type parameter lives in the GenericDecl's scope; the inner decl's scope chain passes through it, so `T` resolves inside the function body.                                                                                    | functional | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`generic-param-visible-in-body.slang`](generic-param-visible-in-body.slang)                                               |
| The ScopeDecl that `if (let x = ...)` creates for the unwrapped variable covers only the positive branch; the name is undefined after the if statement.                                                                                 | negative   | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`if-let-unwrapped-not-visible-after-if.slang`](if-let-unwrapped-not-visible-after-if.slang)                               |
| The `associatedtype A : IBar` inheritance-clause form places its GenericTypeConstraintDecl in the enclosing interface's scope as a requirement member, so generic code constrained to the interface can call IBar's requirement on T.A. | functional | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`assoctype-constraint-inheritance-clause-form.slang`](assoctype-constraint-inheritance-clause-form.slang)                 |
| The `associatedtype A where A : IBar` where-clause form places its GenericTypeConstraintDecl in the enclosing interface's scope like the inheritance-clause form, so generic code can call IBar's requirement on T.A.                   | functional | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`assoctype-constraint-where-clause-form.slang`](assoctype-constraint-where-clause-form.slang)                             |
| The `if (let x = ...)` desugaring creates a ScopeDecl for the user-visible unwrapped variable inside the positive branch, so the unwrapped name resolves there.                                                                         | functional | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`if-let-unwrapped-visible-in-positive-branch.slang`](if-let-unwrapped-visible-in-positive-branch.slang)                   |
| The dedicated `__constraint` keyword inserts a GenericTypeConstraintDecl directly into the interface scope, equivalent to the two associatedtype clause forms, so generic code can call IBar's requirement on T.A.                      | functional | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`interface-constraint-keyword-form.slang`](interface-constraint-keyword-form.slang)                                       |
| The extended type's members are not on the extension's scope chain but are still reached from an extension method through member lookup on the receiver.                                                                                | functional | [#implicit-scopes](../../../../design/name-resolution/scopes.md#implicit-scopes)                                     | [`extension-body-reaches-extended-type-members.slang`](extension-body-reaches-extended-type-members.slang)                 |
| A BlockStmt introduces a fresh scope; a local declared inside is not visible outside that block.                                                                                                                                        | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`block-scope-name-not-visible-outside.slang`](block-scope-name-not-visible-outside.slang)                                 |
| A CallableDecl's scope contains its parameter decls; the parameter is reachable from the function body.                                                                                                                                 | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`function-scope-parameter-visible-in-body.slang`](function-scope-parameter-visible-in-body.slang)                         |
| A FuncDecl owns its own scope; a local declared inside helper() is not visible from another function's scope.                                                                                                                           | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`function-scope-local-not-visible-from-caller.slang`](function-scope-local-not-visible-from-caller.slang)                 |
| A NamespaceDecl owns its own scope; members declared inside it are not visible from the enclosing module scope without qualification.                                                                                                   | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`namespace-member-needs-qualification.slang`](namespace-member-needs-qualification.slang)                                 |
| A `let`-bound local declared inside a block lives in the BlockStmt's scope only; it cannot be referenced from a later statement at the enclosing scope.                                                                                 | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`let-in-block-not-visible-outside.slang`](let-in-block-not-visible-outside.slang)                                         |
| A catch handler is parsed inside a fresh ScopeDecl, so the error variable it binds is not visible after the do/catch statement.                                                                                                         | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`catch-handler-variable-not-visible-outside.slang`](catch-handler-variable-not-visible-outside.slang)                     |
| A do-while loop's block body owns its own scope (the parser does not push a fresh ScopeDecl on the DoWhileStmt itself); a local declared in the body is not visible after the loop.                                                     | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`do-while-body-local-not-visible-outside.slang`](do-while-body-local-not-visible-outside.slang)                           |
| A function parameter lives in its owning CallableDecl's scope; a sibling function cannot see it.                                                                                                                                        | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`function-parameter-not-visible-from-sibling-function.slang`](function-parameter-not-visible-from-sibling-function.slang) |
| A name inside a NamespaceDecl scope is reachable via explicit `Namespace::name` qualification.                                                                                                                                          | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`namespace-qualified-name-resolves.slang`](namespace-qualified-name-resolves.slang)                                       |
| A switch body parses as a scoped BlockStmt (the parser does not assign SwitchStmt::scopeDecl); a local declared inside one of its cases is not visible after the switch.                                                                | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`switch-scope-local-not-visible-outside.slang`](switch-scope-local-not-visible-outside.slang)                             |
| A typedef declared inside a block lives in the BlockStmt's scope only; it cannot be referenced from a later statement at the enclosing scope.                                                                                           | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`typedef-in-block-not-visible-outside.slang`](typedef-in-block-not-visible-outside.slang)                                 |
| A while loop's block body owns its own scope (the parser does not push a fresh ScopeDecl on the WhileStmt itself); a local declared in the body is not visible after the loop.                                                          | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`while-body-local-not-visible-outside.slang`](while-body-local-not-visible-outside.slang)                                 |
| AggTypeDecl/StructDecl introduces a scope; a method body declared inside the struct can refer to the struct's fields by unqualified name.                                                                                               | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`member-scope-fields-visible-from-method.slang`](member-scope-fields-visible-from-method.slang)                           |
| An accessor body resolves its own setter parameter from the AccessorDecl's scope and the enclosing struct's field through the PropertyDecl and struct scopes above it.                                                                  | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`property-accessor-scope-reaches-struct-field.slang`](property-accessor-scope-reaches-struct-field.slang)                 |
| ExtensionDecl owns a scope; the extended type's member-lookup finds the extension method when called via the receiver.                                                                                                                  | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`extension-method-callable-via-receiver.slang`](extension-method-callable-via-receiver.slang)                             |
| ForStmt owns its own scope; the init-statement's variable is not visible after the loop.                                                                                                                                                | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`for-loop-init-not-visible-outside.slang`](for-loop-init-not-visible-outside.slang)                                       |
| IfStmt itself does not own a scope, but its body parses as a BlockStmt that does; a local declared inside the then-block is not visible after the `if`.                                                                                 | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`if-block-local-not-visible-outside.slang`](if-block-local-not-visible-outside.slang)                                     |
| ModuleDecl/FileDecl members are reachable from any function in the file via the parent-scope walk.                                                                                                                                      | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`file-scope-const-visible-in-function.slang`](file-scope-const-visible-in-function.slang)                                 |
| Struct fields live in the AggTypeDecl's scope only; they are not visible from a free function at file scope.                                                                                                                            | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`member-field-not-visible-outside-struct.slang`](member-field-not-visible-outside-struct.slang)                           |
| The ForStmt's scope holds the init variable; the body sees it for the duration of the loop.                                                                                                                                             | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`for-loop-var-visible-in-body.slang`](for-loop-var-visible-in-body.slang)                                                 |
| The ThisTypeDecl belongs to its InterfaceDecl's scope only, so `This` is undefined in a function at file scope.                                                                                                                         | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`this-type-not-visible-at-file-scope.slang`](this-type-not-visible-at-file-scope.slang)                                   |
| The dedicated ScopeDecl the parser pushes for a lambda parameter list makes the parameter visible in the lambda body, with the enclosing function scope still reachable as its parent.                                                  | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`lambda-param-visible-in-lambda-body.slang`](lambda-param-visible-in-lambda-body.slang)                                   |
| The scope holding a lambda's parameter list is owned by the lambda expression, so the parameter name is undefined in the enclosing function after the lambda.                                                                           | negative   | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`lambda-param-not-visible-outside-lambda.slang`](lambda-param-not-visible-outside-lambda.slang)                           |
| The synthesized ThisTypeDecl member of an InterfaceDecl makes `This` resolve inside the interface's own requirement signatures.                                                                                                         | functional | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)                     | [`this-type-visible-in-interface-requirement.slang`](this-type-visible-in-interface-requirement.slang)                     |
| Lookup walks to the parent scope when a name is not found in the current scope; an inner block can see an enclosing block's local.                                                                                                      | functional | [#scope-walking-order-during-lookup](../../../../design/name-resolution/scopes.md#scope-walking-order-during-lookup) | [`block-scope-resolves-outer-from-inner.slang`](block-scope-resolves-outer-from-inner.slang)                               |
| The close-spelling walk skips the core module, so a typo whose only near match is a builtin gets an undefined-identifier diagnostic with no "did you mean" suggestion.                                                                  | negative   | [#scope-walking-order-during-lookup](../../../../design/name-resolution/scopes.md#scope-walking-order-during-lookup) | [`undefined-identifier-no-suggestion-from-core-module.slang`](undefined-identifier-no-suggestion-from-core-module.slang)   |
| Visiting `currentScope` first means an inner declaration with the same name as an outer one shadows it inside the inner block.                                                                                                          | functional | [#scope-walking-order-during-lookup](../../../../design/name-resolution/scopes.md#scope-walking-order-during-lookup) | [`block-scope-shadowing.slang`](block-scope-shadowing.slang)                                                               |
| When a VarExpr fails to resolve, the checker reuses the scope-chain traversal to find a close spelling and attaches a "did you mean" suggestion to the undefined-identifier diagnostic (E30015).                                        | negative   | [#scope-walking-order-during-lookup](../../../../design/name-resolution/scopes.md#scope-walking-order-during-lookup) | [`undefined-identifier-suggests-close-spelling.slang`](undefined-identifier-suggests-close-spelling.slang)                 |
| A `using` declaration splices the named namespace's scope in as a sibling, making the namespace's members reachable without qualification.                                                                                              | functional | [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                                       | [`using-namespace-members-unqualified.slang`](using-namespace-members-unqualified.slang)                                   |
| A `using` of a namespace declared in multiple blocks makes members from every block reachable unqualified, because the splice covers the namespace's owned and sibling scopes.                                                          | functional | [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                                       | [`using-reopened-namespace-members-unqualified.slang`](using-reopened-namespace-members-unqualified.slang)                 |
| Re-opening the same namespace name reuses the existing NamespaceDecl and links siblings; members from both declarations are reachable via the same qualified name.                                                                      | functional | [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                                       | [`namespace-reopened-merges-members.slang`](namespace-reopened-merges-members.slang)                                       |
| When a module is imported, the checker adds its scope as a sibling so names from it are reachable without explicit qualification; the core module's builtins (e.g. `sin`) are reachable through this mechanism.                         | functional | [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                                       | [`builtin-from-implicit-core-module.slang`](builtin-from-implicit-core-module.slang)                                       |

## Untested claims

| Claim                                                                                                                                                                                                                                                                     | Reason                | Anchor                                                                                                     | Why untested                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Scope` is a three-field record (`containerDecl`, `parent`, `nextSibling`) and `containerDecl` is deliberately an unowned pointer so a `Scope` cannot keep an AST node alive.                                                                                             | internal-source-fact  | [#concepts](../../../../design/name-resolution/scopes.md#concepts)                                         | The field layout and the ownership convention have no user-visible consequence; only their effects (parent walk, sibling walk) are observable, and those are covered by the walking-order and sibling-scope rows above.                                                                                                                                                                                  |
| The parser threads scopes with `currentScope` / `currentLookupScope` / `outerScope` and the `PushScope` / `pushScopeAndSetParent` / `PopScope` helpers, each ending in `resetLookupScope()` so the two pointers never drift apart.                                        | internal-source-fact  | [#parser-scope-construction](../../../../design/name-resolution/scopes.md#parser-scope-construction)       | These are parser-internal bookkeeping names. A drift between `currentScope` and `currentLookupScope` would surface as a mis-resolved name, which the positive/negative pairs above already probe; there is no directive that observes the pointers themselves.                                                                                                                                           |
| A `BlockStmt` whose body contains zero declarations still gets a fresh `ScopeDecl`, and the body becomes an `EmptyStmt`.                                                                                                                                                  | internal-source-fact  | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes) | An empty block declares nothing, so no name-visibility observation distinguishes "fresh scope" from "no scope" for it. The non-empty half of the same bullet — per-decl hiding inside a block — is covered by [`block-scope-forward-ref-rejected.slang`](block-scope-forward-ref-rejected.slang) and [`block-decl-init-resolves-outer-same-name.slang`](block-decl-init-resolves-outer-same-name.slang). |
| `SemanticDecl` and `AttributeDecl` are direct `ContainerDecl` subclasses that own a scope via `ContainerDecl::ownedScope`.                                                                                                                                                | internal-source-fact  | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | These decls back semantic / `[attribute]` machinery whose name-resolution behaviour the doc does not state; the `ownedScope` field has no described user-observable consequence.                                                                                                                                                                                                                         |
| `SynthesizedStructDecl`, `GLSLInterfaceBlockDecl`, `AssocTypeDecl`, and `GlobalGenericParamDecl` are `AggTypeDecl`-family subclasses that own scopes via `ContainerDecl::ownedScope`.                                                                                     | internal-source-fact  | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | The row states class-hierarchy facts; the doc names no visibility consequence that distinguishes them from the tested `StructDecl` / `InterfaceDecl` cases, and `SynthesizedStructDecl` has no surface syntax at all.                                                                                                                                                                                    |
| `ThisTypeDecl` inherits the `ownedScope` field but the field stays null because it is never pushed as a parser scope.                                                                                                                                                     | internal-source-fact  | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | Whether the field is null is not observable from source; the reachable consequence — `This` resolves inside the interface and nowhere else — is covered by [`this-type-visible-in-interface-requirement.slang`](this-type-visible-in-interface-requirement.slang) and [`this-type-not-visible-at-file-scope.slang`](this-type-not-visible-at-file-scope.slang).                                          |
| `GpuForeachStmt` declares `ScopeStmt::scopeDecl`.                                                                                                                                                                                                                         | internal-source-fact  | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | The statement is generated internally for GPU-foreach lowering and has no documented surface spelling a test could write.                                                                                                                                                                                                                                                                                |
| `parseTargetSwitchStmtImpl` creates a per-case `ScopeDecl` for `__target_switch` / `__stage_switch` rather than one on the statement.                                                                                                                                     | internal-source-fact  | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | The doc states parser mechanics without committing to a user-visible boundary (e.g. whether a local declared in one case is visible in another), so a test cannot anchor an expectation. See the matching doc-gap row.                                                                                                                                                                                   |
| `CompileTimeForStmt` declares `ScopeStmt::scopeDecl`.                                                                                                                                                                                                                     | out-of-bundle         | [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | The `$for` surface and its iteration behaviour are exercised in `design/ast-reference/statements` (`compiletimefor-*.slang`); this page adds no scope-boundary claim for it beyond the generic `ScopeStmt` statement.                                                                                                                                                                                    |
| When the source language is HLSL the parser builds an `UnscopedForStmt` and skips the scope push, so the `for` init variable leaks into the surrounding scope.                                                                                                            | needs-cli-test        | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes) | HLSL mode is selected by the input file's language, not by anything a `.slang` test file can say; compiling the identical source as `.hlsl` does show the leak. A wrapper invoking `slangc` on an `.hlsl` input is the shape of test that would verify it.                                                                                                                                               |
| A module split across several `__include`d files gets one `FileDecl` per file attached as a sibling of the module's scope, so lookup inside the module sees the union of all files' members.                                                                              | needs-multi-file-test | [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                             | Sibling-scope use case 1 is only observable across two or more source files; this bundle's single-file directives cannot express it.                                                                                                                                                                                                                                                                     |
| When module B imports module A, the checker adds A's scope as a sibling of B's scope so A's names are reachable in B unqualified.                                                                                                                                         | needs-multi-file-test | [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                             | The explicit `import` form of use case 2 needs a second module file. The implicit core-module half of the same mechanism is covered by [`builtin-from-implicit-core-module.slang`](builtin-from-implicit-core-module.slang).                                                                                                                                                                             |
| `isOwnModuleOrIncludedFileScope` filters what an `import` re-exports: only A's own scope and A's own `FileDecl`s cross into B, so a `using namespace Foo;` written in A, or a module C that A merely imported, does not leak into B — a plain `import` is not transitive. | needs-multi-file-test | [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                             | Three files are needed to state the claim (C, A importing C plus a `using`, B importing A) and a fourth observation point in B; single-file directives cannot express it.                                                                                                                                                                                                                                |
| `FuncConstraintDecl` also lands in an interface or generic scope as a requirement member, but it is synthesized during header checking and has no parser path or scope of its own.                                                                                        | internal-source-fact  | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes) | The decl has no surface spelling; the three spellings that do have one are covered by the three constraint-form rows above.                                                                                                                                                                                                                                                                              |
| Pushing a `Scope` whose `containerDecl` is null is not supported; the bare-`Scope*` overload of `PushScope` exists only to restore a pre-built scope.                                                                                                                     | internal-source-fact  | [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes) | A statement about an internal C++ overload contract that no source text can reach.                                                                                                                                                                                                                                                                                                                       |

## Doc gaps observed

| Anchor                                                                                                     | Kind            | Gap                                                                                                                                                                                                                                                                                                                                                               | Suggested addition                                                                                                                                                                             |
| ---------------------------------------------------------------------------------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#edge-cases-and-failure-modes](../../../../design/name-resolution/scopes.md#edge-cases-and-failure-modes) | missing-surface | The `UnscopedForStmt` bullet says the leak happens "when the source language is HLSL" but never names how a reader selects that language. Compiling the same text as a `.slang` file scopes the loop variable; renaming it to `.hlsl` makes it leak, and passing `-lang hlsl` alongside a `.slang` input does not change the behaviour.                           | Name the surface that selects HLSL parsing (input file extension, and whether `-lang hlsl` is expected to apply to a `.slang` input), so a reader can reproduce the `UnscopedForStmt` path.    |
| [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | missing-surface | The `SwitchStmt` / `TargetSwitchStmt` / `StageSwitchStmt` row says `parseTargetSwitchStmtImpl` creates a per-case `ScopeDecl`, but does not name the user-visible consequence — e.g. whether a local declared in one `__target_switch` case is visible from another case of the same statement.                                                                   | Add one sentence stating the cross-case visibility consequence of the per-case `ScopeDecl` so a test can pin it.                                                                               |
| [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | ambiguous-claim | The `WhileStmt` / `DoWhileStmt` row says "the loop body owns its own scope only when it is a `BlockStmt`" but does not state what happens when the body is a single non-block statement that declares a name: whether the name leaks into the enclosing scope, or the form is rejected outright.                                                                  | State the observable behaviour for a non-block loop body in one sentence (leaks / rejected / unreachable-by-grammar).                                                                          |
| [#scope-bearing-ast-nodes](../../../../design/name-resolution/scopes.md#scope-bearing-ast-nodes)           | missing-example | The `LambdaExpr` row states that the parser pushes a dedicated `ScopeDecl` for the parameter list, owned by the expression rather than by `LambdaDecl`, but shows no example and does not spell out the boundary that follows from it. The tests here assume the natural reading: the parameter is visible in the lambda body and undefined after the expression. | Add a two-line lambda example and one sentence naming the boundary ("the parameter list's scope ends with the lambda expression"), so the positive/negative pair anchors to an explicit claim. |
| [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                             | missing-example | Use case 2 now explains the `isOwnModuleOrIncludedFileScope` filter and why a plain `import` must not be transitive, but gives no example of the shape it guards against, so a reader cannot tell which of `using namespace`, a re-exported `import`, or an `__include`d `FileDecl` survives into the importing module.                                           | Add a three-module example (C, A that imports C and writes `using namespace Foo;`, B that imports A) with a one-line note stating which names B can see.                                       |
| [#sibling-scopes](../../../../design/name-resolution/scopes.md#sibling-scopes)                             | ambiguous-claim | The section enumerates four uses of the sibling chain but never says what happens when two siblings on the same chain define the same name. Whether that is an ambiguity diagnostic, a first-wins rule, or an overload set is left open, so a test cannot anchor here.                                                                                            | Add one sentence naming the outcome when sibling scopes supply the same name, cross-linking `lookup.md` if that is where the rule lives.                                                       |
