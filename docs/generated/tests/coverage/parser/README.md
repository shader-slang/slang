---
generated: true
model: claude-opus-5[1m]
generated_at: 2026-08-05T00:00:00+00:00
source_commit: 5634a0ea1bb355712bcf5f719a3d66fac59d4872
watched_paths_digest: 42f4b911f9253e6d36391e4734dc07282f032209ff1f8ab27d77fa319acb0e13
warning: "Auto-generated. May drift from source. Do not edit by hand."
---

# Tests for coverage/parser

White-box characterization tests targeting under-exercised branches of the
recursive-descent parser in `source/slang/slang-parser.cpp` (81.53% line-covered
by master's hand-written `tests/` suite plus the whole generated suite, with
1363 lines still untouched). These pin the compiler's _current observed
behaviour_ (not a spec). The white-box target is named in each test's
`//META: covers=` field. Older tests in this bundle also carry a `//META: doc_ref`
pointing at the LLM-derived `pipeline/02-parse-ast.md` design doc; that field is
not required in this tree, and the tests added at `source_commit` omit it — the
authority here is the source, per `coverage/METHODOLOGY.md`.

## Intent

Drive syntax edge cases through `slangc`/`slangi` and pin the observed result.
The first generation covered operator declarations, subscript declarations,
attribute placement, GLSL layout qualifiers, generic-argument angle-bracket
disambiguation, expression-statement forms, literal suffixes, and parse-error
recovery. The deepening pass at `source_commit` went after the largest remaining
reachable clusters: `__requireCapability`, `__target_switch`, `__GPU_FOREACH`,
the GLSL interface-block and `cbuffer` data-layout forms, the pointer-width
(`z`) integer-literal suffix and its overflow arms, the two angle-bracket
annotation skips (declarator-level and semantic-level), and the statement-level
error branches in `for`, `do` and the `struct Name = Type` alias.

Parse-error paths use `//DIAGNOSTIC_TEST` (they validate locally even without
FileCheck); positive parse forms use `//TEST:INTERPRET` (interpreter validates
the value) or `//TEST:SIMPLE(filecheck=...)` against emitted HLSL/SPIR-V
assembly. Every diagnostic code, message and emitted token below was copied
verbatim from a real `slangc` run at `source_commit`.

Together the 15 tests added at `source_commit` cover 201 lines of
`source/slang/slang-parser.cpp` that neither `tests/` nor the rest of the
generated suite reached.

## Functional coverage

| Test                                                                                                     | What it pins                                                                                                                                                                                  | covers=                       |
| -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- |
| [`operator-decl-invalid-symbol.slang`](operator-decl-invalid-symbol.slang)                               | An `operator @` declaration with an unrecognized symbol is rejected with E20008 ("invalid operator '@'.").                                                                                    | source/slang/slang-parser.cpp |
| [`operator-overload-plus-lowers.slang`](operator-overload-plus-lowers.slang)                             | A user `operator+` parses, binds, and `a + b` evaluates to the overload's result (2+3 = 5) under the interpreter.                                                                             | source/slang/slang-parser.cpp |
| [`subscript-missing-return-type.slang`](subscript-missing-return-type.slang)                             | A `__subscript` with no `-> ReturnType` clause is rejected with E30901.                                                                                                                       | source/slang/slang-parser.cpp |
| [`empty-parentheses-expression.slang`](empty-parentheses-expression.slang)                               | An empty `()` in value position is rejected with E20005 ("empty parenthesis '()' is not a valid expression.").                                                                                | source/slang/slang-parser.cpp |
| [`unintended-empty-statement.slang`](unintended-empty-statement.slang)                                   | A lone `;` body for an `if` emits the W20101 (E20101) unintended-empty-statement warning and still compiles.                                                                                  | source/slang/slang-parser.cpp |
| [`unexpected-body-after-semicolon.slang`](unexpected-body-after-semicolon.slang)                         | A `void foo(); {}` signature-plus-body is rejected with E20102 ("is this ';' a typo?").                                                                                                       | source/slang/slang-parser.cpp |
| [`decl-not-allowed-in-statement.slang`](decl-not-allowed-in-statement.slang)                             | A `namespace` declaration inside a function body is rejected with E30102 ("namespace is not allowed here.").                                                                                  | source/slang/slang-parser.cpp |
| [`anonymous-scoped-enum.slang`](anonymous-scoped-enum.slang)                                             | An unnamed `enum class { ... }` is rejected with E32001 (anonymous scoped enum not allowed).                                                                                                  | source/slang/slang-parser.cpp |
| [`glsl-layout-qualifier-unrecognized.slang`](glsl-layout-qualifier-unrecognized.slang)                   | An unknown identifier inside `layout(...)` is rejected with E31217.                                                                                                                           | source/slang/slang-parser.cpp |
| [`struct-bracket-attribute-placement-2026.slang`](struct-bracket-attribute-placement-2026.slang)         | A bracketed attribute list placed after `struct` is rejected with E31205 under `-std 2026`.                                                                                                   | source/slang/slang-parser.cpp |
| [`invalid-integer-literal-suffix.slang`](invalid-integer-literal-suffix.slang)                           | An integer literal with an unrecognized suffix (`5q`) is rejected with the invalid-suffix diagnostic (E39999).                                                                                | source/slang/slang-parser.cpp |
| [`invalid-float-literal-suffix.slang`](invalid-float-literal-suffix.slang)                               | A float literal with an unrecognized suffix (`1.0z`) is rejected with the invalid-suffix diagnostic (E39999); the compiler emits it twice (non-exhaustive).                                   | source/slang/slang-parser.cpp |
| [`declaration-declares-nothing.slang`](declaration-declares-nothing.slang)                               | A type-only declaration with no declarator (`int;`) is rejected with E39999 ("declaration does not declare anything").                                                                        | source/slang/slang-parser.cpp |
| [`nested-generic-angle-bracket-disambiguation.slang`](nested-generic-angle-bracket-disambiguation.slang) | A trailing `>>` in `Array<Array<int,2>,3>` is split into two generic closers; the nested read returns 9 under the interpreter.                                                                | source/slang/slang-parser.cpp |
| [`comma-expression-statement.slang`](comma-expression-statement.slang)                                   | A comma operator at statement top level (`a=1, b=2;`) parses and both assignments run (a+b == 3) under the interpreter.                                                                       | source/slang/slang-parser.cpp |
| [`require-capability-unknown-name.slang`](require-capability-unknown-name.slang)                         | `__requireCapability(bogus, spirv_1_3)` reports E36105 for the unknown atom, keeps going past the comma, and still parses the statement to completion.                                        | source/slang/slang-parser.cpp |
| [`integer-literal-pointer-width-suffix.slang`](integer-literal-pointer-width-suffix.slang)               | The `z`/`uz` width suffix types `0xFFz` as `intptr_t` and `0xFFuz` as `uintptr_t`, and `-9223372036854775808z` is demoted from `uintptr_t` back to `intptr_t` while the unsigned form is not. | source/slang/slang-parser.cpp |
| [`integer-literal-too-large-width-suffix.slang`](integer-literal-too-large-width-suffix.slang)           | A decimal literal at INT64_MAX+2 with an `ll` or `z` width suffix warns E40004 and is reinterpreted as unsigned.                                                                              | source/slang/slang-parser.cpp |
| [`target-switch-missing-case-label.slang`](target-switch-missing-case-label.slang)                       | A `__target_switch` body that opens with an ordinary statement is rejected with E20001, "unexpected identifier, expected 'case' or 'default'".                                                | source/slang/slang-parser.cpp |
| [`target-switch-multi-statement-case.slang`](target-switch-multi-statement-case.slang)                   | Three statements under one `case hlsl:` label are collected into a single sequence body, so the emitted HLSL folds to 111.                                                                    | source/slang/slang-parser.cpp |
| [`effect-annotation-angle-bracket-skip.slang`](effect-annotation-angle-bracket-skip.slang)               | Under `-enable-effect-annotations`, a `< ... ; ... ; >` clause after a declarator is skipped as a legacy effect annotation and the declaration survives.                                      | source/slang/slang-parser.cpp |
| [`glsl-interface-block-forms.slang`](glsl-interface-block-forms.slang)                                   | Under `-allow-glsl`, a `uniform` block lowers as a constant buffer (`_std140` suffix), an `out` block becomes an entry-point varying, and an `out gl_*` redeclaration is dropped.             | source/slang/slang-parser.cpp |
| [`glsl-layout-cbuffer-data-layout.slang`](glsl-layout-cbuffer-data-layout.slang)                         | Under `-allow-glsl`, `layout(std140/std430/scalar)` in front of an HLSL `cbuffer` selects the constant buffer's data layout (`_std140` / `_std430` / `_natural`).                             | source/slang/slang-parser.cpp |
| [`gpu-foreach-statement.slang`](gpu-foreach-statement.slang)                                             | The hard-coded `__GPU_FOREACH(dev, dims, LAMBDA(uint3 id) { call; });` form parses and lowers to `kernelCall_0_wrapper(...)`. Tagged `characterization-unverified`.                           | source/slang/slang-parser.cpp |
| [`do-statement-without-while.slang`](do-statement-without-while.slang)                                   | A `do { }` body followed by neither `while` nor `catch` is rejected with E20004, "expected 'while' or 'catch'".                                                                               | source/slang/slang-parser.cpp |
| [`for-init-not-a-declaration-or-expression.slang`](for-init-not-a-declaration-or-expression.slang)       | A block statement as a `for` initializer is rejected after the fact with E20001, "unexpected statement, expected expression", plus the two cascading `;` syntax errors.                       | source/slang/slang-parser.cpp |
| [`struct-type-alias-missing-semicolon.slang`](struct-type-alias-missing-semicolon.slang)                 | The `struct Name = Type` alias form requires a terminating `;` and reports E20001 naming the token found instead, then continues parsing.                                                     | source/slang/slang-parser.cpp |
| [`interface-partial-accessor-default-impl.slang`](interface-partial-accessor-default-impl.slang)         | An interface property whose `get` has a default body while `set` does not is rejected with E30318, attached to the accessor that carried the body.                                            | source/slang/slang-parser.cpp |
| [`semantic-annotation-clause-skipped.slang`](semantic-annotation-clause-skipped.slang)                   | An angle-bracketed annotation after a semantic (`: SV_Position < int hint = 3; >`) is skipped wholesale with no flag required, leaving `SV_Position` intact in the emitted HLSL.              | source/slang/slang-parser.cpp |
| [`semantic-not-identifier-or-bitfield.slang`](semantic-not-identifier-or-bitfield.slang)                 | A token after `:` that is neither a semantic name nor an integer bit-field width (here `1.5`) is rejected with E20001 expecting an identifier.                                                | source/slang/slang-parser.cpp |

## Unreachable gaps

| Gap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Why not targeted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Parser::ParseGLSLInterfaceBlock` (slang-parser.cpp:6448-6464) and, with it, every `GLSLInterfaceBlockDecl` consumer                                                                                                                                                                                                                                                                                                                                                                                                   | **Dead code.** The function has no caller anywhere in `source/` — grep finds only its declaration (line 208) and its definition. `GLSLInterfaceBlockDecl` is created nowhere else, so the `isDeclAllowed` arm for it (5414-5422), the `slang-check-modifier.cpp` predicates and the `slang-lower-to-ir.cpp:12449` branch are all dead too. GLSL interface blocks actually reach `ParseBufferBlockDecl` — see `glsl-interface-block-forms.slang`. Candidate for removal, not a test.                                                                                                                         |
| `_fixIntegerLiteral` (slang-parser.cpp:8194-8252) and the W40005 `integer-literal-truncated` diagnostic it alone emits                                                                                                                                                                                                                                                                                                                                                                                                 | **Dead code.** Its one call site (9857-9864) is guarded by `as<BasicExpressionType>(intLit->type.type)`, but `parseIntegerLiteralExpr` never sets `type` on the literal expression, so the guard is always false. W40005 is defined in `slang-diagnostics.lua:4137` and has no other emitter, so no input can produce it. The truncation it was meant to perform happens later anyway: `-1u` still emits `4294967295U`. Candidate for removal, not a test.                                                                                                                                                  |
| `isDeclAllowed` arms for `SubscriptDecl` / `PropertyDecl` parents (5443-5454) and for `ConstructorDecl` / accessor / `FuncDecl` parents (5423-5442)                                                                                                                                                                                                                                                                                                                                                                    | Not reachable through the normal parse path. A non-accessor member inside a `__subscript` body is rejected earlier by the accessor-block parser with E20003 ("unexpected identifier"), so control never gets to the nesting check with those parent types. Reaching them would need a producer change, not a different input.                                                                                                                                                                                                                                                                               |
| Expression-position `__last`, `__shapeConcat`, `__shapePermute`, `__shapeSwap`, `__shapeReduce` branches of `parsePrefixExpr` (9726-9774)                                                                                                                                                                                                                                                                                                                                                                              | The shape-pack operators are used in _type_ position (`MockTile<T, __shapeConcat(A, B, Axis)>`), which resolves through the syntax-decl lookup path and never through `parsePrefixExpr`. `tests/language-feature/generics/diagnose-mock-tile-shape-pack-ops-specialize.slang` already exercises the real surface; the prefix-expression arms are a second entry point with no known valid value-position use.                                                                                                                                                                                               |
| SPIR-V inline-assembly parse diagnostics (`UnrecognizedSpirvOpcode`, `SpirvInstructionWithoutResultId`, `SpirvOperandRange`, `SpirvInstructionWithTooManyOperands`)                                                                                                                                                                                                                                                                                                                                                    | Reachable but reach a narrow `spirv_asm { ... }` embedded-assembly sub-grammar; deferred to keep this bundle on the named hint area (general syntax / operators / attributes / layout / literals / recovery). A dedicated spirv-asm-parser coverage bundle would fit them better.                                                                                                                                                                                                                                                                                                                           |
| `Unimplemented` parse sites                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Defensive markers for partially-parsed constructs; emitting them depends on internal feature flags rather than a stable surface input, so they are not a reliable characterization target.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `IntegerLiteralTooLarge` (E10012, legacy non-`E`-prefixed catalog format)                                                                                                                                                                                                                                                                                                                                                                                                                                              | Reachable (a >64-bit literal triggers it), but it is emitted in the legacy diagnostic-catalog format (`file(line): error 10012:`) rather than the rich `E####` form, which the DIAGNOSTIC_TEST annotation matcher pins less reliably; left out to avoid a fragile text-position match.                                                                                                                                                                                                                                                                                                                      |
| Internal modifier parsers: `parseTargetIntrinsicModifier`, `parseSpecializedForTargetModifier`, `parseGLSLExtensionModifier`, `parseGLSLVersionModifier`, `parseWGSLExtensionModifier`, `parseSemanticVersion` / `parseSPIRVVersionModifier` / `parseCUDASMVersionModifier`, `parseBuiltinTypeModifier`, `parseBuiltinRequirementModifier`, `parseIntrinsicTypeModifier`, `parseImplicitConversionModifier`, `parseVolatileModifier`, `parseCoherentModifier`, `parseIROp` / `parseIntrinsicOpModifier` (~10057-10640) | **Reachable, not unreachable — left for a follow-up bundle.** Each of `__target_intrinsic(hlsl, "foo")`, `__specialized_for_target(hlsl)`, `__glsl_extension(...)`, `__glsl_version(450)`, `__spirv_version(1.3)`, `__cuda_sm_version(7.0)`, `__wgsl_extension(f16)`, `__builtin_type(1)`, `__builtin_requirement(1)`, `__implicit_conversion(1)`, `__intrinsic_op(add)`, `volatile`, `coherent` compiles cleanly from user source and hits its parser (verified: `__spirv_version(1.3)` executes 10203-10245). This bundle is at its `size_cap_files` of 30, so they are recorded here rather than tested. |
| `parseMagicTypeModifierImpl` (~10560-10590)                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Crash, not a test.** `__magic_type(FloatType)` on a user struct terminates `slangc` with SIGSEGV and no diagnostic. Recorded as finding `parser-magic-type-modifier-on-user-struct-sigsegv`.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `parseCompileTimeStmt`'s non-`for` branch (6906-6910)                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Reachable, not unreachable** — `$bogus;` produces E20003 "unexpected identifier". Dropped from this generation only because the bundle reached its 30-file cap; it is the smallest of the reachable gaps found (5 lines).                                                                                                                                                                                                                                                                                                                                                                                 |

## Doc gaps observed

| Anchor                                                                         | Kind                  | Gap                                                                                                                                                                                                                                                                                                                                       | Suggested addition                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------ | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#modifier-parsing](../../../design/pipeline/02-parse-ast.md#modifier-parsing) | undocumented-behavior | The section describes modifier and attribute parsing generically but does not mention that a bracketed attribute list placed _after_ the `struct` keyword is version-gated: silently accepted before 2025, a deprecation warning (E31204) in 2025, and a hard error (E31205) in 2026.                                                     | Add a note (or a small version-by-behaviour table) under modifier parsing stating that post-keyword `struct [attr] Name` placement is deprecated in 2025 and removed in 2026, naming the diagnostic each version emits.                     |
| [#failure-modes](../../../design/pipeline/02-parse-ast.md#failure-modes)       | undocumented-behavior | The failure-modes section does not list the statement-context declaration restriction: a `namespace` (and similarly `import`) is accepted only at file/module scope and emits E30102 ("... is not allowed here.") when written inside a function body.                                                                                    | Add a row naming E30102 and the set of declaration kinds that are file/module-scope-only, so a reader knows which declarations the statement parser rejects.                                                                                |
| [#parser](../../../design/pipeline/02-parse-ast.md#parser)                     | undocumented-behavior | Nothing describes the two independent angle-bracket annotation skips: the declarator-level one, gated on `-enable-effect-annotations` and disambiguated by scanning for a `;` before the matching `>`, and the semantic-level one in `_parseOptSemantics`, which needs no flag and always treats a `<` after a semantic as an annotation. | Add a short subsection covering both skips, stating which one is flag-gated, how the declarator-level disambiguation distinguishes a generic argument list (`<let`, `< X :`) from an annotation, and that both discard the clause entirely. |
| [#parser](../../../design/pipeline/02-parse-ast.md#parser)                     | undocumented-behavior | The pointer-width integer-literal suffix (`z` / `Z`, optionally with `u`) is not described anywhere in `docs/language-reference/expressions-literal.md` or the design doc, even though it selects `intptr_t` / `uintptr_t` and has its own overflow arms distinct from `ll`.                                                              | Document the `z` suffix alongside `l` / `ll` / `u` in the literal-suffix rules, including that a decimal `z` literal at INT64_MAX+1 becomes `uintptr_t` unless a unary minus follows, and that INT64_MAX+2 and above warn W40004.           |
