@tangent-vector Spike done — draft PR #PLACEHOLDER adds the AST-level `ExistentialType` along the lines you proposed. Headline: **yes, it rules out Reproducer 2 at type-check.**

**What the change does (minimal, per your "minimize churn" scope):**
- New `ExistentialType(interfaceOrConjunction) : Type` (`dyn IFoo`), distinct from a `DeclRefType` to the `InterfaceDecl`. Prints as `dyn IFoo`; canonicalizes to a distinct `ExistentialType` (never collapsing to the interface, which `Val::equals`' pointer-identity comparison requires).
- In the subtype/conformance check, `dyn IFoo` does not conform to `IFoo`, and no concrete `X` is a subtype of `dyn IFoo` (reflexivity `dyn IFoo : dyn IFoo` preserved).
- Lowers to the same IR as the interface.
- The interface→existential coercion is formed at the **explicit generic-type-argument** site, and — to keep the spike low-churn — only for **required** conformance constraints (`T : IFoo`); equality and `where optional` constraints, and unconstrained container uses (`Optional<IFoo>`, `IFoo[]`), are left as the interface.

**Results (measured on a from-scratch build at base `b4853080d174f36bc9e7b2d8d8e9fb71d6fc38db`):**

| case | before | after |
| --- | --- | --- |
| **Reproducer 2** `callStatic<IV>()` | ICE `slang-lower-to-ir.cpp:15156` `irWitnessTable` | ✅ `error[E38029]: type argument 'dyn IV' does not conform to the required interface 'IV'` |
| **Reproducer 1, generic form** `makeZero<IV>()` | ICE | ✅ same clean `E38029` |
| **Reproducer 1, bare form** `IV.dzero()` | ICE | ❌ still ICE — not covered by this pass |

So it rules out Repro 2 (your validation target) **and** the generic form of Repro 1, reusing the *existing* `E38029` diagnostic — no new diagnostic was needed. The **bare `IV.dzero()`** form is not fixed: there `IV` is a static-member-access base, not a generic argument, so it never reaches the formation site. I didn't investigate that path in this spike — covering it would need a separate look at where/how an interface used as a static-member-access base should be rejected. This matches your hedge that the change may not be the entirety of the fix.

**On the cost of the full rule (relevant to your "put off for too long" framing):** applying the existential formation to *all* explicit generic arguments (not just conformance-constrained ones) broke **68** regression base files across four directories, because much of the dynamic-dispatch/existential machinery relies today on an interface being usable as a generic argument, and `dyn IFoo` is not yet member-transparent in the checker. That is directional evidence (a lower-bound sample, not the all-contexts total) that the systematic version is a large change requiring first-class checker support for `ExistentialType`. Narrowed to required-conformance constraints, the change is clean: generics 251/251, interfaces 75/75, autodiff 900/900, dynamic-dispatch 695/695 all pass. Within those four suites, the only existing-test change is that two tests that explicitly specialize a generic with an interface now report `E38029` instead of `E33180` — both remain correct rejections; you may prefer to keep `E33180` as the surface diagnostic there.

Reciprocal check on the crash-surface sibling #10892: unchanged by this PR.

The PR is a **draft** and does not assert `Closes #12430` — it rules out Reproducer 2 but leaves the bare Reproducer 1 form and the broader rollout open for your call on direction.

<sub>🤖 Generated by an automated Slang coworker — may be inaccurate. A human maintainer should verify.</sub>
