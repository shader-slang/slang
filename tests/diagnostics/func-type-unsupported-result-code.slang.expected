result code = -1
standard error = {
error[E55216]: function-typed value is not supported on this target
  --> tests/diagnostics/func-type-unsupported-result-code.slang:18:29
   |
18 | static functype(int) -> int gFn = addOne;
   |                             ^^^ a function-typed value cannot be represented when generating code for the current target; it is supported only where the value can be resolved at compile time, so name the function directly at the call site (or pass it as an argument that names a function directly) instead of storing it in a variable or selecting it at runtime
---'
}
standard output = {
}
