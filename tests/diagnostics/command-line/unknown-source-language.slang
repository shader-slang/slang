// unknown-source-language.slang

// Unknown source file extension (can't deduce language)

//DIAGNOSTIC_TEST:SIMPLE(diag=CHECK):batmobile.car
//CHECK: can't deduce language for input file 'batmobile.car'
