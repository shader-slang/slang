// Precomputed constants for PermutoEncoder
// Generated from tiny-cuda-nn with configuration:
// - Dimensions: 2, FeatureDimensionPerEntry: 2, Log2HashmapSize: 8, MaxLevels: 4
// - BaseScale: 4.0f, Log2PerLevelScale: 1.5f, MaxLevelRatio: 1.1f, Seed: 1337
//
// This file is designed to be easily generated by C++ code with flat arrays.

// Encoder configuration parameters
public static const int lc_nPosDims = 2;
public static const int lc_featureDimPerEntry = 2;
public static const uint lc_log2HashmapSize = 8;
public static const uint lc_nLevels = 4;
public static const uint lc_nFeatures = 8;  // lc_nLevels * lc_featureDimPerEntry
public static const uint lc_hashmapSize = 256;  // 1u << lc_log2HashmapSize

// Grid configuration
public static const uint lc_gridSize = 16;  // 16x16 positions
public static const uint lc_nPositions = 256;  // lc_gridSize * lc_gridSize

// Feature table size
public static const uint lc_totalTableSize = 1024;  // lc_hashmapSize * lc_nLevels
public static const uint lc_featureTableSize = 2048;  // lc_totalTableSize * lc_featureDimPerEntry

// maxLevel = maxLevelRatio * N_LEVELS = 1.1f * 4 = 4.4f
public static const float lc_maxLevel = 4.4f;

// Per-level indices: [level0, level1, level2, level3]
public static const uint lc_levelIndices[4] = { 0u, 1u, 2u, 3u };

// Per-level feature table offsets (in number of feature entries, NOT floats): 
// Level i: i * HASHMAP_SIZE = i * 256
// Note: encodePerLevel will multiply by FeatureDimensionPerEntry to get the float offset
public static const uint lc_offsets[4] = { 0u, 256u, 512u, 768u };

// Per-level feature table sizes (all same for this config)
public static const uint lc_tableSizes[4] = { 256u, 256u, 256u, 256u };

// Per-level scales: baseScale * exp2(level * log2PerLevelScale)
public static const float lc_scales[4] = { 4.0f, 11.313708f, 32.0f, 90.509666f };

// Per-level per-dimension scales: flat array [level0_dim0, level0_dim1, level1_dim0, level1_dim1, ...]
public static const float lc_scalesPerDim[4 * 2] = {
    2.828427f, 1.632993f,      // Level 0
    8.000000f, 4.618802f,      // Level 1
    22.627417f, 13.063946f,    // Level 2
    63.999996f, 36.950417f     // Level 3
};

// Per-level per-dimension shifts: flat array [level0_dim0, level0_dim1, level1_dim0, level1_dim1, ...]
public static const float lc_shiftsPerDim[4 * 2] = {
    -3.523006f, -0.289706f,    // Level 0
    -3.260154f, -3.708823f,    // Level 1
    -1.701558f, 0.283022f,     // Level 2
    4.057071f, 3.367149f       // Level 3
};
