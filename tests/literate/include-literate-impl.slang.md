# Included Literate Implementation

This file is included via `__include` from `include-literate.slang`.

```slang
// TEST_IGNORE_FILE:
implementing include_literate;

public int literateIncludedFunc(int x)
{
    return x + 100;
}
```
