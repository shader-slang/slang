# Literate Helper Module

This module provides a simple function for testing module import
of `.slang.md` files.

```slang
module literate_helper;

public int literateAdd(int a, int b)
{
    return a + b;
}
```
