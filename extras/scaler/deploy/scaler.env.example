# GitHub authentication - choose ONE method:

# Option 1: GitHub App (recommended for production)
# Create a GitHub App at https://github.com/organizations/shader-slang/settings/apps
# Required permissions: Administration (read/write), Actions (read)
SCALER_APP_CLIENT_ID=
SCALER_APP_INSTALLATION_ID=
SCALER_APP_PRIVATE_KEY=

# Option 2: Personal Access Token (simpler, for testing)
# Create at https://github.com/settings/tokens
# Required scope: repo, admin:org
SCALER_TOKEN=
