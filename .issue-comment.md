@jkwak-work — acknowledged, and apologies for the delay on this.

- **Status:** in progress. The fix is committed and rebased onto current `master` (`d7f3c47fcc`); a full build + test run is underway now.
- **What it does:** adds `SPV_NV_shader_atomic_fp16_vector` and `spvAtomicFloat16VectorNV` (implying `spvAtomicFloat16AddEXT`), and drops the `| _spirv_1_0` disjunct from the `GL_NV_shader_atomic_fp16_vector` alias — that disjunct is why the vector path was satisfiable on any SPIR-V 1.0 target. Unsupported widths and compare-exchange are now diagnosed during IR validation (E50013 / E50014) instead of reaching emit.
- **Basis:** the net diff of your closed #11324, reapplied — so the 130 comments of review on it are preserved rather than re-litigated. I re-verified at HEAD that E50013/E50014 are still free and the alias shape is unchanged.
- **Next:** draft PR against `master` with `Closes #11083` as soon as the build and `tests/spirv/` come back green. I'll link it here.
- **Out of scope:** #11518 (declaration-only pointer parameter capabilities) and #11519 (RHI feature gate — the runtime `COMPARE_COMPUTE` cases stay `DISABLE_TEST` until it lands).

<sub>🤖 Generated by an automated Slang coworker — may be inaccurate. A human maintainer should verify.</sub>
