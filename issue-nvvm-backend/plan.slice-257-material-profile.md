# Find the next measured material compilation bottleneck

This ExecPlan follows `.agent/PLANS.md`. The maintainer requires completed NVVM research plans and
reports in local commits; raw evidence remains ignored under `build/`. The authorized loop continues.
Delegation remains unavailable after the agent-thread limit; use the documented local fallback with
one writer and separate source/statistical/identity audits, without claiming an independent reviewer.

## Purpose and Observable Result

Refresh the six intact tiled-brass compile measurements on accepted256 and select at most one bounded
optimization hypothesis from current profiles. Preserve all generated PTX/cubins. Produce distributions,
qualitative stack samples, a primary-source explanation and explicit future promotion/discard gates.
No optimization is implemented, and no material GPU runtime or kernel speed is measured.

## Progress

- [x] 2026-09-25: Read WORKFLOW/STATUS, accepted256 and research251 protocol, report and local drivers.
- [x] Declare scope and measurement protocol before running samples.
- [x] Capture accepted133 source/12 artifact/565 input hashes and current environment; all exact256.
      No competing slangc/ptxas/build/GDB/test processes were present before serial timing.
- [x] Complete132 compiles/66 assemblies; all outputs exact256, all samples retained.
      Fresh semantic medians405.72–410.36ms, wall1305.41–1508.12ms; no paired speedup claim.
- [x] Semantic window selected from complete phases;3 completed profiles preserve PTX and collect218
      stacks (71/73/74), with10 current-builder getter paths across all3 seeds.
- [x] Trace one bounded getter-visibility hypothesis through canonical checking context and epoch
      resolution. Preserve one TLS slot, scope/lifetime/null policy and dynamic loader model.
- [x] Separate source/statistical/identity acceptance passes. Complete report/design/STATUS and
      evidence closure, then include accepted research artifacts in the local commit.

## Surprises and Discoveries

Native commands use approved escalation because the default bwrap network setup fails.
The inspected203 environment overrides202's old Debug paths; use matching RelWithDebInfo throughout.

The first objdump query combined demangling with a mangled symbol selector and returned empty
sections. Retain that inspection attempt; the corrected selector proves the getter/TLS calls. No
benchmark or profile was retried. Of218 stacks,52 hit100 frames, including all35 without the checker.
No fully captured stack falls outside checking; qualitative sampling/truncation remains a limitation.

## Decision Log

- 2026-09-25, parent: Select material-driven research because the next rolling implementation window
  would otherwise contain no material slice. The old constructor hotspot was changed by252; remeasure
  before inferring the next optimization. BF lane/aggregate and FP8 roles can wait for this bounded study.
- Reuse251/245's definitive protocol: two opposite six-identity orders, two warmups and nine measured
  fresh processes per cell per round; then two warmups and nine independent assembly samples per cell.
  This gives132 compiles/66 assemblies,108/54 measured samples and24/12 warmups. Keep every attempt.
- Profile separately after inspecting measured phases. If SemanticChecking remains the useful dominant
  phase, reuse the corrected checkAllTranslationUnits-to-generateIR GDB window with seeds257/258/259.
  Qualitative interrupt counts are not CPU fractions or call counts, and debugger times are not timings.

- 2026-09-25, parent: SemanticChecking remains the largest useful directly scoped phase at about406–410ms.
  generateOutput/frontEndExecute are inclusive parents. Use the declared semantic GDB window; keep
  assembly and downstream/lifecycle residuals separate. The fresh perf cycles probe remains denied;
  no profiling permissions change.

## Outcomes and Retrospective

Research is accepted with132 benchmark compiles,66 assemblies and3 completed profiles preserving
accepted256 output. All133 source/12 artifact/565 input identities remain exact;141 primary snapshots
include8 additional dependencies. One future getter-visibility prototype has explicit semantic,
thread/scope/loader and paired performance gates. No optimization is implemented or speedup claimed.

Full256/targeted233/cadence0 and rolling252/254/256 remain authoritative. Research does not advance
implementation cadence. Full256's1707 cells/1668 correct/39 unresolved and18 resolved histories remain
inherited, not freshly executed. Local fallback uses separate audits without an independent-agent claim.

## Context and Current Pipeline

Both eval_buffer and sample_buffer in tests/cuda/complex/tiled_brass_material.slang compile at
NVRTC O3 and NVVM O0/O3, targetingSM80. The material's overloaded/generic calls pass through semantic
checking, IR generation, linking/optimization and backend emission. Slice246 exposed the subtype
predicate and252 exposed the existing metadata constructor for inlining. Profile that current pipeline,
preserving its canonical AST/IR, rather than assuming previous leaf observations remain important.

## Scope and Non-Goals

Research scripts/logs/PTX/cubins/profiles and immutable snapshots under
build/nvvm-loop/slice-257-material-profile; durable plan, five-part report, timing evidence, design
findings and STATUS only. No compiler/provider/runner/fixture/manifest/build-configuration changes,
no runtime dispatch/full-suite rerun, no system profiling-permission, driver, reboot or push changes.
Stop investigation after one defensible bounded candidate or an evidence-grounded no-candidate result.

## Architecture and Invariants

Use run-complex-corpus.py::compile_command for exact accepted source, entry, stage, target, capability,
optimization, backend and performance flags; change only output paths. Every output must match256's
accepted PTX/cubin hash and contain its declared entry/SM80 target. Measure perf_counter around piped
Popen.communicate through process exit, writing logs afterward. Assembly is independent. Do not sum
inclusive phase timers. Historical251/252 timing is unpaired; this study makes no speedup claim.

## Interfaces and Dependencies

Base387930f7651d9ee1f04ba8e563740e8d49b0c76b, clean nvvm-backend branch. Native Ubuntu24.04,
RelWithDebInfo, L4SM89 driver580.126.09, targetSM80, CUDA12.9.2/NVRTC12.9.86, LLVM14.0.6/providerABI41.
Compiler1fc2311e9e0c332f2bff54d65dedb1a225218f740042a2c23c5210e76245c85f and provider
5fe0b977e22b80acc5ee39147c69510a01c09563354a1a67bd9573d1cda1aeab remain unchanged.
Preserve133 source/12 artifact/565 input hashes and18 submodule pins. Source inspection dependencies
outside that baseline get separate immutable snapshots. Material runtime contracts remain unavailable.

## Milestones

1. Adapt inspected251 drivers into a fresh257 root and capture current identity/environment, with
   no competing owned builds/benchmarks/GPU suites. Follow the already-read native slang-build skill.
2. Run serial measurements under timeout --kill-after=30s 30m, each child bounded180 seconds. Retain
   all commands, warmups, samples, errors and outputs. At most4 CPU workers across owned work.
3. Recompute medians, inclusive quartiles, min/max and phase distributions. Choose a profiling window
   from that evidence; run three distinct seeds separately, each completing with exact accepted PTX.
4. Inspect a focused producer-to-consumer source trace for one repeated path. Apply the full input-shape
   audit: canonical source of truth, layer ownership, alternatives and minimal future failing/gating test.
5. Define a future paired optimized prototype and correctness gates, or explicitly decline to select
   an unsupported candidate. Close raw artifacts/references and verify final identities before commit.

## Validation and Acceptance

All132 compiles and66 assemblies must complete and preserve outputs; all108/54 measured samples and
24/12 warmups must remain accounted for. No favorable timing retries/outlier deletion. Three completed
profiles must preserve accepted PTX. Record snapshot caps, truncated stacks and window limitations.
Final133/12/565 identities must match256 exactly. Inherit its full checkpoint with an explicit reference;
no fresh GPU runtime or compiler correctness suite is claimed for unchanged-source research.
Any later production experiment requires predeclared paired timings with reversed orders, exact
artifacts and full checks when shared AST/semantic code changes. No cache/hierarchy/representation
workaround may be justified solely by a faster sample.

## Failure and Recovery

Retain failed or interrupted attempts. Investigate timeout, crash or output mismatch before continuing.
A method correction invalidates its entire batch for that reason, not its values. Do not change
profiling permissions. If samples cannot support a bounded candidate, document that result; do not
expand into unrelated optimizations or force a change to satisfy cadence.

## Artifacts and Hand-Off

Commit completed plan, report.slice-257-material-profile.md, timing-evidence.slice-257.json, design
findings and STATUS. Keep raw commands, distributions, profiles, source snapshots and closed artifact
index in the257 build root. Parent locally audits/commits; no push is authorized.
