# Export only the versioned Slang ABI. In particular, no LLVM archive symbol may become globally
# visible when this module is loaded beside a different LLVM version.
{
    global:
        slang_getNVVMBuilderAPI_V1;
        slang_getNVVMBuilderAPI_V2;
        slang_getNVVMBuilderAPI_V3;
    local:
        *;
};
